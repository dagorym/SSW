# Tactical Test Source Contracts

This document holds the detailed, per-test source-contract catalog for the
tactical test suites under `tests/tactical/*` (and their GUI-live counterparts in
`tests/gui/*`). It was extracted out of `AGENTS.md` so the auto-loaded agent
entry-point context stays slim; consult this file only when you are adding to or
modifying these specific tactical tests.

These are the key source contracts locked by the tactical tests
(`FTacticalGameMechanicsTest`, `FTacticalCombatReportTest`,
`FTacticalAttackResult`, `FTacticalGameMockUITest`, `WXTacticalUIAdapterTest`,
`FTacticalMineDamageFlowTest`, `FTacticalSeekerMovementTest`,
`FTacticalBattleBoardRendererDelegationTest`, and related fixtures):

- **SMC-04**: `drawPlaceMines()` initializes its source-list `y` from `getActionButtonRowBottom()`, not `BORDER` (subsequently changed by SMRIV-01 to `getActionPromptLineY(0)` for the right-column layout); `ACTION_PROMPT_*` constants reserve prompt-text height before the action-button row across all tactical phases.
- **SMC-05**: `drawSeekerMissiles()` contains a `PH_ATTACK_FIRE` branch sourcing pending hexes via `getAllPendingOffensiveFireSeekerHexes()`; pending hexes drawn without rotation, active seekers with heading rotation, branch exits early.
- **SMC-06**: `movementPath` is populated by `resolveActiveSeekersForMovingPlayer()`, starts at pre-move hex, ends at final hex; non-moving seekers have path cleared; second pass starts fresh.
- **SMC-07**: `drawSeekerMissiles` uses heading-rotated `drawCenteredOnHex` for active seekers; `drawSeekerPaths` is gated on `PH_MOVE || PH_SEEKER_ACTIVATION` and draws cyan stepped lines for active seekers with `movementPath.size() >= 2`, so impacting seeker paths remain visible at their final hex while ICM/damage dialogs are displayed during both phases.
- **TSM-010**: `completeMovePhase()` calls `checkForActiveSeekersOnPath` per ship before mine checking; `applyMovementSeekerDamage()` runs before `applyMineDamage()`; detonated seekers are removed exactly once; `clearNonImpactingSeekerMovementPaths()` is called after `applyMovementSeekerDamage()` so surviving non-impacting seekers have their displayed `movementPath` cleared at the same point ship movement routes clear, while `movementAllowance` and `movementTurn` are preserved for subsequent turns (SMFR-05).
- **SMF-01**: `beginOrdnancePlacement()` filters its rebuilt source list to `FWeapon::M` only before entering `BS_PlaceMines`; `beginSeekerPlacement()` calls `rebuildDeployablePlacementSourcesFiltered(FWeapon::SM)` and sets `BS_PlaceSeekers`; `completeMinePlacement()` attempts `beginSeekerPlacement()` and falls through to `BS_SetupAttackFleet` when no SM sources exist; `completeSeekerPlacement()` transitions directly to `BS_SetupAttackFleet` with `toggleActivePlayer()`; neither `beginOrdnancePlacement()` nor `beginSeekerPlacement()` changes state when no sources of their type exist (return false).
- **SMF-02**: `drawPlaceMines()` filters `getDeployablePlacementSources()` to `FWeapon::M`-only rows and shows `m_buttonMinePlacementDone` ("Mine Placement Done"); `drawPlaceSeekers()` mirrors it for `FWeapon::SM`-only rows and shows `m_buttonSeekerPlacementDone` ("Seeker Placement Done") which routes to `completeSeekerPlacement()`; `draw()` dispatches `BS_PlaceSeekers` to `drawPlaceSeekers()`; `drawSeekerActivation()` source-list `y` now starts from `getActionPromptLineY(0)` (top of the lower panel, right column at `lMargin=310`), matching the `drawPlaceMines`/`drawPlaceSeekers` three-column convention (updated from `getActionButtonRowBottom()` by SMRV-02 round5); `drawPlaceMines()` source-list `y` starts from `getActionPromptLineY(0)` (right column, anchored at the top of the lower panel) with instruction text wrapped into the left column via `drawWrappedActionPrompt()` (see SMRIV-01); `drawPlaceSeekers()` source-list `y` now also starts from `getActionPromptLineY(0)` using the three-column layout (middle column at lMargin=310, recall list in right column at recallMargin=620; see SMRIV-02); if the rendered source rows extend below `requestedDisplayHeight`, each of `drawPlaceMines()`, `drawPlaceSeekers()`, and `drawSeekerActivation()` updates `m_lowerPanelLayoutState.requestedDisplayHeight` and calls `applyRequestedDisplayHeight()` so all rows remain visible; `ensureLowerPanelLayoutState()` preserves any height expansion already applied by those helpers (takes max over stats-based height) within the same tactical state/phase so the expansion is not overwritten when the stats-based layout pass runs; when the tactical state or phase changes the max-preserve is skipped so the panel can shrink back to fit the new phase's content (see SMRIV-04); `testMinePlacementDoneButtonLabelReflectsOrdnanceTypes` asserts that a Minelayer in `BS_PlaceMines` shows only the mine button and after `completeMinePlacement()` transitions to `BS_PlaceSeekers` shows only the seeker button; `testDrawPlaceMinesExpandsPanelHeightWhenRowsExceedMinimum`, `testDrawPlaceSeekersExpandsPanelHeightWhenRowsExceedMinimum`, and `testDrawSeekerActivationExpandsPanelHeightWhenRowsExceedMinimum` are source-contract tests that lock the structural shape of each phase's expansion path (variable names, comparison, and `applyRequestedDisplayHeight()` call) — they do not prove runtime behavior; the authoritative behavioral coverage for all three is `testOrdnancePlacementAndActivationPanelHeightAutoExpands` (GUI live test), which drives each phase via offscreen `wxMemoryDC` and asserts that `requestedDisplayHeight` expands to at least `actionButtonRowBottom()` after drawing, verifying the guard end-to-end including the `ensureLowerPanelLayoutState()` max-preservation path; `testDrawPlaceSeekersUsesSeekerSpecificPromptsAndSMFilter` (source-contract) now additionally asserts `int y = getActionPromptLineY(0)` as the source-list y-start (updated by SMRIV-02 from the prior `getActionButtonRowBottom()` anchor).
- **PGS-02** (source-row visibility fix): `applyRequestedDisplayHeight()` now calls `GetParent()->SendSizeEvent()` when the panel minimum height changes, guarded by `m_inResizeReflow` to prevent recursive reflow; this causes `FBattleScreen::applyLayoutPolicy()` to run on the next event-loop iteration so `drawPlaceMines()` and `drawPlaceSeekers()` source rows are actually visible and clickable instead of being clipped in the 120-pixel default canvas; `testOrdnancePlacementAndActivationPanelHeightAutoExpands` (GUI live test) now serves as a regression guard — the former DEFECT comment describing height clobbering has been removed because the `SendSizeEvent` path resolves it — and the assertion that `requestedDisplayHeight` must not decrease after `drawSeekerActivation()` is preserved as a persistence regression guard; `testPlacementSourceRowsArePopulatedAndClickSelectionUpdatesSources` (GUI live test, `TacticalGuiLiveTest`) is the end-to-end behavioral test for AC1-AC5: it constructs two Minelayers, enters `BS_PlaceMines`, asserts `m_shipNameRegions.size() >= 2` (AC1), verifies all row regions start at or above `getActionPromptLineY(0)` (AC3; updated by SMRIV-01 — rows are now anchored to the top-right column, not below the action-button row), simulates a click on row 1 via `checkShipSelectionPublic()` and asserts `getSelectedPlacementSourceIndex() == 1` and `getShip()/getWeapon()` changed to the second Minelayer's M weapon (AC2), places ordnance and asserts `source.shipID` matches the newly selected ship (AC4), and confirms `getShip()/getWeapon()` non-NULL after entry for auto-selection (AC5); `FBattleDisplayTestPeer` was extended with `shipNameRegionCount()`, `shipNameRegion(i)`, and `checkShipSelectionPublic()` to support the new assertions.
- **PGS-03** (additive seeker placement fix): `FTacticalGame::placeOrdnanceAtHex()` now bypasses the `removePlacedOrdnanceForSelection()` toggle path when `getState() == BS_PlaceSeekers` and `selectedSource.weaponType == FWeapon::SM`, calling `placeSeekerFromSelection()` directly instead; this makes every board click during `BS_PlaceSeekers` additive — seeker launcher ammo decrements and a new inactive seeker record is appended on every click, even on repeated clicks to the same hex; mine toggle behavior for `BS_PlaceMines` and attack-phase seeker deployment for `BS_Battle/PH_ATTACK_FIRE` are unchanged; placement stops (returns `false`) when the selected source's launcher ammo is exhausted; `testPreGameSeekerPlacementIsAdditive` in `FTacticalGameMechanicsTest` is the behavioral regression test — it constructs a real `FTacticalGame` with a Minelayer fleet, advances to `BS_PlaceSeekers`, clicks the same hex 3 times, and asserts `getSeekerMissiles().size() == 3`, ammo decremented by 3, and `getPlacedOrdnance()` contains the mine-phase record plus 3 SM-type records (total 4); an ammo-exhaustion click is also verified to return `false` without adding another seeker when initial ammo permits.
- **SMFR-02** (regression fix): `FBattleBoard::draw()` renders placed ordnance via `drawPlacementOrdnanceHexes(dc)` for both `BS_PlaceMines` and `BS_PlaceSeekers` (condition is `m_parent->getState() == BS_PlaceMines || m_parent->getState() == BS_PlaceSeekers`); `FTacticalGame::handleHexClick()` now has a `BS_PlaceSeekers` case that routes SM weapon clicks to `placeOrdnanceAtHex(hex)` — the missing case was the regression that silently dropped board clicks during seeker placement; `placeMineFromSelection()` calls `rebuildDeployablePlacementSourcesFiltered(FWeapon::M)` after a mine is placed (not the unfiltered `rebuildDeployablePlacementSources()`); `placeSeekerFromSelection()` calls `rebuildDeployablePlacementSourcesFiltered(FWeapon::SM)` after a seeker is placed; `placeOrdnanceAtHex()` also uses state-aware filtered rebuilds in the undo path (`BS_PlaceMines` → M-filter, `BS_PlaceSeekers` → SM-filter, other states → unfiltered); `FTacticalBattleBoardRendererDelegationTest` and `FTacticalBattleDisplayFireFlowTest` source-contract assertions for the draw condition now match `"if (m_parent->getState() == BS_PlaceMines || m_parent->getState() == BS_PlaceSeekers) {"`; `testPreGameOrdnancePlacementRecordingBehavior` in `FTacticalGameMechanicsTest` is the behavioral regression test for this fix — it constructs a real `FTacticalGame` with a Minelayer fleet, enters `BS_PlaceMines`, clicks a hex via `handleHexClick()`, asserts ammo decrement + hex in `getMinedHexes()` + placed-ordnance record, advances to `BS_PlaceSeekers` via `completeMinePlacement()`, clicks a seeker hex, and asserts ammo decrement + inactive seeker in `getSeekerMissiles()` + placed-ordnance record.
- **SMFR-03**: `applyMineDamage()` calls `showDamageSummary` unconditionally when mines fire (no guard on `summary.ships.size()`), so zero-damage minefield encounters still show the dialog; `m_lastTriggeredMineHexes` on `FTacticalGame` is populated with triggered hex positions before `requestRedraw()` and cleared only after `showDamageSummary()` returns, enforcing the highlight lifecycle; `FBattleBoard::drawTriggeredMineHexes(dc)` is called from `draw()` inside the `BS_Battle` state guard and shades each triggered hex using `drawShadedHex` with pre-game-placement green (`#00FF00`); `FBattleScreen::getLastTriggeredMineHexes()` delegates to `FTacticalGame::getLastTriggeredMineHexes()`; behavioral tests confirm `getLastTriggeredMineHexes()` returns an empty set on a fresh game and after mine-free `completeMovePhase()`; `testMineEncounterHighlightHexesNonEmptyDuringShowDamageSummaryCallback` (AC1/AC2/AC3) is the end-to-end behavioral test — it constructs a real `FTacticalGame` with an `FMinelayer` defender, places a mine at the attacker's starting hex, installs `FCapturingMineMockUI`, calls `completeMovePhase()`, and asserts `showDamageSummary` was called at least once (AC1), `getLastTriggeredMineHexes()` is non-empty inside the callback (AC2), and empty after `completeMovePhase()` returns (AC3); source-contract tests confirm `showDamageSummary` is called unconditionally, the highlight lifecycle ordering is correct, the `FBattleScreen` delegation is present, and `drawTriggeredMineHexes(dc)` appears inside the `BS_Battle` guard in `FBattleBoard::draw()`.
- **SMFR-04**: `FBattleBoard::drawSeekerMoveCountOverlay(dc)` is called unconditionally inside the `BS_Battle` block in `draw()`, outside any phase guard, so the speed label renders for every active seeker in all battle phases (PH_MOVE, PH_ATTACK_FIRE, PH_DEFENSE_FIRE, PH_SEEKER_ACTIVATION), not only after movement this phase (as it was under SMF-07); the overlay iterates all active seekers and computes `count = movementPath.size()-1` when a path is recorded this phase (`movementPath.size() >= 2`), or falls back to `movementAllowance` between resolution passes when no path is recorded; the `movementPath.size() < 2` filter that previously excluded non-moved seekers has been removed; when multiple seekers share a hex their counts are stacked vertically (per-seeker labels, preserved from SMF-07); `testDrawSeekerMoveCountOverlayCalledUnconditionallyInBSBattle` (source-contract, `FTacticalBattleBoardRendererDelegationTest`) locks the structure: the call site is inside BS_Battle without an intervening phase guard, and the function body uses `movementAllowance` as the fallback instead of filtering on path size; `testSeekerMoveCountLabelFieldsReflectPathAndAllowance` (behavioral model, `FTacticalSeekerMovementTest`) verifies that a freshly activated seeker has a non-negative `movementAllowance` accessible as the fallback label, that after `resolveActiveSeekersForMovingPlayer()` `movementPath.size()-1` is the correct count, and that two co-located seekers retain independent `movementAllowance` values confirming stacked-display correctness; `testSeekerMoveCountOverlayRendersInAllBattlePhases` (behavioral GUI, `TacticalGuiLiveTest`) uses `TestableBattleScreen` and `SeederGame` helper classes (defined in the anonymous test namespace) to inject one active seeker at hex (5,5) with `movementAllowance=3` directly into the model without the full ordnance-placement API, then drives `FBattleBoard::draw()` in `PH_ATTACK_FIRE` via offscreen `wxMemoryDC` and asserts that at least one red pixel appears in the label region of that hex — a pixel-level observable that would FAIL if `drawSeekerMoveCountOverlay()` were re-guarded inside the `PH_MOVE/PH_SEEKER_ACTIVATION` block (because the overlay exits early when no seekers are present and a no-crash assertion alone cannot detect the regression); the test additionally verifies no-crash for `PH_MOVE`, `PH_DEFENSE_FIRE`, and `PH_SEEKER_ACTIVATION`, and AC4 (seeker count unchanged after all draws, confirming display-only behavior).
- **SMFR-05**: `clearNonImpactingSeekerMovementPaths()` on `FTacticalGame` iterates `m_seekerMissiles` and clears `movementPath` on every remaining record; it is called from `completeMovePhase()` after `applyMovementSeekerDamage()` so non-impacting seeker paths disappear together with ship movement routes at end-of-move-phase; only `movementPath` is cleared — `movementAllowance` and `movementTurn` are left intact for next-turn movement bookkeeping; `FBattleBoard::drawSeekerPaths()` is gated on `PH_MOVE || PH_SEEKER_ACTIVATION` so impacting seeker paths remain visible through ICM selection and the damage summary dialog and are removed when the seeker record is erased by `applyMovementSeekerDamage()`; `testClearNonImpactingSeekerMovementPathsPreservesBookkeeping` (behavioral, `FTacticalSeekerMovementTest`) seeds real `FTacticalSeekerMissileState` records with a populated `movementPath` and explicit `movementAllowance`/`movementTurn`, calls `clearNonImpactingSeekerMovementPaths()`, and asserts path is empty while both bookkeeping fields are unchanged; `testNonImpactingSeekerPathClearedAfterDamageApplied` (behavioral) sets up an impacting seeker (on a ship's path) and a non-impacting seeker, calls `applyMovementSeekerDamage()` then `clearNonImpactingSeekerMovementPaths()`, and asserts the impacting seeker is gone, the non-impacting seeker retains its `movementAllowance` and `movementTurn`, and its `movementPath` is empty; `testCompleteMovePhaseCallsNonImpactingClearAfterDamageSourceContract` (source-contract supplement) verifies the call-order structural invariant in `completeMovePhase()`; `testSeekerPathRendersInPHMoveWithMovementPath` (behavioral GUI render, `TacticalGuiLiveTest`) is the AC1 render-side behavioral test — it uses `TestableBattleScreen` to inject one active seeker with a pre-populated `movementPath` (5,5)->(5,7) after `setPhase(PH_MOVE)` so the path is not cleared by `resolveActiveSeekersForMovingPlayer()`, drives `FBattleBoard::draw()` via offscreen `wxMemoryDC` (2000x1500) in `PH_MOVE`, `PH_SEEKER_ACTIVATION`, and `PH_ATTACK_FIRE`, and asserts pixel diffs in path band x=[344..356], y=[290..385] are non-zero for `PH_MOVE` and `PH_SEEKER_ACTIVATION` (drawSeekerPaths ran) and exactly zero for `PH_ATTACK_FIRE` (drawSeekerPaths not called); this completes the AC1 render-side coverage that was previously source-contract only (SMC-07).
- **SMF-03**: `drawOffensiveSeekerPendingRows()` is called from `draw()` (not `drawCurrentShipStats()`) inside a `PH_ATTACK_FIRE` guard, originally at `(leftOffset, getActionButtonRowBottom()+BORDER)` (subsequently changed by SMRIV-03 to `(310, getActionPromptLineY(0))` for alignment with the pre-game placement anchor; subsequently changed again by SMRV-01 round5 to a computed `pendingLMargin = leftOffset + textExtent(widest instruction line) + 2*BORDER` so the panel left edge clears the left-column instruction text); `drawCurrentShipStats()` no longer calls it; if drawn rows extend below `requestedDisplayHeight`, the height is expanded and `applyRequestedDisplayHeight()` is called; `testOffensiveSeekerPendingListRegionVisibilityAndRecall` verifies `m_pendingSeekerRecallRegions` is empty after a draw with no pending seekers and non-`PH_ATTACK_FIRE` phases leave the recall list empty (see SMRIV-03 for AC2/AC3 path-injection and position coverage).
- **SMF-04**: `FTacticalSeekerMissileState` carries an `activationPhaseIndex` int field (not persisted) stamped at activation time; `FTacticalGame` owns `m_seekerActivationPhaseIndex` (initialized to 0 in `reset()`, incremented by `++` in `beginSeekerActivationPhase()`); both `activateSelectedInactiveSeeker()` and `activateInactiveSeekerAtHex()` stamp `activationPhaseIndex = m_seekerActivationPhaseIndex` on the newly activated entry; `getActiveSeekersByMovingPlayerThisPhase()` on `FTacticalGame` filters the full active-seeker list to entries whose `activationPhaseIndex == m_seekerActivationPhaseIndex`; `FBattleScreen::getActiveSeekersByMovingPlayerThisPhase()` delegates to that model method and is declared `const` returning by value; the existing `getActiveSeekersByMovingPlayer()` accessor is unchanged and continues to serve as the full-list accessor for driving movement; `save()` does not reference `activationPhaseIndex`; no wx headers appear in `FTacticalGame.h`.
- **SMRIV-01** (mine-placement layout): `drawPlaceMines()` now lays out the `BS_PlaceMines` phase in two columns: the left column holds the wrapped instruction text (rendered via `drawWrappedActionPrompt()` with `instructionMaxWidth = lMargin - leftOffset - BORDER`) plus the "Mine Placement Done" button below it; the right column (at `lMargin = 310`) holds the ship source-selection rows anchored at `int y = getActionPromptLineY(0)` — the top of the lower panel — so source rows appear beside (not below) the instruction block; `m_shipNameRegions` entries use `wxRect(lMargin, y, ...)` matching the drawn positions so click regions remain aligned with rendered rows; `testPlaceMinesSourceListStartsAtActionButtonRowBottom` (source-contract, `FTacticalBattleDisplayFireFlowTest`) was updated to assert `int y = getActionPromptLineY(0)` (not `getActionButtonRowBottom()`), `drawWrappedActionPrompt(dc,` present in the body, and `m_shipNameRegions.push_back(wxRect(lMargin,y,` still present; `testPlacementSourceRowsArePopulatedAndClickSelectionUpdatesSources` (GUI live test AC3) was updated to check row top positions against `FBattleDisplayTestPeer::actionPromptLineY(0)` (not `actionButtonRowBottomPublic()`); height-expansion and Done-button lifecycle are unchanged.
- **SMRIV-02** (seeker-placement three-column layout): `drawPlaceSeekers()` now uses the same three-column layout convention as `drawPlaceMines()`: the left column holds the wrapped instruction text (via `drawWrappedActionPrompt()` with `instructionMaxWidth = lMargin - leftOffset - BORDER`) plus the "Seeker Placement Done" button; the middle column (at `lMargin = 310`) holds the ship source-selection rows anchored at `int y = getActionPromptLineY(0)` so they start at the top of the lower panel beside the instruction block; the right column (at `const int recallMargin = 620`) holds the placed-seeker recall list also anchored at `int cy = getActionPromptLineY(0)` — both columns start at the same panel-top y so none of the three columns overlaps the others; `m_shipNameRegions` entries use `wxRect(lMargin, y, ...)` and `m_preGameSeekerRecallRegions` entries use `wxRect(recallMargin, cy, ...)` matching their drawn positions; `testDrawPlaceSeekersThreeColumnLayoutMatchesMinePhasePattern` (source-contract, `FTacticalBattleDisplayFireFlowTest`) verifies `int y = getActionPromptLineY(0)`, `int cy = getActionPromptLineY(0)`, `lMargin = 310`, `recallMargin = 620`, `drawWrappedActionPrompt(dc,` present, and `wxRect(lMargin,y,` / `wxRect(recallMargin,cy,` push-back patterns; `testDrawPlaceSeekersUsesSeekerSpecificPromptsAndSMFilter` (source-contract) was updated to assert the `getActionPromptLineY(0)` y-start and absence of `getActionButtonRowBottom()` in the source-list init; `testPlaceSeekersThreeColumnLayoutColumnPositionsAndClickRegions` (behavioral GUI live, `TacticalGuiLiveTest`) is the authoritative behavioral test — it enters `BS_PlaceSeekers` via `FBattleDisplayTestPeer`, drives `drawPlaceSeekers()` via offscreen `wxMemoryDC`, asserts `m_shipNameRegions` left edge >= 310 (AC1), top >= `actionPromptLineY(0)` (AC2), `m_preGameSeekerRecallRegions` left edge >= 620 (AC3), top >= `actionPromptLineY(0)` (AC4), simulates a click on a source row via `checkShipSelectionPublic()` and confirms `getSelectedPlacementSourceIndex()` updates (AC5), and simulates a click on a recall row via `checkPreGameSeekerRecallSelectionPublic()` and confirms the seeker is removed and ammo restored (AC6); `testPreGameSeekerRecallListAppearsAndClickRemovesSeeker` AC4 was updated to assert `recallRect.GetLeft() >= 620` (right-column position) instead of the prior "does not overlap source rows" check.
- **SMRIV-03** (attack-fire recall list anchor): `draw()` now calls `drawOffensiveSeekerPendingRows(dc, 310, getActionPromptLineY(0), 10)` during `BS_Battle/PH_ATTACK_FIRE` — anchoring the offensive-seeker recall list to the top of the bottom panel (`getActionPromptLineY(0)`) in the column to the right of the Done button (`lMargin=310`), consistent with the pre-game placement pattern used by `drawPlaceMines()` and `drawPlaceSeekers()`; the old call used `(leftOffset, getActionButtonRowBottom()+BORDER)` which placed the list below the action-button row; `testActionPromptSpacingContractAppliedAcrossActionPhases` (source-contract, `FTacticalBattleDisplayFireFlowTest`) was updated to assert `drawOffensiveSeekerPendingRows(dc, 310, getActionPromptLineY(0), 10);` (replacing the prior `leftOffset/pendingRegionTop` assertion); `testOffensiveSeekerPendingListRegionVisibilityAndRecall` (behavioral GUI live, `TacticalGuiLiveTest`) was extended with AC2/AC3 coverage that injects a movement path via `TestableBattleScreen::findShipTurnData()` to deploy a pending seeker, then asserts recall regions have `x >= 310` and `y >= getActionPromptLineY(0)` (AC2 — anchored at top of lower panel, not below action-button row), and verifies that clicking a recall region via `checkOffensiveSeekerPendingSelectionPublic()` removes the pending seeker and restores ammo (AC3); `TacticalGuiLiveTest` was extended with `findShipTurnData()` and `checkOffensiveSeekerPendingSelectionPublic()` on `TestableBattleScreen` to support path injection and simulated click recall; note: SMRV-01 round5 subsequently superseded the hardcoded `310` with a dynamically computed margin — see the SMRV-01 round5 entry below.
- **SMRIV-04** (lower-panel height phase-change reset): `LowerPanelLayoutState` gained two new int fields — `lastBattleState` and `lastBattlePhase` (initialized to -1 in the `FBattleDisplay` constructor) — that `ensureLowerPanelLayoutState()` updates on each call to track the most-recently-seen tactical state and phase; when either value differs from the current state/phase (`phaseChanged == true`) the helper skips the max-preserve of `requestedDisplayHeight` so the panel shrinks back to the base-content height for the new phase instead of ratcheting upward permanently; same-phase draws still apply max-preserve so overflowing rows remain visible and clickable within a phase; `testLowerPanelHeightShrinksBackAfterPhaseChange` (behavioral GUI live, `TacticalGuiLiveTest`) is the authoritative test — it constructs an `FBattleScreen` rig with 6 Minelayers to force a tall `BS_PlaceMines` source list, drives two draws in `BS_PlaceMines` via offscreen `wxMemoryDC` (the first draw primes `lastBattleState`/`lastBattlePhase` while `phaseChanged == true` so height is reset to the floor; the second draw has `phaseChanged == false` so max-preserve keeps the expanded height), then transitions to `BS_Battle/PH_MOVE` and draws once more, asserting `requestedDisplayHeight` is less than the `BS_PlaceMines` expanded value (AC1) and no lower than 120 (AC3); AC4 is the regressive-failure guarantee — the test is designed to fail against the pre-SMRIV-04 ratchet-only code (where max-preserve was unconditional) and pass only after the phase-change reset fix.
- **SMRIV-05** (activation-phase seeker removal fix): `beginSeekerActivationPhase()` and `completeSeekerActivationPhase()` in `FTacticalGame` previously contained inline `if (m_ui != NULL) { requestRedraw(); resolvePendingSeekerDetonationDamage(); }` blocks that skipped both the redraw and damage resolution when no `ITacticalUI` was installed, leaving impacting seekers in `m_seekerMissiles` where they could produce a repeated contact on the next resolution pass; the fix replaces both inline blocks with a single `applyMovementSeekerDamage()` call, which handles the UI-present redraw (SMF-06 ordering preserved — redraw before ICM/damage dialogs so the impacting seeker remains visible at its final hex), calls `resolvePendingSeekerDetonationDamage()` when UI is installed, and always calls `clearPendingSeekerContactOutcomes()` then erases detonated seekers from `m_seekerMissiles` after resolution returns, regardless of whether a UI is installed; `testSeekerActivationPhaseResolvesPendingDamageWhenModelHasUI` (source-contract, `FTacticalMineDamageFlowTest`) was updated to assert `applyMovementSeekerDamage()` appears in both `beginSeekerActivationPhase()` and `completeSeekerActivationPhase()` function bodies (replacing the prior `if (m_ui != NULL)` / `resolvePendingSeekerDetonationDamage()` assertions) and verifies the call-ordering invariants `resolveActiveSeekersForMovingPlayer()` before `applyMovementSeekerDamage()` and `applyMovementSeekerDamage()` before `beginMovePhase()` in both functions; `testCompleteSeekerActivationResolvesMovementContactAndSurvivorProgressionRuntime` (behavioral runtime, `FTacticalSeekerMovementTest`) was updated to expect `outcomes.size()==0` (outcomes cleared by `applyMovementSeekerDamage()`) and `seekers.size()==1` (impacting seeker 9101 erased; survivor 9102 intact with correct progression) — previously this test expected `outcomes.size()==1` and `seekers.size()==2` because the damage path was skipped with no UI; `testActivationPhaseImpactingSeekerRemovedAndDoesNotAttackOnNextPhase` (behavioral, `FTacticalSeekerMovementTest`) is the AC1/AC4 primary regression test — it seeds two seekers (impacting seeker 4001 at the defender's hex, non-impacting survivor 4002 at a distant hex), calls `completeSeekerActivationPhase()` with no UI installed, asserts `getSeekerMissiles()` does not contain seeker 4001 (AC1), asserts `getPendingSeekerContactOutcomes()` is empty (AC1), then calls `resolveActiveSeekersForMovingPlayer()` again and asserts that no outcome references seeker 4001 (AC4 — the removed seeker must not attack on the next phase); this test is designed to fail against the pre-fix code (where `applyMovementSeekerDamage()` was not called from `completeSeekerActivationPhase()`) and pass after the fix; `FTacticalPreGameOrdnanceTest.h` include-guard typo `PREAGAME` was corrected to `PREGAME` as an incidental carry fix.
- **SMRIV-06** (placeOrdnanceAtHex dead-branch removal and defensive stale-entry erase): two targeted cleanups to `FTacticalGame::placeOrdnanceAtHex()` in `src/tactical/FTacticalGame.cpp`: (1) removed the unreachable `else if (getState() == BS_PlaceSeekers)` branch that would have called `rebuildDeployablePlacementSourcesFiltered(FWeapon::SM)` in the `removePlacedOrdnanceForSelection` success block — unreachable because the PGS-03 early-return routes all `BS_PlaceSeekers`+SM calls out of the removal block before that code is reached, so the `else if` could never execute; after removal, non-mine non-battle states in the removal success path fall through to the existing `rebuildDeployablePlacementSources()` unfiltered call; (2) added `m_minedHexList.erase(hex)` as a defensive cleanup when the PGS-04 for-loop completes without finding a matching placed-ordnance M record for a hex that is in `m_minedHexList`, preventing a stale minefield entry from blocking re-placement on that hex; `testStaleMinedHexErasedWhenNoPlacedOrdnanceRecord` (behavioral, `FTacticalPreGameOrdnanceTest`) is the AC2 regression test — it uses `FTacticalGameWithStaleInject` (a test-only subclass that exposes `injectStaleMinedHex()`) to insert a hex into `m_minedHexList` with no corresponding placed-ordnance record, then calls `handleHexClick()` for that hex in `BS_PlaceMines` state with a mine launcher selected, and asserts the call returns `true` (stale erase enabled clean placement), the hex is in `getMinedHexes()` backed by exactly one `FWeapon::M` placed-ordnance record, and mine launcher ammo was decremented by 1; without the defensive erase, `placeMineFromSelection()` would find the stale entry in `m_minedHexList` and return `false` (hex already mined), causing the click to silently fail and leaving the stale entry in place; all 6 pre-game ordnance tests (AC1/AC3 — seeker stacking, seeker recall, mine toggle/removal) continue to pass with no regression (224 to 225 total tests).
- **SMRV-01 (round5)** (attack-phase pending-panel computed left margin): `draw()` now computes `pendingLMargin` as `leftOffset + dc.GetTextExtent(widestAttackLine).GetWidth() + 2*BORDER` where `widestAttackLine = "Select legal path hexes to deploy seeker missiles."` — replacing the hardcoded `310` established by SMRIV-03 — so the pending-deployment panel's left edge is placed dynamically to the right of the widest left-column instruction line; the left instruction text is unchanged (not wrapped or clipped); click regions in `m_pendingSeekerRecallRegions` are computed from the new draw position so `checkOffensiveSeekerPendingSelection()` continues to work correctly; `testActionPromptSpacingContractAppliedAcrossActionPhases` (source-contract, `FTacticalBattleDisplayFireFlowTest`) was updated to assert the three computed-margin lines (`const wxString widestAttackLine("Select legal path hexes to deploy seeker missiles.");`, `const int attackTextW = dc.GetTextExtent(widestAttackLine).GetWidth();`, `const int pendingLMargin = leftOffset + attackTextW + 2*BORDER;`) and the `drawOffensiveSeekerPendingRows(dc, pendingLMargin, getActionPromptLineY(0), 10);` call, replacing the prior `drawOffensiveSeekerPendingRows(dc, 310, getActionPromptLineY(0), 10);` assertion; `testOffensiveSeekerPendingListRegionVisibilityAndRecall` AC2 (behavioral GUI live, `TacticalGuiLiveTest`) was updated to assert recall region `x >= computed pendingLMargin` measured via `wxMemoryDC` text extent via a `guiRepoFile()` helper (added to normalize source-contract path resolution from the `GUI_TEST_REPO_ROOT` compile macro introduced by SMRV-T1) rather than the prior `x >= 310` check.
- **SMRV-02 (round5)** (seeker-activation list anchor): `drawSeekerActivation()` now anchors the "Activated seekers:" list at `int y = getActionPromptLineY(0)` (top of the lower panel, right column at `lMargin=310`), matching the `drawPlaceMines`/`drawPlaceSeekers` three-column convention; the previous anchor was `getActionButtonRowBottom()` (established by SMF-05); click regions in `m_seekerActivationRegions` and `m_seekerActivationSeekerIDs` are computed using the same `y` variable so they automatically track the new draw position; `testSeekerActivationPanelListsInstructionAndOneRowPerInactiveSeeker` (source-contract, `FTacticalBattleDisplayFireFlowTest`) was updated to assert `int y = getActionPromptLineY(0)` and `assertNotContains(body, "int y = getActionButtonRowBottom();")` replacing the prior SMF-05 assertion; the SMRV-T2 carry-over relaxed `testBattleDisplayLowerPanelLayoutStatePersistsAcrossPhaseAndGeometryChanges` within-phase resize-back assertion from `CPPUNIT_ASSERT_EQUAL(beforeResizeHeight, restoredHeight)` to `CPPUNIT_ASSERT(restoredHeight >= beforeResizeHeight)`, consistent with the accepted SMRIV-04 design that the lower panel may grow but must not shrink below the pre-resize minimum.
- **Menu bar**: `File`/`Settings`/`Help` order; only `Quit` binds `wxEVT_MENU` and calls `Close(true)`; close path uses `onClose(...)`/`closeBattleScreen(GetReturnCode())`, never `exit(...)`.
- **Speed spin control**: constructed with `wxSP_ARROW_KEYS | wxSP_VERTICAL`, `SetMinSize(GetBestSize())`, under a sizer-managed layout.
- **Tactical zoom image**: loaded via `resolveAssetPath("data/zoom.png")`, not raw base-path concatenation.
