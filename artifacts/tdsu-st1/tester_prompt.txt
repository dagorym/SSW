You are the Tester Agent.

Task summary:
- Updated tactical combat summary aggregation to prevent double counting hull damage when an attack includes both attack-level hullDamage and nested internal-event hull-damage entries.

Modified files:
- include/tactical/FTacticalCombatReport.h

Acceptance criteria to validate (from Planner):
- A hit represented by both attack-level hull damage and matching nested hull-damage events contributes to hullDamageTaken exactly once.
- Standalone report-level events in report.events still contribute their hull damage to summaries.
- Non-hull nested effects still contribute to effect rollups.

Create test files in:
- tests/tactical/FTacticalCombatReportTest.cpp
- tests/tactical/FTacticalCombatReportTest.h

Implementation context for testing:
- In TacticalCombatReportDetail::shouldCountInternalEventHullDamage(), nested event hull damage is now excluded whenever parent attack.hullDamage > 0.
- Attack-level hullDamage remains the canonical player-facing hull total for attack outcomes.
- Nested internalEvents are still preserved in rawEvents/rawAttacksReceived for diagnostics/UI and still contribute to effect summaries through appendEffectSummary().
- Standalone report.events aggregation path was not changed; report-level hull-damage events still add to hullDamageTaken.

Suggested test command(s):
- cd tests/tactical && make && ./TacticalTests
- cd tests && make && ./SSWTests

Shared artifact directory:
- artifacts/tdsu-st1
