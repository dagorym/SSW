Your role is 'tester'. Your task is as follows:

Task summary:
Change the lower-panel height lifecycle in FBattleDisplay so it no longer ratchets up
permanently. The panel keeps expanded height for the duration of the current tactical
phase but resets when the tactical phase changes, allowing it to shrink back when the
next phase needs less height.

Modified files:
- include/tactical/FBattleDisplay.h
- src/tactical/FBattleDisplay.cpp

Acceptance criteria to validate (from Planner):
1. After a phase that expanded the panel height transitions to a phase whose content
   needs less height, the panel's requested/min height decreases (shrinks back) rather
   than remaining at the previously expanded height.
2. Within a single phase, overflowing content still expands the panel and the
   overflowing rows remain visible and clickable.
3. The 120 px minimum-height floor is preserved; the panel never shrinks below it.
4. A behavioral (not source-text-match) test that raises the requested display height
   in one phase, changes the phase to one whose content needs less height, and asserts
   the recomputed height is smaller; this test must fail against the old ratchet-only
   behavior and pass after the fix.

Implementation context:
Two int fields, lastBattleState and lastBattlePhase (both initialised to -1), were
added to the LowerPanelLayoutState struct in include/tactical/FBattleDisplay.h.

In ensureLowerPanelLayoutState() in src/tactical/FBattleDisplay.cpp, the function
now compares m_parent->getState() and m_parent->getPhase() (cast to int) against the
stored last-seen values. If either differs (phaseChanged == true), it updates the
stored values and skips the old max-preserve block:

    if (m_lowerPanelLayoutState.requestedDisplayHeight > requestedHeight)
        requestedHeight = m_lowerPanelLayoutState.requestedDisplayHeight;

so requestedDisplayHeight is recomputed from the current content instead of being
kept at a higher value from a previous phase.

When phaseChanged == false (same phase), the max-preserve is applied as before,
preventing the row-clipping regression that was fixed by the original guard.

The 120 px minimum floor is enforced in all code paths.

Test file location guidance:
- PRIMARY: tests/gui/TacticalGuiLiveTest.cpp
  Add an offscreen wxMemoryDC test that constructs a live FBattleScreen/FBattleDisplay
  rig, drives ensureLowerPanelLayoutState in a phase that causes draw-helper expansion
  (e.g. BS_PlaceMines with a large source list), captures the expanded
  requestedDisplayHeight, then switches state/phase to BS_Battle/PH_MOVE and calls
  ensureLowerPanelLayoutState again, then asserts the new requestedDisplayHeight is
  less than the previously expanded value (and still >= 120).
  This test must fail against the code before this commit (c1d5627) and pass after.

- SUPPLEMENTAL (optional): tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp
  Source-contract coverage confirming lastBattleState and lastBattlePhase exist in
  LowerPanelLayoutState.

Suggested test commands:
    make -C tests tactical-tests && ./tests/tactical/TacticalTests
    make -C tests gui-tests
    cd tests/gui && xvfb-run -a ./GuiTests

Existing validations expected to fail only because approved behavior changed:
None. All 223 tactical tests pass. GUI test module builds clean.

Shared artifact directory:
artifacts/seeker-missile-fixes-round4/SMRIV-04

Startup behavior:
Continue past preflight when blocking inputs are present. Begin authoring the
behavioral test immediately in the same run.

Completion gate:
Do not report success unless all required artifacts exist and all changes are committed.
