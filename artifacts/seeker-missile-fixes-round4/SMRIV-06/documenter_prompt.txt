Your role is 'documenter'. Your task is as follows:

Task summary:
- SMRIV-06: Two cleanup changes to FTacticalGame::placeOrdnanceAtHex() in src/tactical/FTacticalGame.cpp: (1) removed an unreachable else-if branch that would have called rebuildDeployablePlacementSourcesFiltered(FWeapon::SM) during BS_PlaceSeekers in the removePlacedOrdnanceForSelection success block — unreachable because the PGS-03 early-return takes all BS_PlaceSeekers+SM calls before the removal block; (2) added m_minedHexList.erase(hex) as defensive cleanup when the PGS-04 for-loop finds no matching placed-ordnance M record for a hex in m_minedHexList, preventing stale minefield entries.

Acceptance criteria validated:
- AC1: Dead branch removed; no observable placement/removal/undeploy behavior change in any state. Verified by existing seeker stacking, seeker recall, and mine removal tests continuing to pass.
- AC2: Pre-game mine removal still removes minefield and restores ammo to placing ship; stale m_minedHexList entry with no placed-ordnance record is erased by placeOrdnanceAtHex() — verified by new test testStaleMinedHexErasedWhenNoPlacedOrdnanceRecord.
- AC3: Existing pre-game ordnance behavioral tests (mine toggle/removal, additive seeker stacking, seeker undeploy-by-list) continue to pass with no regression. 224 to 225 total tests.

Implementation branch or worktree context:
- Worktree: sf2-SMRIV-06-tester-20260629
- Implementer changed: src/tactical/FTacticalGame.cpp — placeOrdnanceAtHex() only
- Compare against sf2 parent branch for full diff

Files modified by Implementer and Tester to inspect for documentation impact:
- src/tactical/FTacticalGame.cpp (Implementer: removed dead else-if branch, added defensive erase in placeOrdnanceAtHex)
- tests/tactical/FTacticalPreGameOrdnanceTest.cpp (Tester: added testStaleMinedHexErasedWhenNoPlacedOrdnanceRecord and FTacticalGameWithStaleInject harness)
- tests/tactical/FTacticalPreGameOrdnanceTest.h (Tester: added test declaration and updated docblocks for SMRIV-06 coverage)

Test commit:
- 780cdbe

Commands executed:
- make -C tests tactical-tests
- ./tests/tactical/TacticalTests

Final test outcomes:
- 225 tests passed, 0 failed (baseline was 224).
- New test testStaleMinedHexErasedWhenNoPlacedOrdnanceRecord: PASS — verifies AC2 defensive erase.
- All 6 pre-game ordnance tests pass: AC1 and AC3 verified by existing coverage continuing to pass.

Documentation context:
- The placeOrdnanceAtHex() docblock in include/tactical/FTacticalGame.h may need updating to describe: (1) dead branch removal; (2) the defensive m_minedHexList erase when no matching placed-ordnance record is found.
- The FTacticalGame.cpp file-header comment already mentions PGS-03 and PGS-04 — may need an SMRIV-06 entry for the defensive erase.
- doc/test-contracts.md may need a new entry for the testStaleMinedHexErasedWhenNoPlacedOrdnanceRecord behavioral contract.
- AGENTS.md testing section references FTacticalPreGameOrdnanceTest — no structural change needed but content may note the SMRIV-06 addition.

Plan and diff context:
- Plan: plans/seeker-missile-fixes-round4-plan.md, subtask SMRIV-06
- Base branch: sf2 (assumption — the worktree branches from sf2)

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/seeker-missile-fixes-round4/SMRIV-06

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
