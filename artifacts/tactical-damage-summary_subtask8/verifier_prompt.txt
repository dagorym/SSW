You are the Verifier Agent.

Validate the tactical fire-phase report flow implementation in worktree `/tmp/ssw-worktrees/tds-st8-tester-20260322` on branch `tds-st8-tester-20260322`.

Task summary:
- Verify that defensive-fire and offensive-fire resolution both show the shared tactical damage summary dialog before phase transitions, keep destroyed ships visible until acknowledgement, and clear tactical report state after the dialog closes.

Acceptance criteria:
- After defensive fire resolves, the defensive-fire summary dialog is shown before the UI transitions to the next relevant step.
- After offensive fire resolves, the offensive-fire summary dialog is shown before the UI transitions back to movement or toggles the moving player.
- Destroyed ships remain on the board and are still shown in the summary for the report in which they were destroyed.
- `clearDestroyedShips()` runs only after the relevant dialog is acknowledged.
- The summary state is cleared after the dialog is acknowledged so one phase's results do not leak into the next.
- After defensive-fire acknowledgement, the battle advances to offensive fire.
- After offensive-fire acknowledgement, the battle returns to movement and toggles the moving player exactly once.
- The structured attack report preserves attacker, target, weapon, hit, damage, and per-effect detail data from `FTacticalAttackResult`.

Tester work completed:
- Added tactical regression tests in `tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp` and `.h`.
- Wired the new test into `tests/tactical/TacticalTests.cpp` and `tests/tactical/Makefile`.
- Confirmed the actual implementation in `src/tactical/FBattleDisplay.cpp` now uses the tactical report flow described in the handoff.

Test scope and results:
- Tactical-only scope was executed as the smallest valid verification target.
- `make -C tests/tactical` passed.
- `./tests/tactical/TacticalTests` passed.
- Total tactical tests: 26
- Passed: 26
- Failed: 0

Commands run:
- `make -C src libcore.a libgui.a libships.a libstrategic.a libtactical.a libweapons.a libdefenses.a`
- `make -C tests/tactical`
- `./tests/tactical/TacticalTests`

Commit context:
- Test commit hash: `926f2c9`
- Branch: `tds-st8-tester-20260322`

Shared artifact directory to reuse:
- `artifacts/tactical-damage-summary_subtask8`

Follow-up risk:
- The broader `make -C tests` suite was not required for this subtask after the tactical target passed. If you want broader confidence, rerun it, but it is not blocking for this handoff.
