Your role is 'documenter'. Your task is as follows:

Task summary:
- Validated the FBattleScreen modality rework (drop GTK gtk_window_set_modal() grab in favor of wxWindowDisabler, generalize the GTK activate bypass to a delete-event bypass for the title-bar X, and convert WXTacticalUI's single tracked dialog pointer into an innermost-first m_dialogStack) by retiring the now-obsolete TMF-02 AC2 source-contract test and adding behavioral coverage for the parts of the change that can be safely driven in the headless harness.

Acceptance criteria validated:
- The GTK modal grab is no longer used to enforce battle modality -- the obsolete TMF-02 AC2 source-contract test asserting the removed gtk_window_set_modal() call's presence was retired (not replaced with an equivalent wxWindowDisabler source-contract check, per the Verification Policy).
- While the battle is shown, the strategic main frame and the launching parent cannot be interacted with and are restored on close -- covered behaviorally via testWxWindowDisablerDisablesOtherTopLevelsAndRestoresOnDelete, which exercises the wxWindowDisabler mechanism FBattleScreen::ShowModal()/EndModal() rely on directly (two stand-in top-level windows, one skipped, verifying disable-on-construct and restore-on-destroy).
- Child-dialog dismissal is a stack: closing the battle dismisses every live child wxDialog innermost-first -- covered behaviorally for the single-dialog case (hasPendingDialog()/dismissActiveDialog() API correctness) in testWXTacticalUIDismissActiveDialogDismissesNestedStackInnermostFirst, with a source-contract supplement locking the std::vector<wxDialog*> m_dialogStack shape and the rbegin()-first iteration in dismissActiveDialog(), since a genuinely nested two-dialog runtime scenario was confirmed to hang the headless harness indefinitely (documented in the test's comments) and is out of scope for live automated coverage per the Verification Policy.
- The custom FBattleScreen::EndModal m_modalActive guard and the closeBattleScreen() m_closeInProgress guard remain intact and idempotent under reentrancy -- covered behaviorally in testBattleScreenCloseIsIdempotentAcrossDuplicateCloseEvents, which synthesizes a reentrant closeBattleScreen() call from inside the outer call's own Hide() (mirroring two close vectors firing for the same close) and asserts the reentrant call observes m_closeInProgress already set and the frame still ends up hidden.
- Automated tests cover the dismissal-stack ordering, close-path state transitions, and other-window enable/disable bookkeeping as feasible without a live FBattleScreen::ShowModal() loop, per the plan's Verification Policy.
- DEFERRED TO MANUAL VERIFICATION (per explicit Verification Policy instruction, not asserted by any automated test): the minimize decoration being effective, the live mid-battle title-bar X close, and the File->Quit no-assert behavior when more than one child dialog has been live -- these runtime behaviors require FBattleScreen::ShowModal()'s own blocking custom event loop, which cannot be driven live in this headless/xvfb harness without risking runner hangs or heap corruption (confirmed firsthand: an attempted genuinely-nested two-dialog live test reliably hung the runner and was replaced with the single-dialog-plus-source-contract approach described above).

Implementation branch or worktree context:
- Branch: fixes-tmfr-01-tester-20260703 (built on implementer commit d4699c6 'TMFR-01: rework FBattleScreen modality (minimize/close fix, dismissal stack)')
- Modified by Implementer: include/tactical/FBattleScreen.h, src/tactical/FBattleScreen.cpp, include/gui/WXTacticalUI.h, src/gui/WXTacticalUI.cpp, src/gui/SelectCombatGUI.cpp
- Modified by Tester (this commit d49f4fc): tests/gui/TacticalGuiLiveTest.h, tests/gui/TacticalGuiLiveTest.cpp, tests/tactical/WXTacticalUIAdapterTest.h, tests/tactical/WXTacticalUIAdapterTest.cpp, tests/tactical/FTacticalGameMockUITest.h, tests/tactical/FTacticalGameMockUITest.cpp

Files modified by Implementer and Tester to inspect for documentation impact:
- include/tactical/FBattleScreen.h
- src/tactical/FBattleScreen.cpp
- include/gui/WXTacticalUI.h
- src/gui/WXTacticalUI.cpp
- src/gui/SelectCombatGUI.cpp
- tests/gui/TacticalGuiLiveTest.h
- tests/gui/TacticalGuiLiveTest.cpp
- tests/tactical/WXTacticalUIAdapterTest.h
- tests/tactical/WXTacticalUIAdapterTest.cpp
- tests/tactical/FTacticalGameMockUITest.h
- tests/tactical/FTacticalGameMockUITest.cpp

Test commit:
- d49f4fcc0e238e58c74c2cc1727cc7858a84adfa

Commands executed:
- cd tests/gui && make && ./GuiTests
- cd tests && make tactical-tests && ./tactical/TacticalTests
- cd tests && make && ./SSWTests
- make (repo root)

Final test outcomes:
- cd tests/gui && make && ./GuiTests -> 67 tests, 1 pre-existing failure (testLocalGameDialogLaunchesPredefinedAndCustomModalChains, confirmed flaky/pre-existing and unrelated to TMFR-01 files per the implementer report; reproduced as flaky across repeated runs -- sometimes passes, sometimes fails with the same 'centered' assertion, both before and independent of this subtask's test changes)
- cd tests && make tactical-tests && ./tactical/TacticalTests -> 251 tests, 0 failures, stable across 3 repeated runs
- cd tests && make && ./SSWTests -> 195 tests, 0 failures, stable across 2 repeated runs
- make (repo root) -> full SSW + BattleSim executable and test-library build succeeds with no errors

Documentation context:
- doc/test-contracts.md contains a TMF-02 entry (around the AC2 sentence) that documents the now-retired testBattleScreenShowModalContainsGtkWindowSetModal source-contract test by name; that entry is now stale and should be updated or removed by the Documenter to reflect its retirement and the new TMFR-01 tests added in this commit, since AGENTS.md scopes editing that catalog to whoever is adding/modifying the specific tests it describes and this Tester run is not authorized to edit documentation files outside the test directories.
- AGENTS.md's tactical test source-contract catalog reference and the TacticalGuiLiveTest.h/.cpp per-test Doxygen comments added in this commit are the canonical description of the new tests' behavior and rationale; no other doc/*.md content describes FBattleScreen modality behavior that would need updating for this subtask specifically.

Plan and diff context:
- Plan: plans/tactical-misc-fixes-round2-plan.md (TMFR-01 subtask)
- Shared artifact directory: artifacts/tactical-misc-fixes-round2/TMFR-01
- Diff base for this subtask: implementer commit d4699c6 vs prior tip 7c205d7; tester commit d49f4fc adds only test-file changes on top
- TMFR-02 (Turn Left/Right enable logic), TMFR-03 (turn-panel layout/caption), and TMFR-04 (GUI test-suite robustness/author-tag dedup) are separate, file-disjoint subtasks per the plan and were not touched by this subtask's implementer or tester work.

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/tactical-misc-fixes-round2/TMFR-01

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
