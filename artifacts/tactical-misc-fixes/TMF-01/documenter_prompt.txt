Your role is 'documenter'. Your task is as follows:

Task summary:
- TMF-01: Single-sourced the three PH_SEEKER_ACTIVATION instruction strings in
  drawSeekerActivation() by extracting them into static const wxString constants
  (SEEKER_ACTIVATION_PHASE_INSTRUCTION, SEEKER_ACTIVATION_CLICK_INSTRUCTION,
  SEEKER_ACTIVATION_DEACTIVATE_INSTRUCTION) in FBattleDisplay, mirroring the
  SEEKER_DEPLOY_INSTRUCTION pattern. All six use sites (3 GetTextExtent + 3 DrawText)
  replaced with the constants. Tests updated: source-contract assertions now check
  constant-based DrawText forms; constant definitions verified with correct string values;
  inline literal duplicates confirmed absent; AC-b in GUI live test strengthened to check
  region.GetX() >= leftOffset + doneButtonRightExtent + 2*BORDER.

Acceptance criteria validated:
- AC-1: Each instruction string is defined exactly once as a shared constant and used by both the measurement and drawing paths in drawSeekerActivation(). PASS.
- AC-2: No duplicated inline copy of any of the three literals remains in production code. PASS.
- AC-3: The seeker-activation panel renders identically (wording, layout, dynamic margin unchanged). PASS.

Implementation branch or worktree context:
- Implementation branch: tmf-TMF-01-implementer-20260630 (commit ceb75e8)
- Tester branch: tmf-TMF-01-tester-20260630 (test commit 91cb5ec)
- Base branch for diff: master (or the pre-TMF-01 state on the tactical-misc-fixes branch)

Files modified by Implementer and Tester to inspect for documentation impact:
- include/tactical/FBattleDisplay.h (added SEEKER_ACTIVATION_*_INSTRUCTION static const declarations; updated Doxygen Last Modified date)
- src/tactical/FBattleDisplay.cpp (defined three constants; replaced six inline use sites in drawSeekerActivation(); updated Doxygen)
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp (updated source-contract assertions for TMF-01)
- tests/gui/TacticalGuiLiveTest.cpp (added peer method; strengthened AC-b assertion)

Test commit:
- 91cb5ec

Commands executed:
- make -C tests tactical-tests
- tests/tactical/TacticalTests
- make -C tests gui-tests
- make -C tests
- tests/SSWTests

Final test outcomes:
- TacticalTests: 226/226 PASS
- SSWTests: 169/178 pass; 9 pre-existing FGameHeaderDependencyTest failures unrelated to TMF-01
- GuiTests: build clean; xvfb-run unavailable so runtime execution was blocked (environment limitation, not a code defect)

Documentation context:
- doc/test-contracts.md may need updating to catalog the new/updated source-contract entries for testSeekerActivationPanelListsInstructionAndOneRowPerInactiveSeeker and the strengthened testSeekerActivationRowTextShowsPositionAndMarginIsDynamic AC-b
- No change to user-facing behavior; the constants are implementation-only refactoring
- AGENTS.md Tactical test section references testSeekerActivationRowTextShowsPositionAndMarginIsDynamic; verify whether the AC-b strengthening warrants a description update

Plan and diff context:
- Plan path: plans/tactical-misc-fixes-plan.md (section TMF-01)
- Comparison base: master branch or the commit just before the TMF-01 implementer branch diverged

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/tactical-misc-fixes/TMF-01

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
