Your role is 'tester'. Your task is as follows:

Task summary:
Remove wxTOPLEVEL_EX_DIALOG extra-style from FBattleScreen constructor to restore the minimize button on GTK, while preserving the custom modal event loop and gtk_window_set_modal grab.

Modified files:
- src/tactical/FBattleScreen.cpp
- include/tactical/FBattleScreen.h

Acceptance criteria to validate (from Planner):
1. The BattleBoard window presents a functional minimize button.
2. The battle remains modal: the strategic window underneath is not interactable while the BattleBoard is shown.
3. Existing centering/first-show sizing of the BattleBoard is unchanged.
4. Doxygen headers updated — @author and Last Modified updated in both files.

Test file location:
tests/gui/TacticalGuiLiveTest.cpp and tests/gui/TacticalGuiLiveTest.h

Add behavioral coverage verifying:
- Constructing FBattleScreen results in GetExtraStyle() that does NOT include wxTOPLEVEL_EX_DIALOG.
- The constructor default style includes wxMINIMIZE_BOX (verify GetWindowStyleFlag() & wxMINIMIZE_BOX is non-zero).
- The gtk_window_set_modal path is preserved: verify that the source of ShowModal() still contains the gtk_window_set_modal(GTK_WINDOW(m_widget), TRUE) call (source-contract supplement; behavioral modal grab coverage is provided by the existing live tactical test path).

Implementation context for testing:
Single line removed: SetExtraStyle(GetExtraStyle() | wxTOPLEVEL_EX_DIALOG) at src/tactical/FBattleScreen.cpp (previously at line 119, now gone). On GTK, wxTOPLEVEL_EX_DIALOG triggers gtk_window_set_type_hint(GTK_WINDOW_TYPE_HINT_DIALOG) which suppresses the minimize button in most window managers. The frame now presents as GTK_WINDOW_TYPE_HINT_NORMAL so standard frame decorations (minimize, maximize, resize) are visible. Battle modality is preserved by the existing custom modal event loop in ShowModal() (lines ~242-268) and the gtk_window_set_modal(TRUE/FALSE) calls at lines 253 and 265. No functional logic was changed.

Suggested test commands:
  make                                   # from repo root
  cd tests/gui && make && xvfb-run -a ./GuiTests

Existing validations expected to fail only because approved behavior changed:
None. The 9 pre-existing GUI test failures (source-contract failures in ViewFleetGUI.cpp, WXStrategicUI.cpp, ICMSelectionGUI.cpp, FBattleDisplay.cpp, BattleSimGuiLiveTest.cpp) are unrelated to this change.

Shared artifact directory:
artifacts/tactical-misc-fixes/TMF-02

Startup behavior:
If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run. Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers. Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Completion gate:
Do not report success unless all required artifacts exist and all changes are committed.
