Your role is 'documenter'. Your task is as follows:

Task summary:
- Document the TMF-03 fix: FBattleScreen title-bar X close while a WXTacticalUI child
  modal dialog (damage summary, ICM selection, or winner/info message) is active.
  The fix calls dismissActiveDialog() before Hide() in closeBattleScreen, preventing
  the duplicate wxDialog::EndModal assert at src/gtk/dialog.cpp:193.

Shared artifact directory (repository-root-relative):
- artifacts/tactical-misc-fixes/TMF-03

Branch:
- tmf-TMF-03-tester-20260630

Modified production files (all changes are committed; inspect these):
- include/gui/WXTacticalUI.h     — added hasPendingDialog(), dismissActiveDialog(), m_activeDialog
- src/gui/WXTacticalUI.cpp       — implemented hasPendingDialog(), dismissActiveDialog(), m_activeDialog tracking in showMessage/showDamageSummary/runICMSelection
- include/tactical/FBattleScreen.h  — documented dismissActiveDialog() call in closeBattleScreen
- src/tactical/FBattleScreen.cpp — closeBattleScreen calls m_tacticalUI->dismissActiveDialog() before Hide()/EndModal

Modified test files (committed):
- tests/gui/TacticalGuiLiveTest.h
- tests/gui/TacticalGuiLiveTest.cpp

AC3 coverage note (important for documentation accuracy):
- The behavioral test drives hasPendingDialog() and dismissActiveDialog() APIs directly
  from inside ShowModal's event loop (while IsModal is true), which covers the
  WXTacticalUI dismiss-API correctness and FBattleScreen close lifecycle.
- Directly posting wxEVT_CLOSE_WINDOW while ShowModal is live (the ideal full-production
  reproduction) was attempted in pass-2 and confirmed infeasible in the non-modal test
  harness: closeBattleScreen's Hide()+Destroy() path can delete m_tacticalUI via wx idle
  processing before showMessage unwinds, causing heap corruption (a harness artifact
  absent in production's modal FBattleScreen path).
- The test docstring explicitly scopes what is and is not exercised in-harness and why.
- A source-contract assertion locks the dismissActiveDialog() call in closeBattleScreen.

Documentation targets to update:
1. doc/test-contracts.md — add a TMF-03 entry documenting the new
   testBattleScreenXCloseDismissesActiveChildDialog test, including:
   - the AC1 behavioral assertions (hasPendingDialog, dismissActiveDialog in action)
   - the AC2/AC3 close-lifecycle assertion (frame hidden, no crash)
   - the AC4 lifecycle-counter assertion
   - the source-contract supplement (closeBattleScreen calls dismissActiveDialog)
   - the AC3 harness-limitation disclosure (in-harness adapted approach, why, and that
     the source-contract supplement covers the production integration point)
2. AGENTS.md — in the "tactical runtime close-vector" section (or add one), note that
   WXTacticalUI tracks m_activeDialog and FBattleScreen::closeBattleScreen must call
   dismissActiveDialog() before Hide()/EndModal to prevent the duplicate EndModal assert
   on wxGTK.

Do NOT modify:
- doc/rules/tactical_operations_manual.md  (non-volatile external artifact)
- Any production source files (implementation is complete and verified)
- Any test files (tests are complete and committed)

Required completion gate:
- Do not report success unless all required artifacts exist and are committed.
- Continue past scope confirmation, documentation discovery, and diff review without
  stopping to ask the user; all blocking inputs are present.
