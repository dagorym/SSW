Your role is 'Documenter'. Your task is as follows:

## Context

Task: TMF-06 — Skip post-fire damage-summary dialog when no weapon actually fired.

This is a pass-2 remediation run. The pass-1 documenter completed its work; a Verifier WARNING (behavioral test gap for AC2/AC3 dialog suppression) and a NOTE (comment typo in FBattleDisplay.h) were found. Pass-2 tester addressed the WARNING by adding direct behavioral GUI tests. The pass-1 documenter artifacts are archived in artifacts/tactical-misc-fixes/TMF-06/history/pass-1/.

Production fix summary:
- Added `weaponsFired` int to `FTacticalCombatReportSummary` (include/tactical/FTacticalCombatReport.h), populated from `(int)report.attacks.size()` in `buildTacticalCombatReportSummary` (same file, inline function ~line 716).
- Guarded `showTacticalDamageSummaryDialog(summary)` with `if (summary.weaponsFired > 0)` in both `onDefensiveFireDone` and `onOffensiveFireDone` (src/tactical/FBattleDisplay.cpp ~lines 1383, 1402).
- Mine-damage path (ITacticalUI::showDamageSummary called directly from FTacticalGame) is unaffected.
- FTacticalGame.h and src/tactical/FTacticalGame.cpp were NOT modified.

## Branch

tmf-TMF-06-tester-20260630

## Shared Artifact Directory

artifacts/tactical-misc-fixes/TMF-06

## Files Modified by Implementer and Tester

Implementer:
- include/tactical/FTacticalCombatReport.h — FTacticalCombatReportSummary struct: added weaponsFired field with Doxygen; buildTacticalCombatReportSummary: updated Doxygen
- src/tactical/FBattleDisplay.cpp — onDefensiveFireDone and onOffensiveFireDone: added if (summary.weaponsFired > 0) guard
- include/tactical/FBattleDisplay.h — updated file-header Last Modified; inline comment on onDefensiveFireDone and onOffensiveFireDone declarations updated to note weaponsFired > 0 semantics

Tester (pass-1):
- tests/tactical/FTacticalCombatReportTest.h — added 3 new behavioral test declarations
- tests/tactical/FTacticalCombatReportTest.cpp — added 3 behavioral test implementations
- tests/tactical/FTacticalBattleDisplayFireFlowTest.h — added 2 source-contract test declarations
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp — added 2 source-contract test implementations

Tester (pass-2, NEW):
- tests/gui/TacticalGuiLiveTest.h — added testDefensiveFireDoneSkipsDialogWhenNoWeaponsFired and testOffensiveFireDoneSkipsDialogWhenNoWeaponsFired declarations; CPPUNIT_TEST entries; Doxygen comments; author/date updated
- tests/gui/TacticalGuiLiveTest.cpp — added #include "tactical/ITacticalUI.h"; CountingMockTacticalUI class (counts showDamageSummary invocations); FireDoneObserverBattleScreen class (subclasses FBattleScreen, installs counting mock on ITacticalUI seam, sets BS_Battle state); two test implementations

## Pass-2 New Behavioral Coverage

The two new GUI tests (`testDefensiveFireDoneSkipsDialogWhenNoWeaponsFired` and `testOffensiveFireDoneSkipsDialogWhenNoWeaponsFired`) directly observe that the dialog suppression guard works at runtime:

- A `CountingMockTacticalUI` (implements ITacticalUI) is installed on the FTacticalGame so FBattleScreen::showTacticalDamageSummaryDialog routes to the mock's showDamageSummary instead of a real dialog.
- Each test creates a FireDoneObserverBattleScreen, sets the appropriate fire phase (PH_DEFENSE_FIRE / PH_ATTACK_FIRE), draws with a wxMemoryDC to connect the fire-done button, force-enables the button, fires the click event programmatically, and asserts the mock's invocation counter did NOT increase.
- With an empty game (no ships loaded), fireAllWeapons() returns weaponsFired == 0, and the guard must suppress the dialog call. Both tests PASS.

Test results:
- GuiTests: 65 total, 57 pass, 8 fail (8 are pre-existing, unchanged from pass-1 baseline; 2 new tests both PASS)
- TacticalTests: 249/249 PASS

## Your Documentation Tasks

1. **Update doc/test-contracts.md** — Extend the TMF-06 entry (added in pass-1) to describe the two new behavioral GUI tests:
   - `testDefensiveFireDoneSkipsDialogWhenNoWeaponsFired` — behavioral, TacticalGuiLiveTest; drives onDefensiveFireDone via CountingMockTacticalUI/FireDoneObserverBattleScreen; asserts showDamageSummaryCallCount does not increase when weaponsFired==0.
   - `testOffensiveFireDoneSkipsDialogWhenNoWeaponsFired` — behavioral, TacticalGuiLiveTest; same pattern for onOffensiveFireDone/PH_ATTACK_FIRE.
   Retain all pass-1 test entries (weaponsFired behavioral tests, source-contract supplements).

2. **Fix the comment in include/tactical/FBattleDisplay.h** (pass-1 NOTE item, documenter-owned) — The Verifier found in pass-1 that the inline comment on the `onDefensiveFireDone` declaration at approximately line 253 of include/tactical/FBattleDisplay.h said "offensive" instead of "defensive". Inspect that line in the current file and correct it if needed. The correct comment should read: `/// event handler for defensive fire complete button; shows damage summary only when weaponsFired > 0`. If the file already says "defensive", confirm it is correct and make no change. (The corresponding comment on `onOffensiveFireDone` should say "offensive fire complete button" — verify that is also correct.)

3. **Update AGENTS.md or doc/test-contracts.md** if needed to reflect that behavioral GUI coverage was added in pass-2 for the dialog suppression path. The pass-1 documenter already documented the fire-phase ownership note and TMF-06 entry; extend as appropriate.

You do NOT need to update any implementation files — those are Implementer-owned.

## Required Artifacts

Write all artifacts to: artifacts/tactical-misc-fixes/TMF-06/

Required:
- documenter_report.md
- documenter_result.json (must include key "branch_name": "tmf-TMF-06-tester-20260630")
- verifier_prompt.txt

## Completion Gate

Do NOT report success unless:
1. All required artifacts exist in artifacts/tactical-misc-fixes/TMF-06/
2. All documentation changes are committed on branch tmf-TMF-06-tester-20260630
3. The fix to include/tactical/FBattleDisplay.h comment (if needed) is committed

Proceed past scope confirmation and diff review without stopping — all required inputs are present.
