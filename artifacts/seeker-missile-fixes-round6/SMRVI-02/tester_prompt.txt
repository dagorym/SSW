Your role is 'tester'. Your task is as follows:

Task summary:
- Rework drawSeekerActivation() in FBattleDisplay.cpp: (1) replace (heading H, allowance A) row text with board position (X,Y) from seeker.hex; (2) replace fixed lMargin=310 with dynamic margin computed from widest instruction line and Seeker Activation Done button right edge.

Modified files:
- src/tactical/FBattleDisplay.cpp
- include/tactical/FBattleDisplay.h

Acceptance criteria to validate (from Planner):
- During PH_SEEKER_ACTIVATION, each deactivatable row displays the seeker's board position as (X,Y) from seeker.hex.getX()/seeker.hex.getY() and does NOT show heading or allowance.
- During PH_SEEKER_ACTIVATION, the "Activated seekers" column left edge is computed dynamically (not fixed at 310), clearing the widest instruction line and the "Seeker Activation Done" button's right edge.
- Clicking an activation row still deactivates exactly the intended seeker; rows are not clipped and do not overlap the left column, the Done button, or the ship-status display.

Test file location guidance:
- Live render / row-text / margin / click-region behavioral tests belong in tests/gui/TacticalGuiLiveTest.* (offscreen wxMemoryDC + WXGuiTestHarness), consistent with the existing testSeekerActivationAnchorIsAtActionPromptLineY pattern. Required behavioral coverage (each must discriminate the change — fail against pre-change code, pass after):
  (a) Seed an activated seeker at a known hex, render PH_SEEKER_ACTIVATION, assert row text contains (X,Y) and does NOT contain "heading"/"allowance".
  (b) Assert activation column region left edge > old fixed 310 and > widest instruction extent + Done button right edge.
  (c) Simulate a click on a row's region and assert the intended seeker is deactivated (active-seeker set decrements).
- Source-contract coverage may be added in tests/tactical/FTacticalBattleDisplayFireFlowTest.* as a supplement only; per AGENTS.md behavioral tests are mandatory.

Implementation context:
- Added #include <algorithm> for std::max with initializer_list.
- In drawSeekerActivation() in src/tactical/FBattleDisplay.cpp: (1) lMargin now computed as leftOffset + max(widestInstructionW, doneButtonRightExtent) + 2*BORDER, where widestInstructionW = max text extent of the three instruction lines (same font, same literal strings as DrawText calls to prevent drift), and doneButtonRightExtent = button absolute right minus leftOffset (falls back to leftOffset + bestWidth when button not yet laid out on first draw); (2) per-row os stream now outputs (seeker.hex.getX(), seeker.hex.getY()) instead of heading/allowance.
- Click regions (m_seekerActivationRegions) are derived from the drawn text extent and lMargin, so they track the new margin automatically.
- The DrawText calls retain their exact literal string form because an existing source-contract test (testSeekerActivationPanelListsInstructionAndOneRowPerInactiveSeeker in FTacticalBattleDisplayFireFlowTest.cpp, line 729) checks for those literal strings; the GetTextExtent calls use identical literals in the same function body immediately before those DrawText calls.
- The existing source-contract test at line 742 checks for "os << \"Deactivate seeker #\" << seeker.seekerID" — this is still present; the change is only the suffix after seekerID (position instead of heading/allowance).

Suggested test commands:
- cd tests && make tactical-tests && ./tactical/TacticalTests (run from tests/tactical)
- cd tests/gui && make && ./GuiTests (requires display, run from tests/gui directory)

Existing validations expected to fail only because approved behavior changed:
- None. The 1 GUI test failure (testLocalGameDialogLaunchesPredefinedAndCustomModalChains in BattleSimGuiLiveTest) is a pre-existing flaky BattleSim centering test unrelated to this change — confirmed passing on baseline and on retry after changes.

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Do not stop after activation, role acknowledgment, or directory discovery when testing can proceed.

Shared artifact directory:
- artifacts/seeker-missile-fixes-round6/SMRVI-02

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
