Your role is 'tester'. Your task is as follows:

Task summary:
Eliminate the duplicated attack-phase instruction string literal "Select legal path hexes to deploy
seeker missiles." by extracting it into a single shared FBattleDisplay constant
(SEEKER_DEPLOY_INSTRUCTION) and referencing it from both the draw() pendingLMargin measurement
and the drawAttackFire() draw call.

Modified files:
- include/tactical/FBattleDisplay.h
- src/tactical/FBattleDisplay.cpp

Acceptance criteria to validate (from Planner):
1. The attack-phase instruction string is defined exactly once as a shared FBattleDisplay constant;
   both drawAttackFire() (the drawn text) and draw() (the pendingLMargin measurement) reference
   that single constant, with no remaining duplicate string literal.
2. The attack-phase ("Pending seeker deployments") panel still renders to the right of the left
   instruction text with no overlap, and the pending recall click-regions still behave as before
   (no visual or behavioral change).
3. The visible instruction wording and its draw location are unchanged.

Implementation context for testing:
- FBattleDisplay::SEEKER_DEPLOY_INSTRUCTION is declared as a static const wxString in the
  protected section of FBattleDisplay (include/tactical/FBattleDisplay.h).
- It is defined in src/tactical/FBattleDisplay.cpp at file scope inside namespace Frontier:
    const wxString FBattleDisplay::SEEKER_DEPLOY_INSTRUCTION("Select legal path hexes to deploy seeker missiles.");
- In draw(), the old local variable
    const wxString widestAttackLine("Select legal path hexes to deploy seeker missiles.");
  was removed; the text extent is now measured directly from the constant:
    const int attackTextW = dc.GetTextExtent(SEEKER_DEPLOY_INSTRUCTION).GetWidth();
- In drawAttackFire(), the old
    os.str("Select legal path hexes to deploy seeker missiles.");
  was replaced with:
    os.str(SEEKER_DEPLOY_INSTRUCTION.ToStdString());

Test file location guidance:
- Primary behavioral coverage: tests/gui/TacticalGuiLiveTest.* —
  testOffensiveSeekerPendingListRegionVisibilityAndRecall should still pass; it confirms the
  pending panel renders to the right of the left instruction text with no overlap and that
  click-regions remain correct.
- Source-contract test requiring update: tests/tactical/FTacticalBattleDisplayFireFlowTest.*,
  testActionPromptSpacingContractAppliedAcrossActionPhases. Three assertions must be updated to
  match the new constant-based code structure:
    a. assertContains(attackBody, "Select legal path hexes to deploy seeker missiles.") —
       the literal no longer appears in the drawAttackFire() body; update to check for
       SEEKER_DEPLOY_INSTRUCTION instead.
    b. assertContains(drawBody, "const wxString widestAttackLine(\"Select legal path hexes...\")") —
       the widestAttackLine variable no longer exists; update to check for the new
       dc.GetTextExtent(SEEKER_DEPLOY_INSTRUCTION) pattern.
    c. assertContains(drawBody, "const int attackTextW = dc.GetTextExtent(widestAttackLine).GetWidth()") —
       update to check for dc.GetTextExtent(SEEKER_DEPLOY_INSTRUCTION).GetWidth().
- The Tester should also add a supplementary source-contract assertion that the literal string
  "Select legal path hexes to deploy seeker missiles." appears exactly once in the full source
  file (as the constant's definition), and does NOT appear inside the drawAttackFire() or draw()
  function bodies.

Suggested test commands:
- make -C tests tactical-tests && ./tests/tactical/TacticalTests
- make -C tests gui-tests && ./tests/gui/GuiTests   (requires display; use xvfb-run -a if needed)

Existing validations expected to fail because of this approved behavior change:
- FTacticalBattleDisplayFireFlowTest::testActionPromptSpacingContractAppliedAcrossActionPhases
  currently fails (1 of 226 tactical tests). The failure is an expected consequence: three
  source-contract assertions in that test check for the pre-refactor code patterns that are
  intentionally removed. The Tester must update those assertions as described above.

Shared artifact directory:
- artifacts/seeker-missile-fixes-round6/SMRVI-01

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately
  and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when
  repository evidence is sufficient, and label those choices as assumptions.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or
  framework discovery when testing can proceed.

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
