You are operating as the downstream Tester stage for coordinator subtask TSM-002 remediation retry.

Repository worktree: /home/tstephen/repos/SSW-worktrees/seekers-tsm-002-implementer-20260523
Branch: seekers-tsm-002-implementer-20260523
Shared artifact directory (repo-root-relative): artifacts/tactical-seeker-missiles/TSM-002
Coordination plan path: plans/tactical-seeker-missiles-plan.md

Your role is 'tester'. Your task is as follows:

Remediation context:
- This retry addresses one prior BLOCKING defect and one related selection-state warning in `src/tactical/FTacticalGame.cpp`.
- Product code changed only in:
  - src/tactical/FTacticalGame.cpp

Verifier-targeted fixes implemented:
- `sourceMatchesSelection(...)` now requires exact selected weapon-slot identity (selected ship + exact selected weapon slot) before undo removes/restocks placed ordnance.
- Successful `placeMineFromSelection(...)` and `placeSeekerFromSelection(...)` now reselect the same source slot after `rebuildDeployablePlacementSources()` so selected-source index and active ship/weapon pointers stay synchronized.

Acceptance criteria to validate (from Planner):
- The model discovers deployable mine and seeker missile weapon slots.
- The model can select the current deployment source by ship and weapon slot.
- Placing a mine records a source-tracked mine and decrements the selected mine ammo.
- Placing a seeker creates an inactive seeker and decrements the selected seeker missile ammo.
- Undoing a placed item restores ammo to the exact source weapon slot.
- Mines remain limited to one mine group per hex.
- Seekers can stack without limit in a hex.
- Inactive seekers are not considered by `checkForMines(...)` or mine damage.
- Existing mine placement behavior remains available through compatibility methods or updated forwarding methods.

Priority runtime scenarios for this retry:
- Same ship with multiple deployable slots stacking ordnance in one hex, then undo from a specific selected slot; confirm only that exact slot is undone/restocked.
- Post-placement selected-source consistency: after successful mine/seeker placement from nonzero source index, confirm reported selected index remains aligned with active ship/weapon pointers.

Tester test-file location guidance:
- Placement model coverage belongs in `tests/tactical/FTacticalGameMechanicsTest.*` or a focused tactical placement fixture.
- Weapon launcher behavior coverage belongs in `tests/weapons/FSeekerMissileLauncherTest.*` only if launcher metadata/selection behavior changes are required.

Suggested test command(s):
- cd tests && make tactical-tests && ./tactical/TacticalTests
- cd tests/weapons && make   (only if seeker launcher metadata/behavior coverage is touched)

Existing validations expected to fail only because approved behavior changed:
- None

Completion gate:
- Do not report success unless all required artifacts exist and all tester changes are committed.
