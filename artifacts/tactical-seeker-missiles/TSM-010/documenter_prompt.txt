You are the Documenter Agent for this project.

## Mission
Update project documentation to reflect the TSM-010 implementation: integrating active seeker contact into normal ship movement finalization so that when a ship's path enters a hex containing an active seeker owned by the opposing player, seeker detonation is resolved before mine damage.

## Task Summary

TSM-010 added ship-path seeker contact detection to `completeMovePhase()`. When a moving ship's turn path passes through a hex that contains an active seeker owned by the opposing player, seeker detonation is now resolved (via the existing `resolvePendingSeekerDetonationDamage()` seam) before mine damage is applied. Inactive seekers are explicitly skipped by `checkForActiveSeekersOnPath`.

## What Was Implemented

### Implementation Files Modified
- `include/tactical/FTacticalGame.h` — Added declarations for `checkForActiveSeekersOnPath(FVehicle*)` and `applyMovementSeekerDamage()` in the protected section, with full Doxygen headers.
- `src/tactical/FTacticalGame.cpp` — Implemented `checkForActiveSeekersOnPath` (scans ship path for active opposing seekers, appends outcomes), `applyMovementSeekerDamage` (collects IDs, resolves via existing seam or clears, removes seekers exactly once), and updated `completeMovePhase` to call both in the correct order.

### Test Files Modified
- `tests/tactical/FTacticalMineDamageFlowTest.h` and `.cpp` — 4 new source-inspection tests
- `tests/tactical/FTacticalSeekerMovementTest.h` and `.cpp` — 2 new runtime tests; `TestableTacticalGame` extended with `using` declarations for protected methods

### Shared Artifact Directory (repository-root-relative)
`artifacts/tactical-seeker-missiles/TSM-010`

## Acceptance Criteria (all passed)

1. Ship movement paths are checked for active seeker hex contact during movement finalization.
2. Inactive seeker hexes do not trigger contact or damage.
3. Ship-triggered seeker damage resolves before mine damage in completeMovePhase().
4. Detonated seekers are removed exactly once.
5. Destroyed ships do not leave stale occupancy, turn-info, or current selection state.
6. Existing mine damage still applies once per movement completion after seeker-contact resolution.
7. Phase progression to defensive fire remains correct after seeker contact and mine damage.

## Test Results

- 187 tests, all passing (181 pre-existing + 6 new)
- Test commit: 7d3fd26 (branch: seekers-tsm-010-tester-20260528)

## Documentation Scope

The Documenter should inspect and update relevant documentation to reflect:

1. **`doc/DesignNotes.md`** (if tactical seeker/movement-phase design notes exist) — note that `completeMovePhase()` now resolves active seeker path contacts (via `checkForActiveSeekersOnPath` + `applyMovementSeekerDamage`) before mine damage.
2. **AGENTS.md tactical test section** — the tactical test fixtures description should note that `FTacticalMineDamageFlowTest` and `FTacticalSeekerMovementTest` now cover TSM-010 movement-path seeker contact behavior.
3. **`include/tactical/FTacticalGame.h`** — Doxygen headers for `checkForActiveSeekersOnPath` and `applyMovementSeekerDamage` were added by the implementer; verify they accurately describe the behavior.

Do not modify `doc/rules/tactical_operations_manual.md` (non-volatile external artifact).

## Completion Gate

Do not report success unless all required artifacts exist and all changes are committed. Continue past scope confirmation, documentation discovery, and diff review to actually make documentation updates.

## Repository Context

- Branch: seekers-tsm-010-tester-20260528 (worktree: /home/tstephen/repos/SSW-worktrees/seekers-tsm-010-tester-20260528)
- Main branch: master
- Test command: `cd tests && make tactical-tests && ./tactical/TacticalTests`
