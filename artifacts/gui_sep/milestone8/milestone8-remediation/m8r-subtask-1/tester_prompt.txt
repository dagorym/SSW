You are the Tester Agent.

Shared artifact directory (repository-root-relative): `artifacts/milestone8-remediation/m8r-subtask-1`

Subtask under test: Milestone 8 Remediation — Subtask 1 (destroyed-ship cleanup lifecycle contract)

Implementation commit to test:
- `042c3f458b74266802c0f482722053c5305d093f`

Modified files:
- `include/tactical/FTacticalGame.h`
- `src/tactical/FTacticalGame.cpp`
- `src/tactical/FBattleScreen.cpp`
- `src/tactical/FBattleDisplay.cpp`

Planner acceptance criteria to verify:
- The cleanup contract across display, screen, and model is explicit and internally consistent.
- The agreed sequence leaves destroyed-ship IDs available until wx-side cleanup consumes them.
- The contract preserves Milestone 8’s model-owned fire resolution and wx-owned map cleanup split.

Implementation context:
- `FTacticalGame` now explicitly documents and owns destroyed-ship ID capture during fire resolution.
- New model seam API: `clearLastDestroyedShipIDs()` provides a single explicit clear point after UI consumption.
- `FBattleScreen::clearDestroyedShips()` now treats itself as wx cleanup orchestration and clears model bookkeeping only after consumption.
- `FBattleDisplay` fire-done handlers include contract comments and preserve ordering (`resolveCurrentFirePhase` → summary dialog → `clearDestroyedShips` → phase completion).

Validation commands already run by implementer:
- `make -C src/tactical`
- `cd tests/tactical && make && ./TacticalTests`

Tester guidance:
1. Re-run the tactical validation commands above.
2. Confirm source-level contract consistency across the three layers.
3. Confirm no regression in existing tactical delegation seam tests.
4. Write tester artifacts into the same shared artifact directory and pass that directory path forward to the Verifier.
