Your role is 'verifier'. Your task is as follows:

Task summary:
- Implementer restored stopped-ship preview-route completeness so model-owned stopped-ship preview now includes the current-heading forward route alongside alternate legal starting facings.
- Tester updated tactical regression coverage to validate the restored route, preview hex-heading mapping behavior, unchanged gating semantics, and continuation through normal move selection flow.
- Documenter updated repository-authored docs so the stopped-ship preview contract now describes the restored all-legal-facings behavior.

Acceptance criteria to verify:
- When a moving-side ship begins PH_MOVE at `speed == 0`, `nMoved == 0`, and `MR > 0`, `getStoppedShipPreviewRoutes()` includes the current-heading forward preview in addition to alternate legal starting facings.
- The stopped-ship preview lookup surface continues to map preview-route hexes back to the legal starting heading set, including the restored current-heading route where applicable.
- The remediation does not change non-stopped movement highlight semantics or grant new preview options to ships with `MR == 0`.
- Existing stopped-ship preview-click continuation still resolves into the normal route-selection flow after the preview contract is corrected.
- The tactical model remains additive and non-wx.
- Repository-authored docs match the implemented and tested behavior for the stopped-ship preview contract.

Implementation branch or worktree context:
- Implementer branch: `stopped-ship-free-rotation-plan-implementer-20260510`
- Tester branch: `stopped-ship-free-rotation-plan-tester-20260510`
- Documenter branch: `stopped-ship-free-rotation-plan-documenter-20260510`
- Verification worktree should reuse repository-root-relative artifact path context from this documenter branch state.

Files modified across implementation, testing, and documentation scope:
- `src/tactical/FTacticalGame.cpp`
- `tests/tactical/FTacticalMoveRouteSelectionTest.cpp`
- `tests/tactical/FTacticalGameMechanicsTest.cpp`
- `tests/tactical/FTacticalBattleBoardRendererDelegationTest.cpp`
- `tests/tactical/FTacticalModelSelectionHexClickSurfaceTest.cpp`
- `doc/UsersGuide.md`
- `doc/DesignNotes.md`

Commands executed:
- `cd tests && make tactical-tests && ./tactical/TacticalTests` (implementer baseline on behavior-changed expectations; 1 stale failure)
- `cd tests && make tactical-tests && ./tactical/TacticalTests` (tester post-update; `OK (143 tests)`)
- `cd tests && make tactical-tests && ./tactical/TacticalTests` (documenter validation after doc updates; `OK (143 tests)`)

Final test outcomes:
- Tactical runner passes in full after test and documentation updates (`OK (143 tests)`).
- Coverage explicitly validates restored current-heading preview-route participation, preview-hex heading lookup behavior, unchanged MR==0/non-stopped gating semantics, and continuation into the normal route-selection flow.

Plan-source and evaluation-context guidance:
- Use `plans/stopped-ship-move-ui-review-remediation-plan.md` as the story plan source.
- Treat the plan's Documentation Impact section as a hint only; verify against the actual implemented and tested diff.
- Safe assumption: compare the documenter branch against the tester/implementer lineage already present in git history (`54988f8a407f2f967840604fb84895e512830ef0` -> `3474f67` -> `807597b527ca68d7988408f2182f6e19249a9bfc`).

Convention-file guidance:
- `AGENTS.md` and `.myteam/` guidance did not require updates for this documentation-only follow-up.
- Keep `doc/rules/tactical_operations_manual.md` untouched.

Updated documentation files:
- `doc/UsersGuide.md`
- `doc/DesignNotes.md`

Shared artifact directory:
- `artifacts/stopped-ship-move-ui-review-remediation-plan/subtask-1`

Artifact files expected in the shared directory:
- `implementer_report.md`
- `implementer_result.json`
- `tester_report.md`
- `tester_result.json`
- `documenter_report.md`
- `documenter_result.json`
- `verifier_prompt.txt`
- existing upstream prompt handoff files (`tester_prompt.txt`, `documenter_prompt.txt`)

Startup behavior:
- If the branch/worktree context and enough story/diff context are present, begin verification immediately and continue in the same run.
- Infer missing plan-source, convention-file, comparison-base, or artifact-path details from repository context when safe, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, review-scope confirmation, convention discovery, or diff inspection when verification can proceed.

Verification instruction:
- Verify implementation, tests, and updated docs together before deciding the verdict.

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
