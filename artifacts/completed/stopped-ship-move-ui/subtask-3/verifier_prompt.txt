Your role is 'verifier'. Your task is as follows:

Task summary:
- Validate the stopped-ship `PH_MOVE` preview-route UI update that now renders every legal preview route for eligible zero-speed movers, preserves coherent routing after a preview is committed, and keeps ordinary nonzero-speed routing unchanged.
- Confirm the refreshed tactical regression coverage matches that shipped renderer/prompt/delegation behavior.
- Confirm the updated design-note documentation matches the implemented and tested UI behavior without changing unrelated guidance.

Acceptance criteria to verify:
- `PH_MOVE` stopped-ship selection renders all legal preview routes.
- After preview commitment, move selection route behavior remains coherent.
- Move prompt text describes preview-route interaction and no longer uses legacy adjacent-hex-only wording.
- Nonzero-speed routing path remains unchanged.
- `FBattleScreen` accessor additions are additive model forwarders needed by the renderer.
- `doc/DesignNotes.md` accurately describes the shipped renderer branch, prompt-copy change, additive forwarding seam, and validated tactical-suite result.

Implementation branch or worktree context:
- Branch: `stopped-ship-free-rotation-plan`
- Repository: `/home/tstephen/repos/SSW`
- Implementer code commit: `a1d42cc`
- Tester test commit: `fbbaa8f1bb3ef8a333164594d0adf447ab401930`
- Documenter documentation commit: `c500fdfd8bf28ecab0b924f19a391b0e781334bc`
- Comparison-base assumption:
  - `dcbe160..HEAD` is the safest bounded review surface for the full implementer+tester+documenter context because `dcbe160` immediately precedes the subtask-3 implementation commit in the current linear branch history.

Files modified by Implementer, Tester, and Documenter to inspect:
- `src/tactical/FBattleBoard.cpp`
- `src/tactical/FBattleDisplay.cpp`
- `src/tactical/FBattleScreen.cpp`
- `include/tactical/FBattleScreen.h`
- `tests/tactical/FTacticalBattleBoardRendererDelegationTest.cpp`
- `tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp`
- `tests/tactical/FTacticalBattleScreenDelegationTest.cpp`
- `doc/DesignNotes.md`
- `artifacts/stopped-ship-move-ui/subtask-3/implementer_report.md`
- `artifacts/stopped-ship-move-ui/subtask-3/implementer_result.json`
- `artifacts/stopped-ship-move-ui/subtask-3/tester_prompt.txt`
- `artifacts/stopped-ship-move-ui/subtask-3/tester_report.md`
- `artifacts/stopped-ship-move-ui/subtask-3/tester_result.json`
- `artifacts/stopped-ship-move-ui/subtask-3/documenter_prompt.txt`
- `artifacts/stopped-ship-move-ui/subtask-3/documenter_report.md`
- `artifacts/stopped-ship-move-ui/subtask-3/documenter_result.json`
- `artifacts/stopped-ship-move-ui/subtask-3/verifier_prompt.txt`

Commands executed:
- `cd tests && make tactical-tests && ./tactical/TacticalTests`
- `cd tests && make tactical-tests && ./tactical/TacticalTests`
- `git --no-pager log --oneline --decorate -6`

Final test outcomes:
- Tester tactical suite passed: `OK (143 tests)`.
- Documenter reran `cd tests && make tactical-tests && ./tactical/TacticalTests` after documentation edits: `OK (143 tests)`.
- No acceptance criteria remain unmet.

Plan and evaluation context:
- Plan path reviewed: `plans/stopped-ship-move-ui-plan.md`
- Story context: stopped-ship move UI subtask 3 for renderer/prompt/forwarder updates.
- The plan's documentation-impact note said no documentation update was expected beyond validating inline move prompt text. The actual shipped diff added a persistent renderer branch, new `FBattleScreen` forwarders, and updated prompt copy that were not yet captured in `doc/DesignNotes.md`, so a narrow design-note update was required.

Convention-file guidance:
- Read `AGENTS.md` for repository tactical-module and documentation constraints.
- Do not edit `doc/rules/tactical_operations_manual.md`; it remains the immutable external tactical rules artifact.
- No `.myteam` or `AGENTS.md` update was required for this change.

Updated documentation files:
- `doc/DesignNotes.md`

Verification scope:
- Verify implementation behavior, tester coverage, and updated documentation together.
- Confirm `FBattleBoard::drawRoute()` uses stopped-ship preview routes only for the zero-speed pre-displacement selection case and otherwise stays on the existing movement/left/right route path.
- Confirm `FBattleDisplay::drawMoveShip()` now instructs the player to choose a highlighted preview route while still preserving the adjacent-hex plus `Movement Done` rotate-in-place flow.
- Confirm the new `FBattleScreen` accessors remain additive forwarders over the tactical model.
- Infer missing plan-path, comparison-base, convention-file, or artifact-path details from repository context when safe, and label those choices as assumptions instead of treating them as blockers.

Startup behavior:
- If the blocking inputs above are present, continue in the same run.
- Do not stop after activation, review-scope establishment, convention discovery, or diff inspection when verification can proceed.

Shared artifact directory:
- `artifacts/stopped-ship-move-ui/subtask-3`

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
