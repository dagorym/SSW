Your role is 'tester'. Your task is as follows:

Task summary:
- Validate stopped-ship move-click behavior where clicking highlighted preview-route hexes resolves implied starting facing and advances movement through existing route-selection flow.

Modified files:
- src/tactical/FTacticalGame.cpp

Acceptance criteria to validate:
- Clicking a stopped-ship preview route hex selects the correct implied initial facing before adding movement to the path.
- The ship's first moved hex follows the selected preview direction.
- Existing route trimming and follow-on move extension continue to work after a preview-based first selection.
- Rotate-in-place completion still works once the player has chosen a facing without displacement.
- Non-stopped move selection behavior remains unchanged.

Implementation context for testing:
- Updated `FTacticalGame::handleMoveHexSelection(const FPoint & hex)`.
- Existing adjacent facing-only stopped-ship branch is preserved (for facing choice without displacement).
- Added stopped-ship preview-heading inference path:
  - Reads candidate headings from `getStoppedShipPreviewHeadingsForHex(hex)`.
  - Tries each candidate heading by rebuilding remaining moves from `m_shipPos`.
  - Chooses the heading whose rebuilt move highlights include the clicked hex (forward/left/right).
  - Falls through to existing movement append/trim logic so a preview click can extend path immediately.
  - Restores original heading/path state if no candidate heading resolves the clicked hex.
- Logic is constrained under existing stopped-ship free-rotation guard (`speed == 0`, `nMoved == 0`, path length 1, `MR > 0`).

Expected tester-owned coverage location guidance:
- tests/tactical/FTacticalMoveRouteSelectionTest.cpp
- tests/tactical/FTacticalGameMechanicsTest.cpp
- tests/tactical/FTacticalModelSelectionHexClickSurfaceTest.cpp

Suggested test commands:
- cd tests && make tactical-tests && ./tactical/TacticalTests
- (Optional focused iteration assumption) cd tests/tactical && make && ./TacticalTests

Startup behavior:
- Begin testing immediately and continue in the same run when context is sufficient.
- Do not stop after activation metadata alone.

Shared artifact directory:
- artifacts/stopped-ship-move-ui/subtask-2

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
