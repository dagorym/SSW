Your role is 'tester'. Your task is as follows:

Task summary:
- Validate TMPR-2 tactical lower-panel sizing behavior for move-phase prompt wrapping and button-clearance responsiveness under resize transitions.

Modified files:
- include/tactical/FBattleDisplay.h
- src/tactical/FBattleDisplay.cpp

Acceptance criteria to validate (from Planner):
- Requested lower-panel height in selected-ship move mode reflects actual wrapped prompt geometry for the active width.
- Width-driven extra wrapping can increase lower-panel height enough to prevent overlap with the `Movement Done` button.
- The last wrapped prompt line does not overlap the button band when the lower panel can instead grow vertically.
- Removing width pressure lets the extra requested height shrink back toward baseline.
- The existing map-floor and fallback-layout behavior remain intact.

Create test files in:
- tests/gui/TacticalGuiLiveTest.cpp
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp

Implementation context for testing:
- Move-prompt reservation is no longer capped at three lines; wrapped line demand now drives dynamic extra spacer and extra height.
- A dynamic sizer spacer was added above the action-button row to maintain prompt/button clearance as wrapping deepens.
- Requested lower-panel height still starts from V2 baseline but now adds only the extra amount needed for deep wraps/button band clearance.
- Right-split layout now re-expands stats margin to current available split width when valid, enabling shrink-back after width pressure is removed.
- Resize reflow prompt measurement now explicitly sets the move prompt font before wrapping calculations.

Suggested test command(s):
- cd tests && make tactical-tests && ./tactical/TacticalTests
- cd tests/gui && make && xvfb-run -a ./GuiTests

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/tactical-movement-prompt-overlap-replan-v2/tmpr-2

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
