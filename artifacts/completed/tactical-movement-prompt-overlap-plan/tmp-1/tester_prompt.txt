Your role is 'tester'. Your task is as follows:

Task summary:
- Validate tactical lower-panel resize/reflow implementation updates in FBattleScreen/FBattleDisplay.

Modified files:
- include/tactical/FBattleDisplay.h
- src/tactical/FBattleDisplay.cpp
- src/tactical/FBattleScreen.cpp

Acceptance criteria to validate (from Planner):
- Open-dialog resize triggers immediate lower-panel reflow.
- The lower-panel layout state recalculates predictably when size changes require it.
- The selected-ship constrained-width move path is deterministic enough for downstream live GUI tests to target.
- The change stays localized to tactical layout behavior and does not alter tactical rules.

Create test files in:
- `tests/gui/TacticalGuiLiveTest.cpp` (existing guidance)
- `tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp` (existing guidance)
- No new tests are required unless regression investigation proves necessity.

Implementation context for testing:
- FBattleScreen resize handler now triggers FBattleDisplay lower-panel reflow before screen-level layout policy application.
- FBattleDisplay now has a dedicated reflow path (`reflowLowerPanelLayout`) that recalculates move-prompt line reservations and lower-panel layout state using current geometry.
- Move-phase prompt handling retains selected-ship facing-selection and constrained-width prompt behavior text while recomputing reservations for deterministic geometry.
- No tactical combat rules, fire resolution ordering, or turn logic were changed.

Suggested test command(s):
- cd tests && make tactical-tests && ./tactical/TacticalTests
- cd tests/gui && make && xvfb-run -a ./GuiTests

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/tactical-movement-prompt-overlap-plan/tmp-1

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
