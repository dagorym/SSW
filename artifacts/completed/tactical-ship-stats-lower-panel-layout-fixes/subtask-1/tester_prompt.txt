Your role is 'tester'. Your task is as follows:

Task summary:
- Validate the lower-panel tactical layout remediation so ship stats no longer clip in split mode, stacked fallback is chosen when split cannot safely fit, and stacked ship stats stay below the action-button row after resize/phase transitions.

Modified files:
- include/tactical/FBattleDisplay.h
- src/tactical/FBattleDisplay.cpp

Acceptance criteria to validate (from Planner):
- Large-ship stat content, including long weapon lists, is no longer cut off at the right edge when split mode is active.
- Split mode remains right-aligned only when the available width can display the full ship-stats block.
- Stacked mode places the ship-data block fully below the action-button area with no overlap.
- FBattleScreen resize handling still recomputes the lower-panel layout deterministically through the existing display reflow seam.
- The implementation replaces brittle fixed-layout assumptions with a stable lower-panel sizing contract that later tests can verify.

Create test files in:
- tests/gui/TacticalGuiLiveTest.cpp
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp

Implementation context for testing:
- Lower-panel split-vs-stacked selection now uses measured ship-stats requirements with an explicit fit-padding reserve.
- Runtime paint-path fallback now flips split to stacked when actual split room becomes too tight, and uses live action-button geometry to keep stacked ship stats beneath buttons.
- Prompt-side minimum width reservation for split mode is increased to avoid narrow-width clipping behavior.
- No model/state-flow rewrites: resize still reflows through `FBattleDisplay::reflowLowerPanelLayout()` via the existing `FBattleScreen::onSize` seam.

Suggested test command(s):
- cd tests/tactical && make && ./TacticalTests
- cd tests/gui && make && GDK_BACKEND=x11 xvfb-run -a ./GuiTests

Known expected-failure status:
- None. Current required tactical and GUI validation passes after implementation.

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/tactical-ship-stats-lower-panel-layout-fixes/subtask-1

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
