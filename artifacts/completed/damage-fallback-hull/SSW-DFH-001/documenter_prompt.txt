You are the Documenter Agent.

Task summary:
- Review documentation impact for SSW-DFH-001 after second-pass Tester validation.
- The Implementer changed standard advanced-damage DCR resolution in `FVehicle::advancedDamage(...)` so a repeated standard DCR hit falls back to normal weapon hull damage when current DCR is already reduced below max, even if it remains above zero.
- The Tester added the verifier-requested regression coverage for a partially damaged nonzero DCR repeat hit and confirmed full SSW-DFH-001 coverage still passes.
- Do not address SSW-DFH-002 `Disastrous Fire` behavior in this documentation cycle.

Acceptance criteria validated:
- ADF and MR entries continue to apply as cumulative point loss while current values remain above zero.
- ADF entries convert to normal weapon hull damage when current ADF is already zero.
- MR entries convert to normal weapon hull damage when current MR is already zero.
- Repeated hits on already-damaged single-state systems (`power short circuit`, `combat control`, `navigation`, `electrical fire`, `damage control`) convert to normal weapon hull damage instead of reasserting the same state.
- Weapon-hit and defense-hit fallback behavior remains unchanged for ships that still have an eligible undamaged component.
- Damage-resolution output for converted hits records hull damage, not a zero-change non-hull effect.

Implementation branch or worktree context:
- Tester worktree: `/tmp/ssw-worktrees/damage-fallback-SSW-DFH-001-tester-20260502`
- Branch: `damage-fallback-SSW-DFH-001-tester-20260502`
- Shared artifact directory to reuse: `artifacts/damage-fallback-hull/SSW-DFH-001`
- Plan artifact path for provenance: `plans/damage-fallback-hull-plan.md`
- Comparison-base guidance: inspect the current branch against its parent/base as needed; infer the safest base from repository context if not explicitly available.

Files modified by Implementer and Tester to inspect for documentation impact:
- `src/ships/FVehicle.cpp`
- `tests/ships/FVehicleTest.cpp`
- Existing context file: `plans/damage-fallback-hull-plan.md`
- Relevant rules document likely to inspect: `doc/rules/tactical_operations_manual.md`

Test commit:
- `52b37c0daf970bde3afb24ce1bdf3b7355da5bc7`

Commands executed:
- `cd tests/ships && make`
- `cd tests && make`
- `cd tests && ./SSWTests`

Final test outcomes:
- Tester wrote 1 regression case in `tests/ships/FVehicleTest.cpp`.
- `cd tests/ships && make` passed.
- `cd tests && make` passed with existing unrelated `FJumpRoute.cpp` pointer-cast warnings.
- `cd tests && ./SSWTests` passed with `OK (170 tests)`.
- No unmet SSW-DFH-001 acceptance criteria remain from the Tester perspective.

Documentation context:
- The plan says no documentation update is expected because the rules text already describes the intended fallback behavior.
- Documentation review should confirm whether the existing rules and project docs already describe standard non-hull repeat-hit fallback adequately.
- Do not document or validate `Disastrous Fire` semantics for SSW-DFH-002 in this pass.

Plan and diff context:
- Use `plans/damage-fallback-hull-plan.md` for feature scope and acceptance criteria.
- Review current branch changes for `src/ships/FVehicle.cpp` and `tests/ships/FVehicleTest.cpp`.
- Reuse `artifacts/damage-fallback-hull/SSW-DFH-001` for documenter artifacts.

Startup behavior:
- If documentation scope, changed files, plan path, and artifact path are present, begin documentation review immediately and continue in the same run.
- Infer missing documentation-convention, plan, base-branch, or artifact-path details from repository context when safe, and label assumptions instead of treating them as blockers.
- Do not stop after activation metadata, scope confirmation, documentation discovery, or diff review when the next documentation step can be performed.

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
