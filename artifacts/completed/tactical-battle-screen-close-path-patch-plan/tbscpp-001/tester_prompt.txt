You are the Tester Agent.

Task summary:
- Validate TBSCPP-001 close-lifecycle repair for `FBattleScreen`.
- Goal: `File -> Quit` and title-bar `X` share one accepted close path, non-modal frames hide immediately before destruction, and stack-owned modal shim callers unwind via `ShowModal()` return (without destroying stack-owned instances).

Modified files:
- include/tactical/FBattleScreen.h
- src/tactical/FBattleScreen.cpp
- include/tactical/FTacticalGame.h
- src/tactical/FTacticalGame.cpp

Acceptance criteria to validate:
- Non-modal tactical screens visibly disappear after `File -> Quit`.
- Non-modal tactical screens visibly disappear after title-bar `X`.
- Stack-owned modal tactical screens return from `FBattleScreen::ShowModal()` for both `File -> Quit` and title-bar `X`.
- Modal title-bar close does not destroy stack-owned `FBattleScreen` objects.
- Close guard cannot strand first valid close request.
- Close-in-progress state is not owned by tactical model state.
- No `exit(...)`, no `ExitMainLoop(...)`, and no unrelated parent-window closure.

Implementation details to verify:
- `FBattleScreen` now owns close guard state (`m_closeInProgress`).
- `FTacticalGame` close API/state was removed.
- `onMenuQuit(...)` calls `Close(true)`.
- `onClose(...)` handles accepted closes without `event.Skip()`.
- `closeBattleScreen(...)` uses modal `EndModal(...)` return path and non-modal `Hide(); Destroy();` path.
- Constructor adds `wxTOPLEVEL_EX_DIALOG` extra style for modal/top-level wx compatibility.

Suggested validation commands:
- `cd tests && make tactical-tests`
- `cd tests && ./tactical/TacticalTests`
- `cd tests/gui && make`
- `cd /home/tstephen/repos/SSW-worktrees/tbscpp-001-implementer-20260522 && xvfb-run -a tests/gui/GuiTests`

Expected/known post-change test status:
- Tactical runner currently has 3 expected source-contract failures caused by intentional lifecycle contract changes in:
  - `tests/tactical/FTacticalBattleScreenElectricalFireTest.cpp`
  - `tests/tactical/FTacticalGameMechanicsTest.cpp`
- GUI runner currently reports the same 6 pre-existing failures as baseline (unrelated source-token checks).

Manual runtime validation requested:
- Launch a tactical battle screen from strategic flow (non-modal) and verify both close vectors (`File -> Quit`, title-bar `X`) visibly close only the tactical window.
- Exercise stack-owned modal tactical launch sites (BattleSim and strategic modal callers where practical) and verify both close vectors unwind back from `ShowModal()` without stack-object destruction.

Implementation commit to validate:
- 0012fb6e59a6afcf18aa0244a08ac61f5bd1ab97

Shared artifact directory:
- artifacts/tactical-battle-screen-close-path-patch-plan/tbscpp-001

Write tester outputs in that directory (`tester_report.md`, `tester_result.json`, and verifier handoff artifacts per workflow).
