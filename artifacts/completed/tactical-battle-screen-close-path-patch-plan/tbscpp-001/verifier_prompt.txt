You are the Verifier Agent.

Task summary:
- TBSCPP-001 shipped as a tactical close-lifecycle repair for `FBattleScreen`: `File -> Quit` now requests the same accepted close path as the title-bar `X`, non-modal frames hide before `Destroy()`, stack-owned modal callers unwind from `ShowModal()` via `EndModal(...)`, and close-in-progress state is owned by `FBattleScreen` rather than `FTacticalGame`.
- Documenter review updated the affected repository docs and in-code documentation so they describe the delivered lifecycle instead of the older model-owned guard and default-close narrative.

Acceptance criteria to verify:
- Non-modal tactical screens visibly disappear after `File -> Quit`.
- Non-modal tactical screens visibly disappear after title-bar `X`.
- Stack-owned modal tactical screens unwind from `FBattleScreen::ShowModal()` for both close vectors.
- Modal title-bar close does not destroy stack-owned `FBattleScreen` instances.
- The close helper does not call `exit(...)`, does not call `ExitMainLoop(...)`, and does not close unrelated parent windows.
- `FTacticalGame` no longer owns close-in-progress state.
- Documentation now matches the shipped behavior: accepted close events stay on the `FBattleScreen` close path, non-modal screens hide before destruction, modal callers unwind via return code, and the close guard is screen-owned.

Implementation branch or worktree context:
- Branch: `tbscpp-001-documenter-20260522`
- Worktree: `/home/tstephen/repos/SSW-worktrees/tbscpp-001-documenter-20260522`
- Parent tester branch: `tbscpp-001-tester-20260522`
- Parent implementer branch: `tbscpp-001-implementer-20260522`
- Coordination branch / comparison base: `coord-tactical-battle-board-menu-bar-plan-v2-20260522`

Files modified by Implementer, Tester, and Documenter to inspect:
- `include/tactical/FBattleScreen.h`
- `src/tactical/FBattleScreen.cpp`
- `include/tactical/FTacticalGame.h`
- `src/tactical/FTacticalGame.cpp`
- `doc/DesignNotes.md`
- `AGENTS.md`
- `artifacts/tactical-battle-screen-close-path-patch-plan/tbscpp-001/tester_report.md`
- `artifacts/tactical-battle-screen-close-path-patch-plan/tbscpp-001/tester_result.json`
- `artifacts/tactical-battle-screen-close-path-patch-plan/tbscpp-001/documenter_report.md`
- `artifacts/tactical-battle-screen-close-path-patch-plan/tbscpp-001/documenter_result.json`
- `artifacts/tactical-battle-screen-close-path-patch-plan/tbscpp-001/verifier_prompt.txt`

Commands executed:
- `cd tests && make tactical-tests && ./tactical/TacticalTests`
- `cd tests/gui && make`
- `cd /home/tstephen/repos/SSW-worktrees/tbscpp-001-tester-20260522 && xvfb-run -a tests/gui/GuiTests`
- `rg -n "setCloseInProgress|isCloseInProgress|m_closeInProgress" include/tactical/FTacticalGame.h src/tactical/FTacticalGame.cpp include/tactical/FBattleScreen.h src/tactical/FBattleScreen.cpp`
- `rg -n "ExitMainLoop|\\bexit\\s*\\(" include/tactical/FBattleScreen.h src/tactical/FBattleScreen.cpp include/tactical/FTacticalGame.h src/tactical/FTacticalGame.cpp`
- `git --no-pager diff coord-tactical-battle-board-menu-bar-plan-v2-20260522...HEAD -- include/tactical/FBattleScreen.h src/tactical/FBattleScreen.cpp include/tactical/FTacticalGame.h src/tactical/FTacticalGame.cpp tests/gui/TacticalGuiLiveTest.cpp tests/gui/BattleSimGuiLiveTest.cpp tests/tactical/FTacticalBattleScreenElectricalFireTest.cpp doc/DesignNotes.md AGENTS.md`
- `git --no-pager diff --check`

Final test outcomes:
- Tester verdict: PASS for TBSCPP-001 acceptance validation.
- `cd tests && make tactical-tests && ./tactical/TacticalTests`: build succeeded; runner reported 3 expected source-contract failures tied to intentional lifecycle changes.
- `cd tests/gui && make`: PASS.
- `xvfb-run -a tests/gui/GuiTests`: runner reported 6 known baseline unrelated source-token failures; tester confirmed the repaired close semantics still passed by implementation inspection and existing live coverage.
- Documenter validation: `git diff --check` passed after the documentation-only edits.

Plan and review context:
- Governing plan: `plans/tactical-battle-screen-close-path-patch-plan.md`.
- Tester artifacts are the primary acceptance evidence because the tester stage did not emit `documenter_prompt.txt`.
- Review repository documentation conventions in `AGENTS.md` if needed; infer any missing comparison-base or convention details from repository context when evidence is sufficient and label them as assumptions rather than blockers.

Documentation files updated:
- `include/tactical/FBattleScreen.h`
- `doc/DesignNotes.md`
- `AGENTS.md`

Verification scope:
- Verify the implementation behavior, tester evidence, and documentation updates together.
- Confirm the close lifecycle is accurately documented as `Close(true)` / title-bar `X` → `onClose(...)` → `closeBattleScreen(...)`, with non-modal `Hide()` before `Destroy()`, modal `EndModal(...)` unwind, and screen-owned close-guard state.
- Confirm no stale repository docs still claim `FTacticalGame` owns close-in-progress state or that accepted close events continue into default wx frame close handling for this repair.

Startup behavior:
- If the tester branch/worktree context, plan path, and artifact directory are present, begin verification immediately and continue in the same run.
- Infer missing comparison-base, convention, or artifact-path details from repository context when safe, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, review-scope establishment, convention discovery, or diff inspection when review work can proceed.

Shared artifact directory:
- `artifacts/tactical-battle-screen-close-path-patch-plan/tbscpp-001`

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
