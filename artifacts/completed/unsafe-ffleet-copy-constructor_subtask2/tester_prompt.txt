You are the Tester Agent.

Task summary:
- Second remediation for Subtask 2: preserve the source fleet's actual icon metadata in TransferShipsGUI while keeping explicit non-copy fleet creation.

Modified files:
- src/gui/TransferShipsGUI.cpp

Acceptance criteria to validate (from Planner):
- src/gui/TransferShipsGUI.cpp no longer uses new FFleet(*m_fleet).
- The new fleet is created explicitly and populated through explicit ship transfer steps.
- Fleet metadata needed by the UI flow is preserved without reintroducing shared ownership.
- tests/strategic/FFleetTest.cpp covers the updated ownership contract or the explicit transfer workflow.

Create test files in:
- tests/strategic/FFleetTest.cpp

Implementation context for testing:
- TransferShipsGUI::onDone keeps explicit `new FFleet` construction and now reads the source fleet icon path from serialized fleet data, then applies that exact icon path to the new fleet.
- The prior player-default icon fallback remains only for unexpected parse failure; normal flow preserves the source fleet's real icon metadata (including custom militia icons).
- Ownership-transfer behavior from Subtask 2 is unchanged.

Suggested test command(s):
- make -C tests/strategic libstrategicTests.a

Shared artifact directory:
- artifacts/unsafe-ffleet-copy-constructor_subtask2
