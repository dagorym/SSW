You are the Tester Agent.

Task summary:
- Add or extend automated coverage for tactical damage result capture, immediate-report flows, aggregation/filtering behavior, and delayed-cleanup sequencing.

Modified files:
- None required from implementer stage; this subtask is test-focused.

Acceptance criteria to validate (from Planner):
- There are automated tests covering at least:
- recording a hit with hull damage
- recording a non-hull advanced-damage effect
- recording electrical fire damage into an immediate report
- recording mine damage into an immediate report
- aggregating multiple attacks onto one ship
- omitting undamaged ships from the display summary
- preserving destroyed-ship reporting and delayed cleanup through dialog-triggered summary creation
- Tests are organized to cover raw effect capture, aggregation/filtering, and delayed-cleanup integration behavior.
- Tests build and run within the existing `tests/` framework, or the gap is documented if a GUI/runtime path is not practical.

Create test files in:
- tests/tactical/
- tests/ships/
- tests/weapons/

Implementation context for testing:
- Subtasks 1-8 are merged on `tds`, including the tactical report model, structured vehicle/weapon result capture, summary aggregation, shared dialog, immediate electrical-fire and mine-damage reporting, and end-of-phase defensive/offensive fire summaries.
- Existing related coverage already exists in `tests/tactical/FTacticalCombatReportTest.cpp`, `tests/tactical/FTacticalBattleScreenElectricalFireTest.cpp`, `tests/tactical/FTacticalMineDamageFlowTest.cpp`, `tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp`, `tests/ships/FVehicleTest.cpp`, `tests/ships/FTacticalAttackIntegrationTest.cpp`, and `tests/weapons/FWeaponFireResultTest.cpp`.
- Add or extend only the minimal tests needed to close remaining acceptance-criteria gaps.

Suggested test command(s):
- make -C tests/tactical
- ./tests/tactical/TacticalTests
- make -C tests/ships
- make -C tests/weapons
- make -C tests
- ./tests/SSWTests

Shared artifact directory:
- artifacts/tactical-damage-summary_subtask9
