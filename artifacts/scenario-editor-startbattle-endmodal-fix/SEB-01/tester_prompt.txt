Your role is 'tester'. Your task is as follows:

Add behavioral test coverage for the SEB-01 fix (double-wxDialog::EndModal
crash in BattleSim) and reconcile one now-obsolete source-contract test that
asserts the pre-fix behavior.

## Modified files (implementer commit 57cf70b07b30561e24c2a18c72ece784a7ac12bb)

- src/battleSim/ScenarioEditorGUI.cpp — onStartBattle() no longer calls
  Hide() before bb.ShowModal(); it still calls finalizeStartBattle() exactly
  once after the battle returns.
- src/battleSim/ScenarioDialog.cpp — onScenario1()..onScenario4() no longer
  wrap bb.ShowModal() in Hide()/Show(); each is now just bb.ShowModal().

## Acceptance criteria

- onStartBattle no longer calls Hide() before bb.ShowModal(); it still calls
  finalizeStartBattle() exactly once after the battle returns (single
  EndModal).
- Each onScenarioN no longer wraps bb.ShowModal() in Hide()/Show(); just
  bb.ShowModal(); remains.
- FBattleScreen, LocalGameDialog, BattleSimFrame, and the generated .h are
  unchanged.
- Builds cleanly; existing BattleSim/GUI and tactical suites pass (except
  the one expected-obsolete test below); FBattleScreen construct/destroy
  balance still holds.

## Required new/updated coverage

1. In tests/gui/BattleSimGuiLiveTest.cpp: add a behavioral test that drives
   the real onStartBattle (clickStartBattle) with the harness auto-dismissing
   the nested FBattleScreen, and asserts:
   - the editor dialog was NOT Hide()-den (i.e., remained shown/not hidden)
     before the nested FBattleScreen appeared, and
   - finalize (EndModal) ran exactly once.
   This should fail against the pre-fix code and pass post-fix.

2. In tests/gui/BattleSimGuiLiveTest.cpp (or wherever ScenarioDialog is
   already exercised): add analogous coverage for at least one
   ScenarioDialog::onScenarioN handler — assert no Hide()/Show() wrapping
   occurs around the nested FBattleScreen's modal show, and that the
   ScenarioDialog remains replayable (can launch a second scenario, or
   remains a valid modal owner) after the nested battle screen closes.

   IMPORTANT: Do NOT attempt to drive two nested live modal loops
   end-to-end in the same test in a way that keeps both ShowModal() calls
   genuinely blocking simultaneously — prior rounds found this hangs the
   runner / causes heap corruption in this harness. Use the existing
   auto-dismiss action pattern (see other tests in this file using
   runVoidFunctionWithAction / showModalWithAction) to drive the outer
   handler while auto-closing the inner FBattleScreen from the action
   callback, exactly as prior TMFAC2-style tests in this suite do for
   similar nested-modal flows.

3. Existing test needing reconciliation:
   tests/tactical/FTacticalBattleScreenElectricalFireTest.cpp,
   testBattleSimLaunchPathUsesModalDialogFlowIntoBattleScreen (around line
   192-213) currently asserts via source-text inspection that each
   ScenarioDialog::onScenarioN body contains exactly one Hide(), one
   bb.ShowModal(), and one Show() in that order. This assertion now fails
   because the implementer correctly removed Hide()/Show() per the approved
   plan. Update this test's source-contract assertions to match the new
   contract (no Hide()/Show() around bb.ShowModal() in onScenarioN; still
   exactly one bb.ShowModal() call and no bb.Show() calls), OR replace the
   assertion with equivalent coverage in the new behavioral test from item 2
   above, per this repo's rule that source-contract checks may only
   supplement, not replace, behavioral coverage for behavior claims — the
   Hide()/Show() removal is itself a structural/lifecycle claim that this
   source-contract style test may continue to cover, but the crash-avoidance
   behavior itself must be covered behaviorally per item 1.

## Test file location guidance

- tests/gui/BattleSimGuiLiveTest.cpp (new behavioral tests, items 1-2)
- tests/tactical/FTacticalBattleScreenElectricalFireTest.cpp (reconcile
  obsolete assertion, item 3)

## Validation commands

- make -C src all
- cd tests/gui && make && ./GuiTests
  (xvfb-run was not available in this environment; if available in yours,
  prefer `xvfb-run -a ./GuiTests` per the canonical headless command; a
  live $DISPLAY session also works and was used for this implementer's
  validation)
- cd tests && make tactical-tests && ./tactical/TacticalTests

## Known pre-existing/expected results

- BattleSimGuiLiveTest::testLocalGameDialogLaunchesPredefinedAndCustomModalChains
  showed a one-off flaky failure (a `centered` assertion on ScenarioDialog's
  position relative to its parent) on a first run that passed cleanly on
  rerun in this environment. It is unrelated to the edited handler bodies.
  Treat repeat failures here as worth a closer look, but a single flake is
  not attributable to this change.
- FTacticalBattleScreenElectricalFireTest::testBattleSimLaunchPathUsesModalDialogFlowIntoBattleScreen
  fails as described in item 3 above — this is an expected consequence of
  the approved fix, not a regression, and is exactly what your reconciliation
  work in item 3 should resolve.

## Runtime verification (acceptance gate — not automatable here)

The definitive proof that the wxGTK double-EndModal assert is gone requires
a live nested-modal run:
- BattleSim -> Play a Local Game -> Create a New Custom Game -> build fleets
  -> Start Battle: must close with no assert at src/gtk/dialog.cpp:193.
- BattleSim -> Play a Local Game -> Play a Predefined Game -> Play a
  scenario -> Done: dialog must remain replayable and close cleanly.
Do not attempt to reproduce this end-to-end in the automated harness (see
the hang/heap-corruption caveat above); this is a required user
manual-verification pass and must be flagged as such rather than claimed as
proven by automated tests.

## Shared artifact directory

artifacts/scenario-editor-startbattle-endmodal-fix/SEB-01/ (repo-root-relative)
