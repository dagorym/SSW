Your role is 'documenter'. Your task is as follows:

Task summary:
- Validate H9 (bind-once FBattleDisplay button handlers + non-paint updateForPhase()): add behavioral GUI coverage in TacticalGuiLiveTest and re-author the 6 stale source-inspection tests in FTacticalBattleDisplayFireFlowTest as H9 source-contract supplements backed by the new behavioral tests.

Acceptance criteria validated:
- No m_first anywhere (member + assignments removed from src and header); all 9 button click handlers (7 phase + 2 turn) bound once in the ctor; no button Connect/Disconnect in the paint/draw path -- verified by re-authored source-contract tests in FTacticalBattleDisplayFireFlowTest and behaviorally via bind-once click routing in TacticalGuiLiveTest.
- A new public non-paint FBattleDisplay::updateForPhase() exists (no drawing, no DC) and is wired into FBattleScreen::setState, setPhase, and onSize -- behaviorally verified: testUpdateForPhaseShowsExactlyCorrectPhaseButtonWithoutPaint (setState/setPhase, no paint) and testUpdateForPhaseReSyncsButtonStateOnResizeViaOnSize (onSize path).
- Per-phase parity: exactly the correct Done/Set-Speed button shown+enabled per phase, all others hidden; clicking a phase button routes to the same handler and same model effect as before -- behaviorally verified across all 7 phase scenarios (parity test) and the Move->Defensive->Offensive click round, plus Seeker Placement/Activation and Mine Placement completion tests asserting model transitions (PH_/BS_ values) and observed IsShown()/IsEnabled().
- TMF-05 turn-button invariants preserved (existing 3 TMF-05 GUI tests still green) and TMF-06 Done-button dialog-suppression unchanged (testDefensive/OffensiveFireDoneSkipsDialogWhenNoWeaponsFired still green; new fire-Done click round uses empty game weaponsFired==0 with no dialog).
- Doxygen headers on the new updateForPhase() and on every function whose body changed were reviewed in the implementer diff (Last Modified + @author updated); tester added full Doxygen headers to the 5 new GUI test methods.

Implementation branch or worktree context:
- Worktree: /home/tstephen/repos/worktrees/phase4-P4-1-tester-20260712
- Branch: phase4-P4-1-tester-20260712 (tester branch off the P4-1 implementer branch)
- Implementer commit under test: bb3ebfa5 (P4-1: H9 bind-once FBattleDisplay buttons + non-paint updateForPhase())
- Tester test commit: 372b32108053df2d067bd84babd9a253386dfdeb

Files modified by Implementer and Tester to inspect for documentation impact:
- include/tactical/FBattleDisplay.h (Implementer: removed m_first; added updateForPhase() decl + Doxygen; updated draw*/handler headers)
- src/tactical/FBattleDisplay.cpp (Implementer: bind-once handlers in ctor; new updateForPhase(); draw*/handlers no longer manage button lifecycle)
- include/tactical/FBattleScreen.h (Implementer: reDraw() now calls updateForPhase(); setState/setPhase/onSize doc updates)
- src/tactical/FBattleScreen.cpp (Implementer: setState/setPhase delegate to reDraw(); onSize calls updateForPhase(); onSetSpeed final reDraw())
- tests/gui/TacticalGuiLiveTest.cpp (Tester: 5 new H9 behavioral tests + helpers)
- tests/gui/TacticalGuiLiveTest.h (Tester: register + document 5 new tests)
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp (Tester: re-author 6 source-contract tests for H9 structure)
- tests/tactical/FTacticalBattleDisplayFireFlowTest.h (Tester: rename/re-doc 4 renamed tests, keep 2)

Test commit:
- 372b32108053df2d067bd84babd9a253386dfdeb

Commands executed:
- make (repo root) - full build: PASS (exit 0, no warnings)
- cd tests/gui && make && xvfb-run -a ./GuiTests - PASS: OK (80 tests)
- cd tests/tactical && make && ./TacticalTests - PASS: OK (253 tests)
- make check (repo root) - PASS: SSWTests OK (245), TacticalTests OK (253), GuiTests OK (80)

Final test outcomes:
- make check fully green: SSWTests OK (245), TacticalTests OK (253), GuiTests OK (80).
- 5 new behavioral GUI tests added and passing: testUpdateForPhaseShowsExactlyCorrectPhaseButtonWithoutPaint, testUpdateForPhaseReSyncsButtonStateOnResizeViaOnSize, testPhaseDoneButtonClicksAdvanceModelPhaseAndButtonVisibility, testSeekerButtonsShownPerStateAndHiddenAfterCompletionViaUpdateForPhase, testMinePlacementButtonShownInPlaceMinesHiddenAfterCompletionViaUpdateForPhase.
- The 6 previously-failing source-inspection tests were re-authored to the H9 bind-once + updateForPhase() structure (4 renamed for accuracy, 2 kept with only the Show()-literal portion updated) and now pass; each explicitly points to its authoritative behavioral GUI test per AGENTS.md.
- No paint-time button lifecycle was restored to satisfy the old assertions.

Documentation context:
- Behavioral change now confirmed: FBattleDisplay button show/enable/turn-panel state is driven by the non-paint updateForPhase() seam, called from FBattleScreen::setState/setPhase/onSize/reDraw, instead of the removed m_first paint-time connect/show/hide machinery.
- AGENTS.md already documents the TMF-05/TMF-06 GUI-test invariants and the FBattleDisplay/FBattleScreen ownership rules; consider a brief note that updateForPhase() is the non-paint per-phase button seam and that reDraw() now invokes it.
- doc/test-contracts.md may warrant an update for the renamed FTacticalBattleDisplayFireFlowTest cases if it enumerates them (it currently references the SMF-*/TMF-* families rather than individual FireFlow test names).

Plan and diff context:
- Plan: plans/phase4-tactical-gui-hybrid-cleanup-plan.md (finding H9).
- Comparison base for the P4-1 diff: implementer commit bb3ebfa5 vs planner baseline 70637a7e (assumption based on branch history).
- Shared artifact directory: artifacts/phase4-tactical-gui-hybrid-cleanup/P4-1.

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/phase4-tactical-gui-hybrid-cleanup/P4-1

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
