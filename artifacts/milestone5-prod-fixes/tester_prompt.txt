You are the Tester Agent for Milestone 5 production fixes (subtask `m5-prod-fixes`).

Worktree and branch:
- Worktree: /home/tstephen/worktrees/gui_sep_ms5-implementer-20260329
- Branch: gui_sep_ms5-implementer-20260329

Shared artifact directory:
- artifacts/milestone5-prod-fixes

Production changes already landed in:
- include/tactical/FTacticalGame.h
- src/tactical/FTacticalGame.cpp

Behavior/context to test:
1) Destroyed ship cleanup in `FTacticalGame::clearDestroyedShips()` now removes dead ship IDs from:
   - side ship list being cleared
   - model hex occupancy (`m_hexData`)
   - model turn bookkeeping (`m_turnInfo`)
   and still returns surviving ship count for the cleared side while preserving winner bookkeeping.

2) `FTacticalGame::fireICM()` now:
   - clears `m_ICMData` before collection
   - finds target ship hex occupants by scanning `m_hexData`
   - enqueues only actionable ICM opportunities (target hex exists and has at least one same-side ship with usable ICM: has ICM defense, ammo left, power system not damaged, ICM defense not damaged)
   - never enqueues `ICMData` with `vehicles == NULL`

Acceptance criteria to verify with behavior-level tactical tests:
- Dead ship IDs are removed from side list, `m_hexData`, and `m_turnInfo`.
- `clearDestroyedShips()` still returns surviving count for cleared side.
- Winner bookkeeping remains correct when cleared side is eliminated.
- Helper-path behavior exists for model hex and turn-info cleanup (verify via observable behavior).
- `fireICM()` no longer pushes `ICMData` entries with `vehicles == NULL`.
- Weapons are queued only when an actual defending ship in the target hex can spend ICMs.
- `m_ICMData` is cleared before new collection starts.
- No wx tactical rewiring assumptions in tests.

Required test work:
- Create behavior-level tactical tests under existing tactical test locations in:
  /home/tstephen/worktrees/gui_sep_ms5-implementer-20260329/tests/tactical
- Register tests in tactical runners as needed.
- Do not modify production files for test convenience unless absolutely required and justified.

Suggested commands:
- cd /home/tstephen/worktrees/gui_sep_ms5-implementer-20260329/tests/tactical && make && ./TacticalTests
- Optionally verify full suite impact from /home/tstephen/worktrees/gui_sep_ms5-implementer-20260329/tests with existing runners.

Please write your tester outputs into:
- artifacts/milestone5-prod-fixes
