You are the Tester Agent.

Task summary:
- Validate Milestone 5 production fixes for subtask `m5-prod-fixes` already implemented in commit 475cd7e057b600cc7e5f396e05b5004c9b380d59.
- This remediation run only regenerated handoff artifacts; production code was not changed.

Modified files:
- include/tactical/FTacticalGame.h
- src/tactical/FTacticalGame.cpp

Acceptance criteria to validate (from plan):
- clearDestroyedShips() removes dead ship IDs from the cleared side ship list, m_hexData occupancy lists, and m_turnInfo bookkeeping.
- Winner bookkeeping remains correct when a side is eliminated (m_hasWinner/m_winnerID consistency).
- fireICM() clears prior m_ICMData before collecting.
- fireICM() resolves target ship hex occupants by scanning m_hexData.
- fireICM() enqueues only actionable interceptions (at least one same-side defender in target hex has usable ICM: defense present, ammo > 0, power not damaged, ICM defense not damaged).
- fireICM() never enqueues ICMData with vehicles == NULL and remains compatible with existing ICMData usage.

Create test files in:
- tests/tactical

Implementation context for testing:
- FTacticalGame is additive model-side tactical state (non-wx); no runtime wx rewiring is expected in this milestone.
- Destroyed-ship cleanup now includes explicit model-state purging paths for occupancy and turn bookkeeping.
- ICM collection now stores only actionable opportunities tied to the target-hex occupant list.

Suggested test command(s):
- cd /home/tstephen/worktrees/gui_sep_ms5-implementer-20260329/tests/tactical && make -s && ./TacticalTests
- (optional broader check) cd /home/tstephen/worktrees/gui_sep_ms5-implementer-20260329/tests && make -s && ./SSWTests

Shared artifact directory:
- artifacts/milestone5-prod-fixes
