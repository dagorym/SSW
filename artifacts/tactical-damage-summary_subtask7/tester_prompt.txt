You are the Tester Agent.

Task summary:
- Integrate immediate mine-damage reporting after mine application.

Modified files:
- src/tactical/FBattleBoard.cpp

Acceptance criteria to validate (from Planner):
- Mine damage shows a summary immediately after mine damage is applied.
- The shared tactical summary dialog is used for the report.
- Destroyed ships remain on the board and are still shown in the summary for the report in which they were destroyed.
- `clearDestroyedShips()` runs only after the relevant dialog is acknowledged.
- Mine damage is applied exactly once per triggering movement event, and the dialog appears only after mine-damage resolution completes.

Create test files in:
- tests/tactical/

Implementation context for testing:
- `FBattleBoard::applyMineDamage()` now builds tactical mine-damage attack reports, appends them to the shared tactical report, and shows the shared `TacticalDamageSummaryGUI` modal dialog before destroyed ships are cleared from the board.
- The tactical report is marked immediate for mine damage, so the summary should appear as a post-resolution modal step rather than at the end of a broader combat phase.
- Destroyed ships should still be visible in the mine-damage summary because `clearDestroyedShips()` is deferred until after the dialog closes.
- Target regression checks should confirm mine-trigger handling occurs once per movement event and that the summary is not shown before mine resolution completes.

Suggested test command(s):
- make -C src tactical
- make -C src gui
- make -C tests

Shared artifact directory:
- artifacts/tactical-damage-summary_subtask7
