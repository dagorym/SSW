You are the Tester Agent.

Task summary:
- Remediated BattleSim live GUI launch-path coverage so dialog-launch assertions now prove concrete dialog types (`LocalGameDialog`, `ScenarioDialog`, `ScenarioEditorGUI`) while preserving `FBattleScreen` lifecycle coverage and explicit parent/window cleanup behavior.

Modified files:
- tests/gui/BattleSimGuiLiveTest.cpp

Acceptance criteria to validate (from Planner):
- The `BattleSimFrame` test proves `LocalGameDialog` was presented.
- The `LocalGameDialog` test proves `ScenarioDialog` and `ScenarioEditorGUI` were each presented from their respective button paths.
- The BattleSim scenario-launch tests still verify `FBattleScreen` lifecycle counts and also finish with zero live top-level windows/dialogs.
- The BattleSim live tests explicitly destroy shown parents and pump events before returning.

Create test files in:
- tests/gui/

Implementation context for testing:
- `testBattleSimFrameOpensLocalGameDialogAndReturns()` now waits for a modal dialog and asserts it is `LocalGameDialog` with `BattleSimFrame` parent.
- `testLocalGameDialogLaunchesPredefinedAndCustomModalChains()` now waits for modals and asserts class identity (`ScenarioDialog` for predefined path, `ScenarioEditorGUI` for custom path), preserving parent linkage assertions.
- Scenario launch tests still assert `FBattleScreen` constructed/destroyed/live counters and now also assert that no shown top-level windows beyond pre-test baseline remain after explicit hide/destroy + event pumping + stabilization cleanup.
- Parent frames are explicitly hidden before destroy; cleanup sequencing remains deterministic (`pumpEvents`, `stabilizeTopLevels`, `forceCloseShownTopLevels`).

Suggested test command(s):
- `cd tests/gui && make && xvfb-run -a ./GuiTests`

Shared artifact directory:
- artifacts/gui-test-warning-remediation-plan/subtask-3-battlesim

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
