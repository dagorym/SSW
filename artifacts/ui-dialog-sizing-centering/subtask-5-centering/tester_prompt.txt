You are the Tester Agent.

Task summary:
- Remediated GUI modal crash/lifecycle instability in strategic live tests and reinforced centering behavior at tactical/strategic adapter modal launch sites while preserving BattleSim/strategic/tactical placement coverage.

Modified files:
- src/battleSim/BattleSimFrame.cpp
- src/battleSim/LocalGameDialog.cpp
- src/battleSim/ScenarioDialog.cpp
- src/gui/TacticalDamageSummaryGUI.cpp
- src/gui/WXStrategicUI.cpp
- src/gui/WXTacticalUI.cpp
- tests/gui/BattleSimGuiLiveTest.cpp
- tests/gui/StrategicGuiLiveTest.cpp
- tests/gui/TacticalGuiLiveTest.cpp

Acceptance criteria to validate (from Planner):
- Representative frames open centered on screen.
- Representative parent-backed dialogs open centered relative to their parent.
- Parentless modal cases use a deterministic screen-centered fallback.
- Live GUI regression tests verify the placement policy for at least one frame, one tactical dialog, and one strategic or BattleSim dialog.

Create test files in:
- No new tests expected for this remediation cycle (reuse existing GUI live suites in `tests/gui/` unless a regression requires narrowly-scoped follow-up).

Implementation context for testing:
- Strategic crash remediation centered on `StrategicGuiLiveTest::testCombatSelectionDialogsReturnCodesAndState` and nearby modal-launch lifecycle handling.
- Tactical/strategic adapters now explicitly center dialogs before `ShowModal()`:
  - `src/gui/WXStrategicUI.cpp`
  - `src/gui/WXTacticalUI.cpp`
- BattleSim modal chains no longer hide/show parent windows around nested modal launches:
  - `src/battleSim/BattleSimFrame.cpp`
  - `src/battleSim/LocalGameDialog.cpp`
  - `src/battleSim/ScenarioDialog.cpp`
- Tactical damage summary dialog now uses fit/min-size centering contract and init-dialog default-focus reinforcement.
- GUI live tests were updated for deterministic headless behavior while retaining representative frame/dialog placement checks.

Suggested test command(s):
- `cd tests/gui && make && ./GuiTests` (used in this environment)
- If virtual display tooling is available, also run canonical headless form: `cd tests/gui && make && xvfb-run -a ./GuiTests`

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/ui-dialog-sizing-centering/subtask-5-centering

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
