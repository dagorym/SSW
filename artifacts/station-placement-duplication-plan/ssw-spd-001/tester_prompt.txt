Your role is 'tester'. Your task is as follows:

Task summary:
- Implemented SSW-SPD-001 by fixing tactical station setup so repeated valid BS_SetupStation clicks relocate one pending station occupant instead of duplicating it.

Modified files:
- src/tactical/FTacticalGame.cpp

Acceptance criteria to validate (from Planner):
- A valid station placement click during BS_SetupStation records exactly one pending station occupant on the map.
- A second valid adjacent placement click before orbital direction finalization relocates that pending station occupant instead of creating a duplicate.
- Invalid clicks do not create a station occupant or corrupt the pending station position.
- Station orbit initialization still works from the final selected hex after the fix.
- Non-station ship placement and rotation flow are unchanged.

Create test files in:
- tests/tactical/FTacticalStationOrbitalMovementTest.cpp
- tests/tactical/FTacticalShipPlacementOrientationTest.cpp (if shared placement-orientation behavior is involved)

Implementation context for testing:
- Change is narrowly scoped to FTacticalGame::placeStation(...).
- Before placing at the newly clicked valid adjacent hex, the model now removes any existing occupant entries for the same station ID across tactical hex occupancy lists.
- The station position bookkeeping (setStationPosition, m_shipPos, PH_SET_SPEED transition) remains unchanged after relocation.
- Invalid placement guards (bounds/state/station-present/adjacent-to-planet) still return false before mutating placement state.

Suggested test command(s):
- cd tests && make tactical-tests && ./tactical/TacticalTests

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/station-placement-duplication-plan/ssw-spd-001

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
