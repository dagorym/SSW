Security Review Report

Scope reviewed:
- P5-5 (Save-file header, load-failure orchestration & error UX) REMEDIATION pass 2 of phase5-save-format-hardening. Reviewed read-only against the pass-1 security-reviewed state, focusing on (a) confirming the FGame aggregate-loader layer is unchanged from pass 1, and (b) the single user-approved in-scope mitigation applied this pass in FMainFrame::onOpen.
- FGame loader layer (UNCHANGED this pass, confirmed byte-identical): git diff 7baddaca(pass-1 verifier)..HEAD over src/strategic/FGame.cpp, include/strategic/FGame.h, include/core, src/core produces EMPTY output -- the magic/version gate, aggregated per-field/sub-load error checks, reportLoadError routing, and no-partial-live-singleton teardown are exactly as security-reviewed in pass 1 (see history/pass-1/security_report.md).
- onOpen mitigation (CHANGED this pass): src/FMainFrame.cpp + include/FMainFrame.h -- m_drawingPanel->setGame(m_game) is deferred out of the pre-load path into the success-only branch (called only after FGame::load()==0 and is.is_open()). On a failed/corrupt load the panel's m_game pointer stays NULL, so FGamePanel::onPaint's existing `if (m_game != NULL)` guard makes any repaint delivered during the load-error dialog's nested modal loop a no-op instead of dereferencing the still-NULL FMap singleton.
- Supporting review inputs: pass-1 security_report.md / security_result.json (archived under history/pass-1/); doc/deferred-tasks.md (SF-nested-load-returns, SF-nullfmap-paint-guard); pass-2 behavioral test tests/gui/StrategicGuiLiveTest.cpp testMainFrameOnOpenFailedLoadWithFrameShownSurvivesForcedRepaint (+ RepaintPumpTimer helper).
- Verification (run synchronously from this worktree): GUI suite built + `xvfb-run -a ./GuiTests` -> OK (83 tests, includes the new frame-shown forced-repaint mitigation test); top-level `SSWTests` (run from tests/) -> OK (289 tests, full FGameSaveFormatTest malformed-input rejection suite intact). (9 transient FGameHeaderDependencyTest source-inspection failures observed only when the binary was launched from the wrong cwd resolved to OK once run from tests/ -- cwd-relative source-path lookups, not a regression.)

Why specialist review was triggered:
- Loading a save file is an untrusted-input path and P5-5 is the aggregate loader boundary plus the GUI load-error UX; plan Section 7 marks stream-parsing subtasks as security-review-required. This remediation pass specifically closes the one reachable availability defect (a NULL-FMap paint crash) that pass-1 attributed to P5-5's own onOpen error dialog, so specialist re-review is required to confirm the mitigation actually closes that window without weakening the loader's failure-path teardown or introducing new exposure.
- Coordinator requested reaffirmation/revision of the P5-5 security outcome given the applied mitigation and the two now-tracked follow-up defects.

Acceptance criteria / plan reference:
- plans/phase5-save-format-hardening-plan.md -- Section 4 (P5-5 acceptance criteria), Section 7 (Security Notes: untrusted-input path).
- artifacts/phase5-save-format-hardening/P5-5/history/pass-1/security_report.md (pass-1 CONDITIONAL PASS: 0 blocking / 2 warning / 1 note).
- doc/deferred-tasks.md (SF-nested-load-returns for pass-1 finding #1; SF-nullfmap-paint-guard for the deeper gui root cause of pass-1 finding #2).

Findings

BLOCKING
- None

WARNING
- None

NOTE
- src/strategic/FGame.cpp - FGame aggregate-loader layer is UNCHANGED and remains correct. `git diff 7baddaca..HEAD -- src/strategic/FGame.cpp include/strategic/FGame.h include/core src/core` is EMPTY; the file is byte-identical to the pass-1 security-reviewed state.
  All pass-1 controls are intact with no regression: magic validated FIRST (readU32 failure OR magic != kSaveMagic -> nonzero before any game data is interpreted/allocated), version SECOND, every header field / m_universe->load() / player count / per-player->load() return checked, a failed player load deletes the just-allocated FPlayer before returning nonzero, reportLoadError routes through IStrategicUI::showMessage() (console fallback otherwise) using fixed string literals with no format-string hazard, and a failed load leaves no partially-built live FGame singleton. Confirmed still green: SSWTests OK (289) incl. the FGameSaveFormatTest wrong-magic/unsupported-version/truncated-after-header/unknown-factory-type/console-fallback/round-trip suite.
- src/FMainFrame.cpp:156-199 - onOpen setGame-deferral mitigation for pass-1 finding #2 is correct and CLOSES the reachable-crash window at the P5-5 call site; it does not weaken failure-path teardown and does not alter the success-path outcome.
  On a failed/corrupt load, m_game is created but the panel is never wired to it (setGame deferred), so the panel's m_game stays NULL through FGame::load()'s reportLoadError -> showMessage nested modal loop; FGamePanel::onPaint's `if (m_game != NULL)` guard makes any spontaneous repaint a no-op, so the previously-reachable FGamePanel::onPaint -> WXGameDisplay::draw -> WXMapDisplay::getScale -> FMap::getMaxSize() NULL-this segfault can no longer fire. On re-open, onClose->resetGame->clearGame() already NULLed the panel pointer beforehand, so the panel is NULL for the whole failed-load duration. Teardown is unchanged: resetGame() is still reached on `!is.is_open() || load()!=0` (clearGame + delete m_game + m_game=NULL + game-dependent menu items disabled; none enabled). Success path is end-state identical (setGame now runs in the else-branch after load()==0, then Refresh/menu-enable) -- no intervening modal on success, so no behavioral change. No new inputs, allocation, or trust boundary; the change strictly narrows the window in which the panel holds a game pointer, so it introduces no new security concern. Behaviorally proven by testMainFrameOnOpenFailedLoadWithFrameShownSurvivesForcedRepaint (frame shown, RepaintPumpTimer forces Refresh every 5ms across the nested modal loop while opening a corrupt save; asserts no crash and all 7 game-dependent menu items remain disabled). GuiTests OK (83).
- doc/deferred-tasks.md - Both pass-1 follow-ups are correctly recorded and correctly dispositioned as out-of-P5-5-scope, non-blocking, tracked items.
  Finding #2 root cause: SF-nullfmap-paint-guard documents that the GUI paint path (FGamePanel::onPaint / WXGameDisplay::draw / WXMapDisplay::getScale / FMap::getMaxSize) still lacks a defensive NULL/partially-built-FMap guard; the P5-5 pass-2 setGame deferral is explicitly recorded as a call-site mitigation (only closes onOpen's own load-error trigger), not the root-cause guard, which is a gui-module follow-up. Finding #1: SF-nested-load-returns documents that FPlayer::load()/FFleet::load() discard nested v->load()/f->load() return codes so a deep-truncation-inside-fleet input bypasses FGame::load's aggregate abort -- a data-integrity/robustness gap (NOT memory-safety: reads stay bounded via readString<=kMaxSerializedStringBytes and fixed-width readU32/read<T>, sticky failbit, createShip null-checked), targeted at P5-3 remediation (src/strategic/FPlayer.cpp, src/strategic/FFleet.cpp), outside P5-5's allowed files. Neither is exploitable for memory corruption or RCE; both are appropriately deferred and tracked rather than lost.

Test sufficiency assessment:
- Sufficient for this remediation pass. The pass-2 mitigation is backed by a real behavioral test: testMainFrameOnOpenFailedLoadWithFrameShownSurvivesForcedRepaint SHOWS the FMainFrame (its sibling deliberately does not, to sidestep the pre-mitigation crash) and drives a RepaintPumpTimer that forces frame->Refresh() every 5ms across onOpen's entire nested modal sequence (native file chooser then the load-error dialog) while opening a corrupt save, then asserts the process survives and all 7 game-dependent menu items stay disabled -- directly exercising the exact crash window pass-1 identified and proving it is closed by the setGame deferral. This is a runtime behavioral assertion, not source inspection, satisfying the repo behavioral-verification mandate.
- FGame loader coverage unchanged and re-confirmed: SSWTests OK (289) including the byte-level magic/version header emission, byte-for-byte round-trip, and four malformed-input rejection paths (wrong magic, unsupported version, truncated-after-header, unknown factory type) each requiring nonzero load() + exactly one IStrategicUI::showMessage(), plus the no-UI console fallback. GuiTests OK (83).
- Residual documented gap unchanged from pass 1 (tied to SF-nested-load-returns): deep-truncation-inside-fleet is intentionally not tested because the fix lives in P5-3-scope files; the aggregate-abort guarantee for that input class remains only partially proven and is tracked for P5-3 remediation. This does not affect P5-5's scoped acceptance.

Documentation / operational guidance assessment:
- Adequate. The onOpen Doxygen headers (include/FMainFrame.h, src/FMainFrame.cpp) accurately describe the deferred-setGame mitigation and why it prevents a repaint during the load-error nested modal loop from dereferencing a half-built/NULL game or map, and explicitly note the deeper gui NULL-FMap guard remains a tracked follow-up. FGame::save/load and reportLoadError Doxygen (unchanged) and include/core/FSaveFormat.h continue to document the versioned header contract; AGENTS.md / doc/UsersGuide.md describe the versioned fixed-width little-endian format and up-front magic/version rejection.
- doc/deferred-tasks.md now formally tracks both residual defects (SF-nullfmap-paint-guard, SF-nested-load-returns) with origin, why-deferred, and target-phase, closing the pass-1 recommendation to record the follow-ups so the residual untrusted-input-path gaps are not lost.

Artifacts written:
- artifacts/phase5-save-format-hardening/P5-5/security_report.md
- artifacts/phase5-save-format-hardening/P5-5/security_result.json

Outcome:
- PASS
