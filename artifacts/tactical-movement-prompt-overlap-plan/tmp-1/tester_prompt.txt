Your role is 'tester'. Your task is as follows:

Task summary:
- Validate the FBattleDisplay lower-panel layout-state implementation that now persists prompt/stats split mode and requested display height across tactical phases unless geometry constraints invalidate the active split.

Modified files:
- include/tactical/FBattleDisplay.h
- src/tactical/FBattleDisplay.cpp

Acceptance criteria to validate (from Planner):
- FBattleDisplay has an explicit shared layout state for prompt/stats positioning and requested height.
- The ship-stats split remains stable across phases when size constraints do not force change.
- The display can communicate a requested height based on its current layout state.
- The change remains localized to tactical display/layout behavior.

Create test files in:
- tests/gui/TacticalGuiLiveTest.cpp
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp

Implementation context for testing:
- Added `LowerPanelLayoutState` to track mode, prompt-line reservation, stats position, and requested display height.
- Added wrapped prompt helper methods and movement-prompt reservation flow so prompt height and control spacing are computed through shared helpers.
- Added geometry-validation path (`ensureLowerPanelLayoutState`) to keep prior split unless current constraints invalidate it.
- Added requested-height propagation from current layout state via display min-size updates.
- Defensive/offensive/mine prompt paths now explicitly reserve action prompt lines.

Suggested test command(s):
- cd tests && make tactical-tests && ./tactical/TacticalTests
- cd tests/gui && make && xvfb-run -a ./GuiTests

Validation notes:
- Baseline failures seen before edits were source-token mismatches in tactical/gui layout prompt tests; these now pass with updated implementation.

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/tactical-movement-prompt-overlap-plan/tmp-1

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
