Your role is 'documenter'. Your task is as follows:

Task summary:
- Document the validated FBattleDisplay lower-panel layout-state update that persists prompt/stats split state and requested display height across tactical phases, with geometry-driven invalidation behavior.

Acceptance criteria validated:
- FBattleDisplay has an explicit shared layout state for prompt/stats positioning and requested height.
- The ship-stats split remains stable across phases when size constraints do not force change.
- The display can communicate a requested height based on its current layout state.
- The change remains localized to tactical display/layout behavior.

Implementation branch or worktree context:
- Branch: `layout-update-tester-20260516`
- Worktree: `/home/tstephen/worktrees/SSW/layout-update-tester-20260516`
- Implementation commit inspected for localization: `55ed2b6`

Files modified by Implementer and Tester to inspect for documentation impact:
- include/tactical/FBattleDisplay.h
- src/tactical/FBattleDisplay.cpp
- tests/tactical/FTacticalBattleDisplayFireFlowTest.h
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp
- tests/gui/TacticalGuiLiveTest.h
- tests/gui/TacticalGuiLiveTest.cpp

Test commit:
- ec57142

Commands executed:
- cd tests && make tactical-tests && ./tactical/TacticalTests
- cd tests/gui && make && xvfb-run -a ./GuiTests

Final test outcomes:
- Tactical tests: OK (150 tests)
- GUI tests: OK (35 tests)
- Added tactical source-contract checks for shared lower-panel state, persistence/invalidation flow, and requested-height propagation.
- Added live GUI regression for cross-phase lower-panel min-height stability under non-invalidating geometry changes.

Documentation context:
- Tactical display/layout behavior docs may need updates where FBattleDisplay lower-panel rendering or layout policy is described.
- Emphasize that layout mode/position/requested-height state is now shared and preserved across phases unless geometry invalidates active split assumptions.

Plan and diff context:
- Plan/story context: tactical movement prompt overlap plan, artifact lane `tmp-1`.
- Comparison base assumption: use `layout-update` as the likely base branch and compare through tester commit `ec57142` plus the implementation commit `55ed2b6`.

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/tactical-movement-prompt-overlap-plan/tmp-1

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
