Your role is 'documenter'. Your task is as follows:

Task summary:
- SMFR-01 pass-2 remediation: Fix drawSeekerActivation() height expansion being silently overwritten by ensureLowerPanelLayoutState() in the BS_Battle draw() pass. Added a max-preservation guard so any previously-expanded requestedDisplayHeight survives the stats-based layout pass. This fixes the one remaining failing acceptance criterion (drawSeekerActivation() must reserve vertical space) without regressing the two already-passing criteria (drawPlaceMines and drawPlaceSeekers expansion).

Acceptance criteria validated:
- drawPlaceMines(), drawPlaceSeekers(), and drawSeekerActivation() reserve vertical space so their rendered rows are fully visible — PASS
- The placed-ordnance removal list is visible (right of the phase-ending button widget, left of the ship-status display) and its rows remain clickable during mine and seeker placement — PASS (source-contract verified)
- The seeker activation/deactivation list is visible in the same region and its rows remain clickable during the seeker activation phase — PASS (behavioral: testOrdnancePlacementAndActivationPanelHeightAutoExpands)
- The lists no longer render below or behind the ship-status display — PASS (max-preservation guard in ensureLowerPanelLayoutState verified behaviorally)
- Existing phase-ending buttons, ship-status display, and offensive-fire pending list keep their current positions and behavior — PASS (201 tactical tests pass)

Implementation branch or worktree context:
- Branch: sf2-SMFR-01-tester-20260619
- Implementation commit: d3ee240 (SMFR-01 pass-2: Preserve expanded requestedDisplayHeight in ensureLowerPanelLayoutState)
- Fix location: src/tactical/FBattleDisplay.cpp lines 582-588 — max-preservation guard at end of ensureLowerPanelLayoutState()

Files modified by Implementer and Tester to inspect for documentation impact:
- src/tactical/FBattleDisplay.cpp
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp
- tests/tactical/FTacticalBattleDisplayFireFlowTest.h
- tests/gui/TacticalGuiLiveTest.cpp
- tests/gui/TacticalGuiLiveTest.h

Test commit:
- 74938b9

Commands executed:
- make -C tests tactical-tests
- tests/tactical/TacticalTests
- make -C tests gui-tests
- DISPLAY=:0 tests/gui/GuiTests

Final test outcomes:
- Tactical tests: 201 PASS, 0 FAIL
- GUI tests: 36 PASS, 10 FAIL (all 10 failures are pre-existing, listed in tester_prompt.txt expected failures)
- testOrdnancePlacementAndActivationPanelHeightAutoExpands: PASS (previously failing — now fixed)
- testDrawPlaceMinesExpandsPanelHeightWhenRowsExceedMinimum: PASS
- testDrawPlaceSeekersExpandsPanelHeightWhenRowsExceedMinimum: PASS
- testDrawSeekerActivationExpandsPanelHeightWhenRowsExceedMinimum: PASS

Documentation context:
- The fix adds a max-preservation guard in FBattleDisplay::ensureLowerPanelLayoutState() so any requestedDisplayHeight already expanded by drawSeekerActivation(), drawPlaceMines(), or drawPlaceSeekers() is not silently overwritten by the stats-based height computation.
- No public API changes. No new files. Only src/tactical/FBattleDisplay.cpp modified in the implementation.
- AGENTS.md source contracts (SMF-01 through SMF-04) remain valid and the tests that enforce them continue to pass.
- The AGENTS.md tactical test bullet block (SMF-02, SMF-03, SMF-04) may need a brief note about the SMFR-01 guard if any explicit contract description is maintained there for this behavior.

Plan and diff context:
- Plan: seeker-missile-fixes-round3, subtask SMFR-01
- Artifact dir: artifacts/seeker-missile-fixes-round3/smfr-01
- Base branch for diff: sf2 (assumption — the worktree was branched from sf2)
- Implementation commit diff: ensureLowerPanelLayoutState() now ends with: if (m_lowerPanelLayoutState.requestedDisplayHeight > requestedHeight) { requestedHeight = m_lowerPanelLayoutState.requestedDisplayHeight; }

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/seeker-missile-fixes-round3/smfr-01

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
