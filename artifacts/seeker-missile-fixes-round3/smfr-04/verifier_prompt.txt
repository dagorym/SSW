Your role is 'verifier'. Your task is as follows:

## Task Summary
SMFR-04 pass-2 is complete through the Documenter stage. Verify that the implementation, tests, and documentation are consistent, correct, and satisfy all acceptance criteria.

**Production implementation** (commit `5b48aef`): `FBattleBoard::drawSeekerMoveCountOverlay(dc)` in `src/tactical/FBattleBoard.cpp` (line ~117) is called unconditionally inside the `BS_Battle` block in `draw()`, outside any `PH_MOVE/PH_SEEKER_ACTIVATION` phase guard. The overlay iterates all active seekers and uses `movementPath.size()-1` when a path is recorded this phase, or falls back to `movementAllowance` between resolution passes.

**Tester strengthening** (commit `07841a8`): `testSeekerMoveCountOverlayRendersInAllBattlePhases` in `TacticalGuiLiveTest.cpp` was upgraded from a no-crash assertion to a pixel-level behavioral assertion. It uses `TestableBattleScreen` and `SeederGame` helper classes to inject one active seeker at hex (5,5) with `movementAllowance=3`, drives `FBattleBoard::draw()` in `PH_ATTACK_FIRE` via offscreen `wxMemoryDC`, and asserts at least one red pixel appears in the label region. This assertion would FAIL if the overlay call were re-guarded inside `PH_MOVE/PH_SEEKER_ACTIVATION`.

**Documentation** (commit `444831a`): `AGENTS.md` SMFR-04 entry updated to document the pixel-level assertion, `TestableBattleScreen`/`SeederGame` helpers, and the regression-detection property of the test.

## Acceptance Criteria to Verify
- **AC1**: `drawSeekerMoveCountOverlay()` is called unconditionally inside `BS_Battle` block (outside any phase guard) AND produces a visible red label pixel in `PH_ATTACK_FIRE` for an active seeker — verified by `testSeekerMoveCountOverlayRendersInAllBattlePhases` pixel assertion.
- **AC2**: Count = `movementPath.size()-1` when path recorded this phase, `movementAllowance` as fallback otherwise — verified by model-level tests in `FTacticalSeekerMovementTest`.
- **AC3**: Co-located seekers have independent per-seeker counts for stacked display — verified by model-level tests.
- **AC4**: Seeker list count unchanged after board draws in all `BS_Battle` phases — verified by count assertion in the GUI test.

## Files Modified (Full Review Surface)
- `src/tactical/FBattleBoard.cpp` — Implementer, commit 5b48aef
- `tests/gui/TacticalGuiLiveTest.cpp` — Tester, commit 07841a8
- `tests/gui/TacticalGuiLiveTest.h` — Tester, commit 07841a8
- `AGENTS.md` — Documenter, commit 444831a

## Implementation Branch Context
- Comparison base: `sf2`
- Documenter branch: `sf2-SMFR-04-documenter-20260619`
- Plan: `plans/seeker-missile-fixes-round3-plan.md`, subtask SMFR-04

## Final Test Outcomes
47 GUI tests: 37 pass, 10 pre-existing failures (out of scope). Target test `testSeekerMoveCountOverlayRendersInAllBattlePhases` PASSED including pixel-level red-label assertion in `PH_ATTACK_FIRE`. 211 tactical tests pass.

## Shared Artifact Directory
`artifacts/seeker-missile-fixes-round3/smfr-04`

## Convention and Guidance Files
- `AGENTS.md` — repository guidance including behavioral-test mandate and in-code documentation requirements
- `plans/seeker-missile-fixes-round3-plan.md` — plan document for this story

## Startup Behavior
Blocking inputs are present. Begin verification immediately in the same run. Do not stop after activation, scope confirmation, or diff review when verification work can proceed. Infer missing plan-source, convention-file, or artifact-path details from repository context when safe.

## Completion Gate
Do not report success unless all required artifacts exist and all changes are committed.
