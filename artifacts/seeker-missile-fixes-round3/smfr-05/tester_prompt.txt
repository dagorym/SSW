Your role is 'tester'. Your task is as follows:

SMFR-05 pass-2 remediation: Verify the new behavioral render test for AC1.

The Verifier's pass-1 WARNING identified that AC1's render side (seeker movement
paths actually drawing on the board during PH_MOVE / PH_SEEKER_ACTIVATION) was
covered only by a source-contract assertContains check on the phase gate. This
pass-2 remediation adds a behavioral GUI test that exercises the actual render path.

Changed files (test-only, no production code changes):
- tests/gui/TacticalGuiLiveTest.cpp
- tests/gui/TacticalGuiLiveTest.h

New test added:
- testSeekerPathRendersInPHMoveWithMovementPath in TacticalGuiLiveTest

Test strategy:
- Uses SeederGame/TestableBattleScreen (SMFR-04 infrastructure) to inject an active
  seeker with a 2-hex movementPath (5,5)->(5,7) directly into the model AFTER calling
  setPhase(PH_MOVE). Critical: the seeder MUST run after setPhase(PH_MOVE) because
  setPhase(PH_MOVE) triggers resolveActiveSeekersForMovingPlayer() which clears seeker
  movementPaths. If seeded before setPhase, pathSize=1 and drawSeekerPaths skips it.
- Drives FBattleBoard::draw() in PH_MOVE, PH_SEEKER_ACTIVATION, and PH_ATTACK_FIRE
  via offscreen wxMemoryDC (2000x1500 bitmap).
- Uses a before/after differential: draw baseline (no seekers), then draw with seeker+path.
  Compares pixel diffs in the path band x=[344..356], y=[290..385] (the vertical line
  from hex (5,5) center at (350,295) to hex (5,7) center at (350,381)).
- The count label from drawSeekerMoveCountOverlay lands at (360,360) which is OUTSIDE
  the path band, so it does not contaminate the gate check.
- Platform pre-check: verifies dc.DrawLine at those coords produces cyan pixels on
  a fresh wxMemoryDC before the board-level assertions.

Acceptance criteria verified by this test:
- AC1 render side: FBattleBoard::draw() in PH_MOVE produces pixel diffs in the path
  band when a seeker with movementPath >= 2 is present (drawSeekerPaths drew the line).
- AC1 render side: PH_SEEKER_ACTIVATION also produces path band diffs.
- AC1 gate: PH_ATTACK_FIRE produces zero path band diffs (drawSeekerPaths not called).

Validation commands:
1. Build tactical tests and run:
   make -C tests tactical-tests && ./tests/tactical/TacticalTests
   Expected: 214 tests pass.

2. Build GUI tests:
   cd tests/gui && make
   Expected: builds cleanly.

3. Run GUI tests (requires display or xvfb):
   cd tests/gui && ./GuiTests
   Or with xvfb: cd tests/gui && xvfb-run -a ./GuiTests
   Expected: testSeekerPathRendersInPHMoveWithMovementPath passes.
   The 9 pre-existing failures in the GUI suite remain unchanged.

Failure-mode reasoning to verify in your report:
- The test FAILS if drawSeekerPaths is removed or gated out of PH_MOVE because
  the path band diffs become zero (AC1 assertion fails).
- The test FAILS if the seeker movementPath.size() < 2 because drawSeekerPaths
  skips the seeker (path band diffs zero). This would happen if seeded before
  setPhase(PH_MOVE) due to resolveActiveSeekersForMovingPlayer clearing the path.
- The gate check FAILS if drawSeekerPaths incorrectly draws in PH_ATTACK_FIRE.

Shared artifact directory: artifacts/seeker-missile-fixes-round3/smfr-05/

Implementation/code commit hash: 6dddd6a

Do not report success unless all required artifacts exist and all changes are committed.

Continue past preflight when blockers are absent.
