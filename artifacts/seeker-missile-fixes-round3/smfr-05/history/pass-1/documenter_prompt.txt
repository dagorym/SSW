Your role is 'documenter'. Your task is as follows:

Task summary:
- SMFR-05: Correct seeker movement-path persistence lifecycle so paths are visible through ICM selection and the damage summary, impacting seekers' paths clear when the damage summary closes, and non-impacting seekers' paths clear with ship movement routes at completeMovePhase without discarding movementAllowance/movementTurn bookkeeping.
- Implementation added clearNonImpactingSeekerMovementPaths() to FTacticalGame, called from completeMovePhase() after applyMovementSeekerDamage(). FBattleBoard::drawSeekerPaths now guards on PH_MOVE || PH_SEEKER_ACTIVATION so impacting-seeker paths remain visible during ICM and damage dialogs.
- 3 new behavioral tests added to FTacticalSeekerMovementTest covering AC3 (non-impacting path cleared at completeMovePhase), AC4 (movementAllowance/movementTurn preserved after path clear), and a source-contract supplement verifying the call order in completeMovePhase.

Acceptance criteria validated:
- A seeker's movement path is visible from movement resolution, through ICM selection, and while the damage summary dialog is displayed.
- An impacting seeker's path clears when the damage summary dialog closes (seeker removed by applyMovementSeekerDamage which erases the record and its movementPath).
- A non-impacting seeker's path remains drawn until completeMovePhase clears ship movement paths, then clears with them (clearNonImpactingSeekerMovementPaths called from completeMovePhase after applyMovementSeekerDamage).
- Clearing a non-impacting seeker's displayed path preserves its movementAllowance and movementTurn for next-turn movement bookkeeping.
- Seeker path rendering continues to work for multiple simultaneous seekers during PH_SEEKER_ACTIVATION and PH_MOVE.

Implementation branch or worktree context:
- Branch: sf2-SMFR-05-tester-20260619 (worktree off sf2)
- Implementer commit: cf2afb8 (SMFR-05: Clear non-impacting seeker paths with ship routes at completeMovePhase)
- Comparison base: sf2 branch prior to SMFR-05 implementer commit (assumption based on worktree branch origin)

Files modified by Implementer and Tester to inspect for documentation impact:
- include/tactical/FBattleBoard.h
- include/tactical/FTacticalGame.h
- src/tactical/FTacticalGame.cpp
- tests/tactical/FTacticalSeekerMovementTest.h
- tests/tactical/FTacticalSeekerMovementTest.cpp

Test commit:
- e60a4fb7872fda608f673efce76c5b64e9c12aed

Commands executed:
- make -C tests tactical-tests
- tests/tactical/TacticalTests

Final test outcomes:
- 214 tests pass, 0 failures. 3 new behavioral tests added. All 211 pre-existing tests continue to pass.

Documentation context:
- AGENTS.md tactical test invariants section likely needs updating to record the SMFR-05 lifecycle contracts: clearNonImpactingSeekerMovementPaths() called from completeMovePhase() after applyMovementSeekerDamage(); only movementPath cleared; movementAllowance and movementTurn preserved.
- AGENTS.md SMF-06 or SMF-07 block may need a note that drawSeekerPaths is gated on PH_MOVE || PH_SEEKER_ACTIVATION (existing wording should be confirmed or extended for SMFR-05).
- New test names for AGENTS.md: testClearNonImpactingSeekerMovementPathsPreservesBookkeeping, testNonImpactingSeekerPathClearedAfterDamageApplied, testCompleteMovePhaseCallsNonImpactingClearAfterDamageSourceContract.
- doc/rules/tactical_operations_manual.md must NOT be edited — this is an internal lifecycle fix, not a rules change.
- FTacticalGame.h file header already mentions SMFR-05 in the @brief block; documenter should verify the doxygen comment on clearNonImpactingSeekerMovementPaths() accurately reflects the AC4 invariant (only movementPath cleared, not bookkeeping fields).

Plan and diff context:
- Plan path: plans/seeker-missile-fixes-round3-plan.md
- Comparison base: sf2 branch (assumption — the worktree diverged from sf2 at the SMFR-05 implementer commit)

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/seeker-missile-fixes-round3/smfr-05

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
