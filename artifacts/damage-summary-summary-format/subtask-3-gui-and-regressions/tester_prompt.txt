You are the Tester Agent.

Task summary:
- Remediation pass for Subtask 3 strengthened deterministic regression checks for the multiline tactical damage-summary contract by adding exact line-order assertions in tactical test fixtures.

Modified files:
- tests/tactical/FTacticalDamageSummaryGUITest.cpp
- tests/tactical/FTacticalCombatReportTest.cpp

Acceptance criteria to validate (from Planner):
- GUI regression tests assert the new multi-line ship-summary output rather than the old single-line `effects:` format.
- Tactical model regression tests include examples equivalent to the requested summary-format cases.
- The dialog continues to render the summary and hit-detail sections without regressing the current empty-state behavior.
- GUI-specific validation passes in the existing tactical GUI test runner.

Create test files in:
- tests/tactical/FTacticalDamageSummaryGUITest.cpp
- tests/tactical/FTacticalCombatReportTest.cpp

Implementation context for testing:
- Added exact expected-line assertions for `<Ship>:` + bullet lines, including explicit `ADF (-N)` / `MR (-N)` bullets and ordered grouped weapon/defense bullets.
- Existing source-contract checks for TacticalDamageSummaryGUI close behavior, empty-state text, and hit-detail section were preserved.
- No production GUI code paths changed in this remediation commit.

Suggested test command(s):
- cd tests/tactical && make && ./TacticalTests
- cd tests/gui && make && xvfb-run -a ./GuiTests (assumption from plan; xvfb-run unavailable in this environment)
- Fallback used here: cd tests/gui && make && GDK_BACKEND=x11 ./GuiTests

Existing validations that fail only because approved behavior changed:
- None identified.
- Current failures observed are unrelated live-GUI flakiness in existing suite (e.g., StrategicGuiLiveTest sawPaint assertion, occasional tactical/battlesim modal lifecycle assertions).

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer missing test-location or test-command details from repository conventions when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/damage-summary-summary-format/subtask-3-gui-and-regressions

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
