You are the Tester Agent.

Task summary:
- Stabilized the direct parent-backed `TacticalDamageSummaryGUI` live test path by removing the app-level modal action race and keeping parent-relative centering plus real close-button behavior checks.

Modified files:
- tests/gui/TacticalGuiLiveTest.cpp

Acceptance criteria to validate (from Planner):
- Stabilize the direct parent-backed `TacticalDamageSummaryGUI` live test so repeated GUI reruns stop flaking.
- Preserve the centering semantic check (parent-relative with owning-top-level fallback behavior) and real close-button behavior verification.
- Keep the fix minimal and test-focused unless tightly coupled production code is required.
- Required validation target must pass:
  - `cd tests/tactical && make && ./TacticalTests`
  - `cd tests/gui && make && GDK_BACKEND=x11 ./GuiTests && GDK_BACKEND=x11 ./GuiTests && GDK_BACKEND=x11 ./GuiTests`

Create test files in:
- Assumption: no new tests needed; validate existing live GUI/tactical coverage in `tests/gui/` and `tests/tactical/`.

Implementation context for testing:
- The direct test `testTacticalDamageSummaryDialogDisplaysContextAndCloseBehavior` now uses `showModalWithAction(*dialog, ...)` for the first parent-backed damage-summary modal interaction.
- The close action still asserts `assertDialogCenteredOnParent(dialog, parent, 200)` and verifies close-button discovery/default/focus behavior with real Enter-key/button-close behavior.
- The parent frame is now created at a deterministic position (`wxPoint(120, 120)`) to reduce wm placement variance while preserving parent-centered assertion intent.
- Modal fallback timeout for this action path is now 2000ms to avoid race with assertion/action body on slower GUI runs.

Suggested test command(s):
- `cd tests/tactical && make && ./TacticalTests`
- `cd tests/gui && make && GDK_BACKEND=x11 ./GuiTests && GDK_BACKEND=x11 ./GuiTests && GDK_BACKEND=x11 ./GuiTests`
- Optional stronger stability sample: `cd tests/gui && make && for i in 1 2 3 4 5; do GDK_BACKEND=x11 ./GuiTests; done`

Startup behavior:
- If acceptance criteria and implementation context are present, begin testing work immediately and continue in the same run.
- Infer any missing command/location details from repository conventions when safe; label those choices as assumptions.
- Do not stop after activation, directory discovery, artifact-directory confirmation, or framework discovery when testing can proceed.

Shared artifact directory:
- artifacts/damage-summary-summary-format/subtask-3-gui-and-regressions

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
