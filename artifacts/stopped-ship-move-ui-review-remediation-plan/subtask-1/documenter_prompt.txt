Your role is 'documenter'. Your task is as follows:

Task summary:
- Implementer restored stopped-ship preview-route completeness so model-owned stopped-ship preview now includes current-heading forward route alongside alternate legal starting facings.
- Tester updated tactical regression coverage to validate the restored route, preview hex-heading mapping behavior, unchanged gating semantics, and continuation through normal move selection flow.

Acceptance criteria validated:
- When a moving-side ship begins PH_MOVE at `speed == 0`, `nMoved == 0`, and `MR > 0`, `getStoppedShipPreviewRoutes()` includes the current-heading forward preview in addition to alternate legal starting facings.
- The stopped-ship preview lookup surface continues to map preview-route hexes back to the legal starting heading set, including the restored current-heading route where applicable.
- The remediation does not change non-stopped movement highlight semantics or grant new preview options to ships with `MR == 0`.
- Existing stopped-ship preview-click continuation still resolves into the normal route-selection flow after the preview contract is corrected.
- The tactical model remains additive and non-wx.

Implementation branch or worktree context:
- Branch: `stopped-ship-free-rotation-plan-tester-20260510`
- Worktree: `/home/tstephen/repos/SSW-worktrees/stopped-ship-free-rotation-plan-tester-20260510`

Files modified by Implementer and Tester to inspect for documentation impact:
- src/tactical/FTacticalGame.cpp
- tests/tactical/FTacticalMoveRouteSelectionTest.cpp
- tests/tactical/FTacticalGameMechanicsTest.cpp
- tests/tactical/FTacticalBattleBoardRendererDelegationTest.cpp
- tests/tactical/FTacticalModelSelectionHexClickSurfaceTest.cpp

Test commit:
- 3474f67

Commands executed:
- `cd tests && make tactical-tests && ./tactical/TacticalTests` (baseline; 1 failing stale expectation)
- `cd tests && make tactical-tests && ./tactical/TacticalTests` (post-update; `OK (143 tests)`)

Final test outcomes:
- Tactical runner passes in full after test updates (`OK (143 tests)`).
- Coverage now explicitly validates restored current-heading preview-route participation and retains guard/fallback expectations across move-selection and renderer delegation surfaces.

Documentation context:
- Review tactical movement preview behavior notes where stopped-ship free-rotation preview expectations are described.
- Confirm any docs that describe preview heading options or route-highlight semantics match the restored 6-heading eligible preview contract.

Plan and diff context:
- Story/plan context: stopped-ship move UI review remediation (subtask 1) from shared artifacts.
- Comparison base assumption: compare tester branch changes against implementer baseline on the same task branch lineage to document net behavioral/testing delta.

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/stopped-ship-move-ui-review-remediation-plan/subtask-1

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
