Verifier Report

Scope reviewed:
- Implementer commit 375ff9b0: FGame::load() (src/strategic/FGame.cpp:717-743), after each player's p->load(is) succeeds and before the pre-existing 'place fleets into their systems' loop, iterates p->getFleetList() and validates each fleet's getLocation() (0 sentinel or must resolve via m_universe->getSystem(id)) and getJumpRoute() (FFleet::NO_ROUTE sentinel or must resolve via m_universe->getJumpRoute(id)); on the first unresolved reference it deletes the not-yet-owned FPlayer p, calls reportLoadError(...), and returns 1. Doxygen updated in include/strategic/FGame.h:218-241.
- Tester commit 4f78c7a0: three new behavioral tests added to tests/strategic/FGameSaveFormatTest.{h,cpp} -- two negative byte-patching tests (out-of-range m_location; out-of-range m_jumpRouteID) and one positive-control round trip proving sentinels (location 0, NO_ROUTE) still load cleanly.
- Documenter commit 4b073be6: extends AGENTS.md's existing save-format Contributor Notes paragraph (line 404) with a concise FF-1 addendum; no other file touched.
- Security commit 01012cc8: CONDITIONAL PASS, no BLOCKING findings against FF-1; two WARNINGs (m_destination not validated; location-0 + in-transit illegal-state combination not rejected) explicitly documented as PRE-EXISTING and OUTSIDE FF-1's plan-declared scope.
- Independently re-verified by this Verifier pass: full diff inspection of all five changed production/test/doc files, byte-offset/field-order cross-check against FFleet::save()/load(), sentinel-value cross-check against FFleet.h/FSystem.cpp, a full `make check` run (293/253/85 tests, all green), and a temporary disable-the-fix rebuild+rerun proving both negative tests genuinely fail without the fix (rc==0) and the positive control still passes -- code was fully restored via `git checkout --` immediately after, confirmed by a clean `git status` and a second full `make check` pass.

Acceptance criteria / plan reference:
- plans/save-load-robustness-followups-plan.md, Section 4 (IN/OUT scope) and Section 5 FF-1 subtask: load-time validation of exactly two fleet located-object reference IDs (location/system ID, jump-route ID) against the loaded FMap; abort nonzero on any out-of-range reference; sentinels (location 0, FFleet::NO_ROUTE) exempt; Documentation impact subsection requires Doxygen updates plus a concise AGENTS.md addendum and explicitly no doc/deferred-tasks.md or doc/UsersGuide.md change.
- Design decision D1 (RESOLVED, Section 2): fix is load-time validation (root-cause), not gui NULL guards; gui draw path intentionally unmodified by this plan.
- artifacts/save-load-robustness-followups/FF-1/security_report.md (CONDITIONAL PASS) -- consulted and reconciled against plan scope.

Convention files considered:
- AGENTS.md (Contributor Notes save-format paragraph; module boundary rules; Doxygen requirements; behavioral-verification mandate)
- CLAUDE.md (pointer to AGENTS.md)
- doc/deferred-tasks.md and doc/UsersGuide.md (confirmed correctly untouched per plan's explicit no-change instruction)
- tests/strategic/FGameSaveFormatTest.{h,cpp} existing conventions (RecordingUI mock IStrategicUI pattern, byte-patching test pattern already established by prior FR-1/P5-5 tests)

Findings

BLOCKING
- None

WARNING
- src/strategic/FGame.cpp:587-591 - Fleet located-object reference m_destination is not validated by FF-1 and reaches an unguarded NULL dereference in FGame::moveFleets() (m_universe->getSystem(fleets[i]->getDestination())->addFleet(f)) for a crafted in-transit fleet with an out-of-range, non-sentinel m_destination.
  Confirmed PRE-EXISTING (moveFleets() was not touched by FF-1) and OUTSIDE FF-1's plan-declared scope: plans/save-load-robustness-followups-plan.md Section 4/5 explicitly enumerates only the fleet location (system) ID and jump-route ID as IN scope for FF-1; m_destination validation is not part of FF-1's acceptance criteria. Verified this against the plan text directly. Not a BLOCKING finding against FF-1 as delivered, but a legitimate follow-up candidate for the coordinator/planner (mirrors the identical NO_ROUTE-style sentinel-exempt validation pattern FF-1 already established, with a NO_DESTINATION sentinel exemption).
- src/strategic/FGame.cpp:590 - A crafted save with m_inTransit=true and m_location==0 (the documented sentinel, exempted by FF-1) passes FF-1 validation but reaches an unguarded getSystem(0)->removeFleet(...) NULL dereference in moveFleets() -- an illegal in-transit+location-0 state combination that FF-1 does not reject.
  Confirmed PRE-EXISTING and OUTSIDE FF-1's plan-declared scope for the same reason as the m_destination finding: the plan's FF-1 subtask validates only location-resolves-or-is-0 and jump-route-resolves-or-is-NO_ROUTE independently, not cross-field state legality. Not BLOCKING against FF-1's acceptance criteria as written, but worth flagging to the coordinator/planner as a related follow-up (reject the in-transit+location-0 inconsistency at load, or null-guard the moveFleets() call sites).

NOTE
- src/strategic/FGame.cpp:717-728 - The inline comment (and similarly the AGENTS.md addendum) describes the fix as validating a fleet's 'located-object reference IDs' before immediately enumerating the specific two fields checked (location, jump-route); read in isolation the opening clause could be misread as covering all three located-object references a fleet serializes (location, destination, jump-route).
  Low-severity documentation-precision observation, not a correctness defect: the very next clauses in both the code comment and the Doxygen/AGENTS.md text explicitly name only location and jump-route as validated, so there is no material inaccuracy once read in full, and the plan itself (Section 4) is precise about scope. Flagged only because Security's report raised the same wording caveat; no action required for FF-1 as delivered, but future edits extending validation (e.g. the m_destination follow-up above) should tighten this phrasing.
- src/strategic/FGame.cpp:729-751 - The abort path's memory safety (delete p on the not-yet-owned FPlayer) depends on the FF-1 validation loop running before the pre-existing fleet-to-system wiring loop and before m_players.push_back(p); this ordering must be preserved by future edits to avoid dangling FSystem fleet-list pointers or a stale m_players entry.
  Verified this ordering is exactly as implemented (validation loop at lines 730-743 precedes both the wiring loop at 745-750 and push_back at 751), matching Security's independent confirmation. Documenting as a load-safety invariant for future maintainers; no action required for FF-1 as delivered.

Test sufficiency assessment:
- Sufficient and behavioral, satisfying the repository's mandatory behavioral-verification policy. All three new FGameSaveFormatTest cases construct a real save byte-stream via buildValidSaveBytes(), locate the real 'Task Force Prenglar' fleet record by its length-prefixed name tag, and either corrupt a specific field (m_location or m_jumpRouteID) or leave the stream untouched, then drive the real FGame::load() and assert on observed runtime outcomes (rc, RecordingUI showMessageCalls count, g.getPlayers().size()) -- no source-text/source-structure assertions anywhere in the new tests.
- Cross-checked the byte-offset arithmetic in both negative tests against the actual FFleet::save()/load() field order in src/strategic/FFleet.cpp (m_owner, m_location, m_inTransit(1 byte bool), m_destination, m_transitTime, m_jumpLength, m_speed, m_jumpRouteID -- all unsigned int/4-byte native fields per include/strategic/FFleet.h) and confirmed the computed offsets (locationPos = ownerPos+4; jumpRoutePos = locationPos+4+17) are correct.
- Independently confirmed the behavioral-test mandate is met, not merely claimed: temporarily wrapped the FF-1 validation loop in #if 0 in src/strategic/FGame.cpp, rebuilt src/strategic and tests/SSWTests, and reran -- both testLoadFleetWithOutOfRangeLocationIdReturnsNonzeroAndReportsExactlyOnce and testLoadFleetWithOutOfRangeJumpRouteIdReturnsNonzeroAndReportsExactlyOnce failed exactly as expected (assertion 'rc != 0' failed, i.e. rc==0 with the fix disabled), while testLoadValidSaveWithSentinelLocationAndJumpRouteSucceeds still passed unaffected. Restored the file via `git checkout --` immediately afterward; `git status` confirmed a clean tree and a full `make check` rerun afterward stayed green (293/253/85).
- The 9 unrelated FGameHeaderDependencyTest failures observed only when running SSWTests directly from a non-tests/ working directory (this agent's cwd resets between Bash calls) are a relative-path/CWD artifact of the test harness itself, not a regression -- confirmed absent when running the full `make check` sequence from repo root, which cd's into tests/ correctly and reports 293/293 passing.
- Positive-control coverage (sentinel location 0 for the not-yet-placed Strike Force Nova fleet, plus NO_ROUTE for every placed, non-transiting fleet) guards against a false-positive rejection of legitimate save data, closing the obvious edge case a validation-only test suite could otherwise miss.
- No production file was modified by the Tester -- confirmed via `git diff 879248eb 01012cc8 -- tests/strategic/FGameSaveFormatTest.h tests/strategic/FGameSaveFormatTest.cpp` showing only test-file changes, and the separate Implementer commit (375ff9b0) is the sole owner of the src/strategic/FGame.cpp and include/strategic/FGame.h diffs.

Documentation accuracy assessment:
- include/strategic/FGame.h Doxygen for load() (lines 218-241) accurately and specifically describes the FF-1 addition: location must resolve via FMap::getSystem(id) unless it is the 0 sentinel; jump-route ID must resolve via FMap::getJumpRoute(id) unless it is FFleet::NO_ROUTE; first unresolved reference aborts like a truncated/corrupt read, reported via the same reportLoadError()/IStrategicUI::showMessage() path. Matches the shipped code exactly.
- The inline src/strategic/FGame.cpp comment (lines 717-728) matches the Doxygen and the code precisely, including the sentinel rationale (FFleet's default-constructed m_location; createSFNova()'s deliberately unplaced fleet) and the crash-avoidance motivation (WXPlayerDisplay::drawFleets() NULL dereference).
- AGENTS.md's save-format Contributor Notes paragraph (line 404) addendum concisely records the FF-1 fix -- what is validated, the two sentinel exemptions, the abort/reporting mechanism, and the load-time-vs-gui-guard design decision -- without duplicating the pre-existing FR-1/FR-2/P5-5 sentences in the same paragraph; confirmed by direct diff inspection this is the only AGENTS.md change and it reads as a natural, non-redundant continuation of the existing paragraph.
- Confirmed doc/deferred-tasks.md and doc/UsersGuide.md were correctly left unchanged, matching the plan's explicit instruction that FF-1 was never a tracked deferred item and that the existing generic corrupt-save File->Open note already covers this behavior; doc/test-contracts.md is also correctly untouched since FGameSaveFormatTest is a strategic, not tactical-catalog, suite.
- Minor wording caveat (see NOTE finding above, also raised independently by Security): the opening clause of both the code comment and the AGENTS.md sentence describes validating a fleet's 'located-object reference IDs' before immediately enumerating only the two fields actually checked; not materially inaccurate once read in full, but worth tightening if/when a future follow-up (m_destination validation) is added.

Artifacts written:
- artifacts/save-load-robustness-followups/FF-1/verifier_report.md
- artifacts/save-load-robustness-followups/FF-1/verifier_result.json

Verdict:
- PASS
