Your role is 'documenter'. Your task is as follows:

Task summary:
- PGS-01: Restore pre-game mine board placement. The implementer fixed FBattleDisplay::onSetSpeed() to use an enteredMinePlacement flag so setShip(NULL) is skipped when beginMinePlacement() succeeds, and fixed FTacticalGame::beginOrdnancePlacement() to guard on the return value of selectPlacementSourceByIndex() before entering BS_PlaceMines.

Acceptance criteria validated:
- AC1: beginMinePlacement() preserves m_curShip non-NULL after entry (verified by model test Part 2 and GUI test)
- AC2: beginMinePlacement() sets m_curWeapon to mine launcher (type M) (verified by model test Part 2 and GUI test)
- AC3: placeMineAtHex() returns false when m_curShip is NULL (buggy path regression, verified by model test Part 1)
- AC4: handleHexClick() in BS_PlaceMines records mine, decrements ammo, appends placed-ordnance record (verified by GUI test)
- AC5: Source assertion updated to match new if-guard on selectPlacementSourceByIndex in beginOrdnancePlacement()

Implementation branch or worktree context:
- Implementer branch: sf2-pgs-01-tester-20260622
- Fix 1: src/tactical/FBattleDisplay.cpp onSetSpeed() - enteredMinePlacement flag skips setShip(NULL) when beginMinePlacement() succeeds
- Fix 2: src/tactical/FTacticalGame.cpp beginOrdnancePlacement() - if-guard on selectPlacementSourceByIndex() returns false before entering BS_PlaceMines

Files modified by Implementer and Tester to inspect for documentation impact:
- src/tactical/FBattleDisplay.cpp
- src/tactical/FTacticalGame.cpp
- tests/tactical/FTacticalGameMechanicsTest.cpp
- tests/tactical/FTacticalGameMechanicsTest.h
- tests/gui/TacticalGuiLiveTest.cpp
- tests/gui/TacticalGuiLiveTest.h

Test commit:
- eeb5899269ec321a3b6a3e4d4e07b395a8bc8558

Commands executed:
- make -C tests tactical-tests
- tests/tactical/TacticalTests
- make -C tests/gui
- DISPLAY=:0 tests/gui/GuiTests
- make -C tests
- tests/SSWTests

Final test outcomes:
- PASS: TacticalTests - all 215 tests pass (0 failures)
- PASS: FTacticalGameMechanicsTest::testPreGameMinePlacementPreservesShipAfterBeginMinePlacement - new behavioral model test verifies beginMinePlacement() preserves m_curShip/m_curWeapon and placeMineAtHex() records mines when ship reference is non-NULL
- PASS: TacticalGuiLiveTest::testOnSetSpeedMinePlacementPreservesShipForFirstBoardClick - new GUI behavioral test verifies FBattleScreen::beginMinePlacement() keeps getShip()/getWeapon() non-NULL and handleHexClick() records mine/decrements ammo
- PASS: Source-inspection assertion updated at FTacticalGameMechanicsTest.cpp line 586 to match new if-guard form in beginOrdnancePlacement()
- PRE-EXISTING (unrelated): 9 SSWTests failures - source-path-dependent tests running outside expected root
- PRE-EXISTING (unrelated): 10 GuiTests failures - source-contract assertions against FBattleDisplay.cpp patterns and one BattleSim dialog test

Documentation context:
- PGS-01 restores pre-game mine board placement functionality
- AGENTS.md documents pre-game mine/seeker placement as an area where source-string matching previously shipped undetected regressions
- The fix adds behavioral coverage at both model and GUI levels to prevent recurrence

Plan and diff context:
- Plan: sf2 / pregame-ordnance-placement
- Subtask: PGS-01
- Shared artifact dir: artifacts/pregame-ordnance-placement/PGS-01
- Implementation PR diff: FBattleDisplay.cpp onSetSpeed() enteredMinePlacement flag; FTacticalGame.cpp beginOrdnancePlacement() if-guard

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/pregame-ordnance-placement/PGS-01

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
