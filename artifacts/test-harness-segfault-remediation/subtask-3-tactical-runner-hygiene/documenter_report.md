## Documenter Report

### Files Updated
- **`AGENTS.md`** — Added the top-level `cd tests && make tactical-tests && ./tactical/TacticalTests` guidance so contributor test-running instructions match the implemented tactical runner clean-rebuild path.

### Summary
Updated repository-wide testing guidance to document the new top-level tactical runner hygiene command in `AGENTS.md`, matching the implemented `tests/Makefile` behavior validated by the tester.

### Commit Message
`Document tactical runner rebuild path`
