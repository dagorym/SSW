Your role is 'verifier'. Your task is as follows:

Original task summary:
- Add a minimal GitHub Actions CI workflow (.github/workflows/ci.yml) that builds the repo and runs `make check` on Ubuntu, per roadmap Phase 3 BS4 (item #3). Runs after P3-1 (common.mk / -std=c++17 pin) and P3-3 (`make check` target), both already merged. Documenter stage: document the new CI workflow in AGENTS.md and mark the roadmap item #3 annotation as fully resolved now that the CI deliverable has landed.

Acceptance criteria to verify:
- .github/workflows/ci.yml exists and is valid workflow YAML (yaml.safe_load succeeds; actionlint remained unavailable in this environment).
- The workflow declares push + pull_request (to master) + workflow_dispatch triggers, one Ubuntu job, and steps that install deps (including a wx 3.3.2 provisioning step), run `make`, and run `make check`.
- The workflow sets `permissions: contents: read` (workflow-level and job-level), pins third-party actions to a fixed ref (commit SHA with version-tag comment), and references no secrets.
- The build/test commands in the workflow (`make`, `make check`) match the locally-verified repo-root Makefile `all`/`check` targets exactly -- no CI-only divergent build path.
- Documentation reflects the new CI workflow: AGENTS.md's Build System section documents that CI runs make + make check on ubuntu-24.04 with the wx 3.3.2 source-build/cache rationale, and doc/synthesized-roadmap.md Part III item #3 annotation is updated to reflect the CI deliverable as delivered.

Implementation branch or worktree context:
- Branch: phase3-P3-4-tester-20260712, based on the merged P3-1 (common.mk / -std=c++17 pin) and P3-3 (`make check` target) work.
- Implementation/code commit: 80933b44 'P3-4: Add minimal GitHub Actions CI workflow (build + make check)'.
- Single new file: .github/workflows/ci.yml -- no other files were changed by the Implementer for this subtask.
- Test commit: No Changes Made (structural/YAML validation only; no CppUnit fixtures added).
- Documentation worktree/branch: phase3-P3-4-documenter-20260712, based on the Tester branch.
- Documentation commit: b1e530ee 'P3-4: Document minimal CI workflow and mark roadmap item #3 fully resolved'.

Files modified by the Implementer, Tester, and Documenter:
- .github/workflows/ci.yml (Implementer, new)
- AGENTS.md (Documenter, modified -- added Continuous Integration note under Build System / Linux / Make)
- doc/synthesized-roadmap.md (Documenter, modified -- Part III 'Now' item #3 annotation updated from 'RESOLVED (build-system portion)' to plain 'RESOLVED', crediting P3-4 and describing the CI workflow)

Commands executed:
- git diff --stat phase3 HEAD (confirmed only .github/workflows/ci.yml plus P3-4 artifact files changed relative to the phase3 coordination branch)
- python3 -c "assert 'Continuous Integration' in open('AGENTS.md').read()" style checks confirming the new AGENTS.md section and roadmap annotation text landed as intended
- git commit for the documentation-only change (hash b1e530ee)

Final test outcomes:
- AC1 (valid workflow YAML): PASS -- yaml.safe_load succeeds; actionlint unavailable in this environment (per Tester).
- AC2 (triggers/job/steps): PASS -- push/pull_request to master, workflow_dispatch, single ubuntu-24.04 job, wx provisioning + make + make check steps (per Tester).
- AC3 (permissions/pins/secrets): PASS -- contents: read at workflow and job level, actions pinned to 40-hex commit SHAs with version comments, no secrets references (per Tester).
- AC4 (command parity): PASS -- workflow invokes exactly `make` and `make check`, matching the repo-root Makefile all/check targets (per Tester).
- Documentation review: AGENTS.md Continuous Integration note and doc/synthesized-roadmap.md item #3 annotation verified present and accurate against the actual .github/workflows/ci.yml contents (wx 3.3.2 source build, actions/cache keyed on wx version, ubuntu-24.04 runner, push/PR/workflow_dispatch triggers).
- EXPLICIT LIMITATION (carried from Tester): this environment cannot execute GitHub Actions; no cloud run was performed or claimed.

Plan or evaluation context guidance:
- Plan: plans/phase3-build-system-ci-plan.md (Phase 3 Build System + CI plan), subtask P3-4 / roadmap Phase 3 'Now' item BS4; see Section 3 (P3-4 Documentation Impact) and Section 5 (overall Documentation Impact).
- Section 5 requires: a short 'Continuous Integration' AGENTS.md note pointing at .github/workflows/ci.yml (done), and a doc/synthesized-roadmap.md Part III item #3 annotation update reflecting the CI deliverable plus the deliberately-deferred Windows ConformanceMode flip / tactical-to-gui relocation / CMake adoption (done).
- Comparison base: the coordination branch is 'phase3'; P3-1/P3-2/P3-3 are already merged there. This Documenter pass compared phase3..HEAD and found no other substantive diff beyond .github/workflows/ci.yml and the P3-4 artifact files.

Convention-file guidance:
- Single-source-of-truth rule (AGENTS.md): canonical build/CI facts belong in AGENTS.md, not in entry-point pointer files (CLAUDE.md); no changes were made to CLAUDE.md.
- doc/rules/* is non-editable per AGENTS.md's Non-negotiable Constraints; not touched.
- No Doxygen/in-code documentation requirement applies -- the only substantive changed file is a GitHub Actions YAML workflow, not C++ source.
- Diff-first-editing: both edits were minimal, localized insertions/replacements (AGENTS.md: +4 lines new subsection; roadmap: 1-line annotation edit) with no unrelated rewrites.

Updated documentation files:
- AGENTS.md
- doc/synthesized-roadmap.md

Shared artifact directory:
- artifacts/phase3-build-system-ci/P3-4

Verification scope:
- Review implementation, tests, and updated documentation together.
- Infer missing plan-source, convention-file, or artifact-path details from repository context when safe.
- Continue in the same run when blockers are absent.

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
