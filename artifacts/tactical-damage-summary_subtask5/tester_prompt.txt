You are the Tester Agent.

Task summary:
- Verify the tactical report context preserves the `immediate` flag when `FBattleScreen` lazily seeds a report from an attack, so immediate-damage reports are not mislabeled as end-of-phase reports.

Modified files:
- src/tactical/FBattleScreen.cpp

Acceptance criteria to validate (from Planner):
- The Subtask 5 dialog/report context still supports immediate and end-of-phase report types.
- A context seeded from an attack report preserves whether it is immediate.
- The fix is minimal and does not regress the existing dialog or build wiring.

Create test files in:
- tests/tactical/

Implementation context for testing:
- `FBattleScreen::appendTacticalAttackReport()` now copies `FTacticalAttackReport::immediate` into the lazily created `FTacticalCombatReportContext`.
- `FBattleScreen::appendTacticalReportEvent()` already preserves `event.immediate`; no dialog or build wiring changes were made.
- The existing tactical damage summary dialog and its build integration remain unchanged from Subtask 5.

Suggested test command(s):
- make -C src tactical
- make -C tests/tactical
- ./tests/tactical/TacticalTests

Shared artifact directory:
- artifacts/tactical-damage-summary_subtask5
