You are the Tester Agent.

Task summary:
- Replaced fleet copy construction in TransferShipsGUI with explicit fleet creation and metadata initialization that preserves exclusive ship ownership.
- Added regression coverage for explicit ship transfer using removeShip/addShip flow.

Modified files:
- src/gui/TransferShipsGUI.cpp
- tests/strategic/FFleetTest.cpp

Acceptance criteria to validate (from Planner):
- src/gui/TransferShipsGUI.cpp no longer uses new FFleet(*m_fleet).
- The new fleet is created explicitly and populated through explicit ship transfer steps.
- Fleet metadata needed by the UI flow is preserved without reintroducing shared ownership.
- tests/strategic/FFleetTest.cpp covers the updated ownership contract or the explicit transfer workflow.

Create test files in:
- tests/strategic/FFleetTest.cpp

Implementation context for testing:
- TransferShipsGUI::onDone now creates new fleets with new FFleet and explicitly sets owner, location, militia/home-system state, and holding flag before updateFleet applies the selected ship list.
- updateFleet continues to move membership through remove/add operations against fleet ship lists.
- FFleetTest::testAddRemoveShip now verifies explicit transfer of a ship pointer from one fleet to another using removeShip and addShip, then checks source/target ship counts and presence.

Suggested test command(s):
- make -C tests/strategic libstrategicTests.a

Shared artifact directory:
- artifacts/unsafe-ffleet-copy-constructor_subtask2
