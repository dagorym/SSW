Your role is 'documenter'. Your task is as follows:

Task summary:
- Render the two deployment phases (BS_PlaceMines and BS_PlaceSeekers) with their own Done button and grammatically correct, type-specific prompt and selection text. Builds on SMF-01's model-level two-phase deployment state machine.

Acceptance criteria validated:
- AC-01/02: Mine phase prompt and selection line text verified via testMinePhaseUsesExactPromptTextAndMFilter
- AC-03: Mine phase shows only 'Mine Placement Done' button (label fixed in constructor, M-only filter); routes to completeMinePlacement()
- AC-04/05: Seeker phase prompt and selection line text verified via testDrawPlaceSeekersUsesSeekerSpecificPromptsAndSMFilter
- AC-06: Seeker phase shows only 'Seeker Placement Done' button; routes to completeSeekerPlacement()
- AC-07: Mine phase M-only filter; seeker phase SM-only filter - both verified via source-contract tests
- AC-08: No 'Weapon Placement Done' combined-label path remains (assertNotContains on full source file)
- AC-09: draw() dispatches BS_PlaceSeekers to drawPlaceSeekers(dc); onLeftUp() dispatches BS_PlaceSeekers to checkShipSelection(event)
- AC-10: m_buttonSeekerPlacementDone constructed, added to actionSizer, hidden at startup
- AC-11: getActionButtonRowBottom() includes m_buttonSeekerPlacementDone in shown-button scan
- Runtime: Minelayer in BS_PlaceMines shows 'Mine Placement Done' (shown) and 'Seeker Placement Done' (hidden); after button-click transition to BS_PlaceSeekers shows 'Seeker Placement Done' (shown) and 'Mine Placement Done' (hidden)

Implementation branch or worktree context:
- Branch: sf2-smf-02-tester-20260602 (worktree of sf2)
- Implementer modified: include/tactical/FBattleDisplay.h, src/tactical/FBattleDisplay.cpp
- drawPlaceMines() now filters to FWeapon::M only; uses mine-specific prompt text; fixed 'Mine Placement Done' label
- drawPlaceSeekers() is a new method filtering to FWeapon::SM only; seeker-specific prompt; m_buttonSeekerPlacementDone
- onSeekerPlacementDone() disconnects, hides, calls completeSeekerPlacement(), resets m_first
- BS_PlaceSeekers dispatched in draw() and onLeftUp()
- m_buttonSeekerPlacementDone constructed, added to actionSizer, hidden at startup
- getActionButtonRowBottom() scans m_buttonSeekerPlacementDone

Files modified by Implementer and Tester to inspect for documentation impact:
- include/tactical/FBattleDisplay.h
- src/tactical/FBattleDisplay.cpp
- tests/tactical/FTacticalBattleDisplayFireFlowTest.h
- tests/tactical/FTacticalBattleDisplayFireFlowTest.cpp
- tests/gui/TacticalGuiLiveTest.h
- tests/gui/TacticalGuiLiveTest.cpp

Test commit:
- 2ff820e

Commands executed:
- make -C tests tactical-tests && ./tests/tactical/TacticalTests
- make -C tests gui-tests && DISPLAY=:0 ./tests/gui/GuiTests

Final test outcomes:
- TacticalTests: 196/196 passed (includes 4 new SMF-02 source-contract tests)
- GuiTests: 36/44 passed; 8 pre-existing failures unrelated to FBattleDisplay (relative-path resolution errors in StrategicGuiLiveTest and other TacticalGuiLiveTest source-contract tests); 0 new failures
- testMinePlacementDoneButtonLabelReflectsOrdnanceTypes now passes with updated two-phase runtime behavior

Documentation context:
- SMF-02 adds a new BS_PlaceSeekers state phase with drawPlaceSeekers() and onSeekerPlacementDone() handlers in FBattleDisplay
- The two-phase deployment (mines then seekers) replaces the previous combined ordnance placement panel
- AGENTS.md CLAUDE.md may need updates if they reference the pre-game setup flow
- doc/DesignNotes.md or similar design docs may need updating for the new state machine

Plan and diff context:
- Plan file: plans/seeker-missile-fixes-round2-plan.md
- Task SMF-02 builds on SMF-01 (model-level two-phase state machine)
- Modified implementation files: include/tactical/FBattleDisplay.h, src/tactical/FBattleDisplay.cpp

Startup behavior:
- If Tester branch/worktree context and enough story and diff context are present, begin documentation work immediately and continue in the same run.
- Infer missing plan-path, comparison-base, artifact-path, or documentation-convention details from repository context when repository evidence is sufficient, and label those choices as assumptions instead of treating them as blockers.
- Do not stop after activation, scope confirmation, documentation discovery, or diff review when documentation work can proceed.

Shared artifact directory:
- artifacts/seeker-missile-fixes-round2/SMF-02

Completion gate:
- Do not report success unless all required artifacts exist and all changes are committed.
