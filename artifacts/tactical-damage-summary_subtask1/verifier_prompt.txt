You are the Verifier Agent.

Task summary:
- Validate the tactical combat summary aggregation change that prevents player-facing hull totals from double counting nested attack hull-damage events already represented by `attack.hullDamage`.

Modified implementation file under review:
- `include/tactical/FTacticalCombatReport.h`

Acceptance criteria to verify:
- A hit represented by both attack-level hull damage and matching nested hull-damage events contributes to `hullDamageTaken` exactly once.
- Standalone report-level events in `report.events` still contribute their hull damage to summaries.
- Non-hull nested effects still contribute to effect rollups.

Worktree and branch context:
- Worktree: `/tmp/ssw-worktrees/tds-st1-tester-20260322`
- Branch: `tds-st1-tester-20260322`
- Implementation/source commit before tester changes: `5bc618740843a2c21b3431290db0658def2cacc1`

Tester changes:
- Modified `tests/tactical/FTacticalCombatReportTest.h`
- Modified `tests/tactical/FTacticalCombatReportTest.cpp`

Commands executed:
- `make -C tests/tactical FTacticalCombatReportTest.o`
- `make -C tests/tactical`
- `./tests/tactical/TacticalTests`
- `make -C tests/tactical clean`

Final test outcomes:
- Attempt 1/3 passed
- Tactical suite result: `OK (31 tests)`
- Added coverage for duplicate nested hull-damage suppression on the attacked ship
- Added coverage for standalone report-level hull-damage events
- Added coverage for non-hull nested effect rollups when duplicate hull accounting is suppressed

Shared artifact directory to reuse:
- `artifacts/tactical-damage-summary_subtask1`

Verification risks and follow-up checks:
- Confirm the summary logic only suppresses duplicate hull accounting when the nested hull-damage event targets the same ship as the attack target.
- Confirm raw nested events remain preserved in `rawEvents` despite suppression from `hullDamageTaken`.
- Confirm no unrelated tactical summary behavior regressed outside the added tactical report cases.
