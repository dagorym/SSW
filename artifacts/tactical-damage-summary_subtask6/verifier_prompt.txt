You are the Verifier Agent.

Task summary:
- Validate immediate electrical-fire tactical damage reporting before movement begins.

Acceptance criteria:
- Electrical fire damage shows a summary before movement starts.
- The shared tactical summary dialog is used for the report.
- Destroyed ships remain on the board and are still shown in the summary for the report in which they were destroyed.
- `clearDestroyedShips()` runs only after the relevant dialog is acknowledged.
- Movement does not begin until the report dialog is closed.
- After the electrical-fire dialog is dismissed, control returns to the same moving player and the movement phase begins normally.

Context:
- Branch/worktree: `tds-st6-tester-20260322` in `/tmp/ssw-worktrees/tds-st6-tester-20260322`
- Implementation file changed by upstream work: `src/tactical/FBattleScreen.cpp`
- Tester added source-inspection coverage in `tests/tactical/FTacticalBattleScreenElectricalFireTest.{h,cpp}`
- Tactical test runner wiring was updated in `tests/tactical/Makefile` and `tests/tactical/TacticalTests.cpp`
- Shared artifact directory to reuse: `artifacts/tactical-damage-summary_subtask6`

Commands executed:
- `make -C src tactical`
- `make -C src gui`
- `make -C tests/tactical`
- `./tests/tactical/TacticalTests`
- `make -C tests/tactical clean`

Final test outcome:
- Tactical test suite passed: `OK (20 tests)`
- The new electrical-fire report sequencing checks passed.
- No acceptance criteria remain unmet based on the tested source flow.

Notes for verification:
- The tests are source-inspection based because the tactical GUI path is modal and the repository’s existing tactical tests use source validation for flow guarantees.
- The remaining verification risk is limited to runtime behavior outside the static control-flow assertions already covered by the test suite.
